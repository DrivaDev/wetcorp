---
phase: 03-oc-wizard-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/wizard-types.ts
  - src/lib/wizard-calculations.ts
  - src/app/(importador)/importador/oc/nueva/page.tsx
  - src/components/wizard/Step1Form.tsx
  - src/components/wizard/ProductosTable.tsx
autonomous: true
requirements:
  - OC1-01
  - OC1-02
  - OC1-03
  - OC1-05
  - CALC-01
  - CALC-02
  - CALC-03

must_haves:
  truths:
    - "El importador puede ver el formulario de Step 1 en /importador/oc/nueva?step=1"
    - "El importador puede ingresar info general (referencia, proveedor, mails, país, fechas, tipo de cambio, estado, notas)"
    - "El importador puede agregar y eliminar filas de productos en la tabla dinámica"
    - "El total de cada fila (Cantidad × Valor USD) se recalcula en tiempo real con decimal.js"
    - "El FOB Total (suma de todos los totales) se muestra en USD y en ARS"
    - "El botón Trash2 está deshabilitado cuando queda solo 1 producto"
    - "El botón Continuar a Paso 2 está deshabilitado cuando el formulario no es válido"
  artifacts:
    - path: "src/lib/wizard-types.ts"
      provides: "Tipos TypeScript para todo el wizard: ProductRow, InfoGeneralState, GastosDespacho, GastosDespachante, GastosAdicionales, OtroGastoRow, Step1Data"
      exports: ["ProductRow", "InfoGeneralState", "GastosDespacho", "GastosDespachante", "GastosAdicionales", "OtroGastoRow", "Step1Data"]
    - path: "src/lib/wizard-calculations.ts"
      provides: "Funciones puras de cálculo financiero con decimal.js"
      exports: ["calcTotalFila", "calcFOBTotal", "arsToUSD", "usdToARS", "calcSubtotalDespacho", "calcSubtotalDespachante", "calcSubtotalAdicionales", "calcSubtotalOtros", "calcTotalGastos", "calcLandedCost", "isStep1Valid", "formatUSD", "formatARS"]
    - path: "src/app/(importador)/importador/oc/nueva/page.tsx"
      provides: "Server Component wrapper que lee searchParams.step (Promise en Next.js 16) y renderiza WizardPage"
    - path: "src/components/wizard/Step1Form.tsx"
      provides: "Formulario controlado de Step 1 con info general y tabla de productos"
    - path: "src/components/wizard/ProductosTable.tsx"
      provides: "Tabla dinámica de productos con add/remove filas y cálculos en tiempo real"
  key_links:
    - from: "src/app/(importador)/importador/oc/nueva/page.tsx"
      to: "src/components/wizard/WizardPage.tsx"
      via: "prop initialStep pasado al Client Component"
      pattern: "initialStep.*step"
    - from: "src/components/wizard/ProductosTable.tsx"
      to: "src/lib/wizard-calculations.ts"
      via: "calcTotalFila y calcFOBTotal en onChange"
      pattern: "calcTotalFila|calcFOBTotal"
    - from: "src/components/wizard/Step1Form.tsx"
      to: "src/lib/wizard-types.ts"
      via: "tipos InfoGeneralState y ProductRow"
      pattern: "InfoGeneralState|ProductRow"
---

<objective>
Instalar decimal.js, crear los tipos del wizard, las funciones de cálculo financiero, la page.tsx del wizard (Server Component con await searchParams), el formulario de Step 1 (info general) y la tabla dinámica de productos con recálculo en tiempo real.

Purpose: Establece la base tipada y computacional del wizard. Sin estos tipos y funciones, los planes 03-02 y 03-03 no pueden implementarse correctamente. La page.tsx crea el punto de entrada de la ruta.

Output: decimal.js instalado, wizard-types.ts, wizard-calculations.ts, page.tsx, Step1Form.tsx, ProductosTable.tsx
</objective>

<execution_context>
@C:\Users\Equipo\Documents\DrivaOC\.planning\phases\03-oc-wizard-ui\execute-plan.md
@C:\Users\Equipo\Documents\DrivaOC\.planning\phases\03-oc-wizard-ui\summary.md
</execution_context>

<context>
@C:\Users\Equipo\Documents\DrivaOC\.planning\ROADMAP.md
@C:\Users\Equipo\Documents\DrivaOC\.planning\STATE.md

@C:\Users\Equipo\Documents\DrivaOC\.planning\phases\03-oc-wizard-ui\03-CONTEXT.md
@C:\Users\Equipo\Documents\DrivaOC\.planning\phases\03-oc-wizard-ui\03-RESEARCH.md
@C:\Users\Equipo\Documents\DrivaOC\.planning\phases\03-oc-wizard-ui\03-UI-SPEC.md
@C:\Users\Equipo\Documents\DrivaOC\.planning\phases\03-oc-wizard-ui\03-PATTERNS.md

<interfaces>
<!-- Tipos y contratos extraídos del codebase existente. No re-explorar. -->

De src/lib/mock-ocs.ts (modelo de referencia para compatibilidad Phase 5):
```typescript
export type EstadoOC =
  | 'borrador'
  | 'en_proceso'
  | 'en_transito'
  | 'en_aduana'
  | 'entregada'
  | 'cancelada'

export interface OC {
  id: string
  numeroOC: string
  proveedor: string
  emailProveedor: string
  despachante: string
  emailDespachante: string
  numeroDespacho: string
  estado: EstadoOC
  fecha: string
}
```

De src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string  // clsx + tailwind-merge
```

De src/components/dashboard/FilterBar.tsx (clases de input/select establecidas en el proyecto):
```typescript
const inputClass =
  'w-full sm:w-56 px-4 py-2 rounded-lg border border-acento bg-white text-base text-texto placeholder:text-texto/50 focus:outline-none focus:ring-2 focus:ring-principal/30 focus:border-principal transition-colors duration-150'

const selectClass =
  'w-full sm:w-48 px-4 py-2 rounded-lg border border-acento bg-white text-base text-texto focus:outline-none focus:ring-2 focus:ring-principal/30 focus:border-principal transition-colors duration-150 cursor-pointer'
```

De src/components/dashboard/OCTable.tsx (patrón de tabla + touch targets):
```typescript
// Estructura de tabla con overflow scroll:
<div className="w-full overflow-x-auto rounded-xl border border-acento bg-white">
  <table className={cn('w-full text-base', minWidth)}>

// Fila de tabla:
<tr key={oc.id} className="border-b border-acento/50 hover:bg-acento/20 transition-colors duration-150">

// Botón icono con touch target:
<button
  className="min-h-[44px] min-w-[44px] flex items-center justify-center rounded-lg text-red-600 hover:text-red-700 hover:bg-red-50 transition-colors duration-150"
  aria-label="Eliminar OC"
>
  <Trash2 size={18} />
</button>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar decimal.js + crear wizard-types.ts y wizard-calculations.ts</name>
  <files>
    src/lib/wizard-types.ts
    src/lib/wizard-calculations.ts
    package.json (modificado por npm install)
  </files>
  <read_first>
    - src/lib/mock-ocs.ts — interfaz OC existente; los tipos del wizard deben ser compatibles con estos campos para la migración de Phase 5
    - src/lib/utils.ts — confirmar que cn() está disponible con import { cn } from '@/lib/utils'
    - .planning/phases/03-oc-wizard-ui/03-RESEARCH.md §Pattern 1, §Pattern 4, §Code Examples — patrones exactos de decimal.js que se deben usar
    - .planning/phases/03-oc-wizard-ui/03-CONTEXT.md §Specifics — reglas de conversión de divisas (Flete USD → ARS, Otros gastos mixtos)
  </read_first>
  <action>
**Paso 1: Instalar decimal.js**
```bash
npm install decimal.js
```
decimal.js 10.6.0 incluye tipos TypeScript nativos — no instalar @types/decimal.js.

**Paso 2: Crear src/lib/wizard-types.ts**

Exportar exactamente estos tipos (todos los campos numéricos como `string` — inputs controlados, nunca `number`):

```typescript
import type { EstadoOC } from './mock-ocs'

export interface ProductRow {
  id: string          // crypto.randomUUID() — key estable en React
  producto: string    // requerido
  descripcion: string // opcional
  cantidad: string    // string para input controlado; Decimal solo en calc
  valorUSD: string    // string para input controlado
}

export interface InfoGeneralState {
  referenciaOC: string
  estado: EstadoOC
  proveedor: string
  emailProveedor: string
  emailDespachante: string
  paisOrigen: string
  fechaOC: string          // 'YYYY-MM-DD' — input type="date"
  llegadaEstimada: string  // 'YYYY-MM-DD' — input type="date", opcional
  tipoCambio: string       // string para input controlado; > 0 para validar
  divisa: 'ARS/USD' | 'ARS/EUR'
  notas: string            // opcional
}

export interface GastosDespacho {
  sim: string
  derechos: string
  otros: string
}

export interface GastosDespachante {
  terminal: string
  fleteInternacional: string  // USD — se convierte a ARS para subtotal
  fleteInterno: string        // ARS
  senasa: string              // ARS
  despachante: string         // ARS
}

export interface GastosAdicionales {
  depositoFiscal: string
  digitalizacion: string
  estanciaCamion: string
  iibb: string
}

export interface OtroGastoRow {
  id: string
  descripcion: string
  monto: string
  divisa: 'ARS' | 'USD'
}

export interface Step1Data {
  info: InfoGeneralState
  productos: ProductRow[]
}
```

**Paso 3: Crear src/lib/wizard-calculations.ts**

Todas las funciones son puras (sin side effects). Usar `import { Decimal } from 'decimal.js'` (named import, no default). Nunca usar operadores aritméticos nativos (`*`, `/`, `+`, `-`) para valores monetarios.

```typescript
import { Decimal } from 'decimal.js'
import type {
  ProductRow,
  InfoGeneralState,
  GastosDespacho,
  GastosDespachante,
  GastosAdicionales,
  OtroGastoRow,
  Step1Data,
} from './wizard-types'

// --- Productos ---

export function calcTotalFila(row: ProductRow): Decimal {
  const cant = new Decimal(row.cantidad || '0')
  const val  = new Decimal(row.valorUSD || '0')
  return cant.times(val)
}

export function calcFOBTotal(rows: ProductRow[]): Decimal {
  return rows.reduce(
    (acc, row) => acc.plus(calcTotalFila(row)),
    new Decimal(0)
  )
}

// --- Conversión de divisas ---

export function arsToUSD(montoPesos: string, tipoCambio: string): Decimal {
  const monto = new Decimal(montoPesos || '0')
  const tc    = new Decimal(tipoCambio || '1')
  if (tc.isZero()) return new Decimal(0)
  return monto.dividedBy(tc)
}

export function usdToARS(montoUSD: string, tipoCambio: string): Decimal {
  return new Decimal(montoUSD || '0').times(new Decimal(tipoCambio || '0'))
}

// --- Subtotales de gastos (todos devuelven Decimal en USD equivalente) ---

export function calcSubtotalDespacho(g: GastosDespacho, tc: string): Decimal {
  const sim      = arsToUSD(g.sim, tc)
  const derechos = arsToUSD(g.derechos, tc)
  const otros    = arsToUSD(g.otros, tc)
  return sim.plus(derechos).plus(otros)
}

export function calcSubtotalDespachante(g: GastosDespachante, tc: string): Decimal {
  const fleteUSD    = new Decimal(g.fleteInternacional || '0')
  const terminal    = arsToUSD(g.terminal, tc)
  const fleteIntARS = arsToUSD(g.fleteInterno, tc)
  const senasa      = arsToUSD(g.senasa, tc)
  const despachante = arsToUSD(g.despachante, tc)
  return fleteUSD.plus(terminal).plus(fleteIntARS).plus(senasa).plus(despachante)
}

export function calcSubtotalAdicionales(g: GastosAdicionales, tc: string): Decimal {
  const deposito   = arsToUSD(g.depositoFiscal, tc)
  const digital    = arsToUSD(g.digitalizacion, tc)
  const estancia   = arsToUSD(g.estanciaCamion, tc)
  const iibb       = arsToUSD(g.iibb, tc)
  return deposito.plus(digital).plus(estancia).plus(iibb)
}

export function calcSubtotalOtros(rows: OtroGastoRow[], tc: string): Decimal {
  return rows.reduce((acc, row) => {
    if (row.divisa === 'USD') {
      return acc.plus(new Decimal(row.monto || '0'))
    }
    return acc.plus(arsToUSD(row.monto, tc))
  }, new Decimal(0))
}

// --- Total de gastos y Landed Cost ---

export function calcTotalGastos(
  despacho: GastosDespacho,
  despachante: GastosDespachante,
  adicionales: GastosAdicionales,
  otros: OtroGastoRow[],
  tc: string
): Decimal {
  return calcSubtotalDespacho(despacho, tc)
    .plus(calcSubtotalDespachante(despachante, tc))
    .plus(calcSubtotalAdicionales(adicionales, tc))
    .plus(calcSubtotalOtros(otros, tc))
}

export function calcLandedCost(fobUSD: Decimal, gastosUSD: Decimal): Decimal {
  return fobUSD.plus(gastosUSD)
}

// --- Validación Step 1 ---

export function isStep1Valid(
  info: InfoGeneralState,
  productos: ProductRow[]
): boolean {
  const hasRequiredInfo =
    info.referenciaOC.trim() !== '' &&
    info.proveedor.trim() !== '' &&
    info.emailProveedor.trim() !== '' &&
    info.emailDespachante.trim() !== '' &&
    info.paisOrigen.trim() !== '' &&
    info.fechaOC !== '' &&
    new Decimal(info.tipoCambio || '0').greaterThan(0)

  const hasValidProducts =
    productos.length >= 1 &&
    productos.every(
      (r) =>
        r.producto.trim() !== '' &&
        new Decimal(r.cantidad || '0').greaterThan(0) &&
        new Decimal(r.valorUSD || '0').greaterThanOrEqualTo(0)
    )

  return hasRequiredInfo && hasValidProducts
}

// --- Formateo para UI ---

export function formatUSD(d: Decimal): string {
  return `USD ${d.toFixed(2)}`
}

export function formatARS(d: Decimal): string {
  return `$ ${d.toFixed(2)}`
}
```
  </action>
  <verify>
    <automated>cd C:\Users\Equipo\Documents\DrivaOC && node -e "const { Decimal } = require('decimal.js'); console.log(new Decimal('1.1').plus('2.2').toFixed(2))" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - package.json contiene `"decimal.js"` en dependencies: `grep -c "decimal.js" package.json` devuelve 1
    - wizard-types.ts exporta los 7 tipos requeridos: `grep -c "^export" src/lib/wizard-types.ts` devuelve >= 7
    - wizard-calculations.ts usa named import: `grep -c "import { Decimal } from 'decimal.js'" src/lib/wizard-calculations.ts` devuelve 1
    - wizard-calculations.ts no usa operadores nativos para dinero: `grep -v "^//" src/lib/wizard-calculations.ts | grep -c "[^a-zA-Z0-9_'\"]\*[^*]"` devuelve 0 (sin multiplicación nativa)
    - wizard-calculations.ts exporta formatUSD y formatARS: `grep -c "export function formatUSD\|export function formatARS" src/lib/wizard-calculations.ts` devuelve 2
    - wizard-types.ts no contiene campos de tipo `number` (todos son `string`): `grep -c ": number" src/lib/wizard-types.ts` devuelve 0
    - TypeScript compila sin errores: `npx tsc --noEmit` no reporta errores en wizard-types.ts ni wizard-calculations.ts
  </acceptance_criteria>
  <done>decimal.js instalado; wizard-types.ts y wizard-calculations.ts creados con tipos explícitos sin `any`, funciones de cálculo usando decimal.js, y formateo de UI.</done>
</task>

<task type="auto">
  <name>Task 2: Crear page.tsx del wizard + Step1Form.tsx + ProductosTable.tsx</name>
  <files>
    src/app/(importador)/importador/oc/nueva/page.tsx
    src/components/wizard/Step1Form.tsx
    src/components/wizard/ProductosTable.tsx
  </files>
  <read_first>
    - src/lib/wizard-types.ts — interfaces InfoGeneralState, ProductRow, Step1Data (recién creadas en Task 1)
    - src/lib/wizard-calculations.ts — calcTotalFila, calcFOBTotal, isStep1Valid, formatUSD, formatARS (recién creadas en Task 1)
    - src/app/(importador)/importador/dashboard/page.tsx — patrón de layout wrapper del route group
    - src/components/dashboard/FilterBar.tsx — inputClass y selectClass exactos del proyecto (reusar en Step1Form)
    - src/components/dashboard/OCTable.tsx — patrón de tabla con overflow-x-auto, thead, tr, botón Trash2 con touch target
    - .planning/phases/03-oc-wizard-ui/03-UI-SPEC.md §Tabla de Productos, §Layout del Wizard, §Contrato de Inputs, §Jerarquía de Botones — valores exactos de clases Tailwind
    - .planning/phases/03-oc-wizard-ui/03-CONTEXT.md §D-01 a D-10 — comportamiento de navegación y validación
    - .planning/phases/03-oc-wizard-ui/03-RESEARCH.md §Pattern 3 — searchParams como Promise en Next.js 16 (await obligatorio)
  </read_first>
  <action>
**Archivo 1: src/app/(importador)/importador/oc/nueva/page.tsx**

Server Component async — NO tiene 'use client'. Lee searchParams como Promise (Next.js 16). Pasa initialStep a WizardPage como Client Component.

```typescript
import { WizardPage } from '@/components/wizard/WizardPage'

interface PageProps {
  searchParams: Promise<{ step?: string }>
}

export default async function NuevaOCPage({ searchParams }: PageProps) {
  const { step } = await searchParams
  return <WizardPage initialStep={step ?? '1'} />
}
```

Nota: WizardPage aún no existe — se crea en plan 03-03. Pero la page.tsx debe existir con este contrato para que los demás componentes estén en su ruta correcta. Si hay un error de TypeScript por WizardPage no encontrado, crear un stub temporal:
```typescript
// src/components/wizard/WizardPage.tsx — stub temporal hasta plan 03-03
'use client'
export function WizardPage({ initialStep }: { initialStep: string }) {
  return <div>Step {initialStep}</div>
}
```

**Archivo 2: src/components/wizard/ProductosTable.tsx**

Client Component. Recibe los props necesarios desde Step1Form — no maneja su propio estado.

Props interface:
```typescript
interface ProductosTableProps {
  productos: ProductRow[]
  onAdd: () => void
  onRemove: (id: string) => void
  onChange: (id: string, field: keyof Omit<ProductRow, 'id'>, value: string) => void
}
```

Estructura de la tabla:
- Contenedor: `<div className="w-full overflow-x-auto rounded-xl border border-acento">`
- Tabla: `<table className="w-full text-base min-w-[700px]">`
- thead con `bg-fondo border-b border-acento`. Columnas y anchos exactos de UI-SPEC.md:
  - `#` — `w-[40px] px-3 py-3 text-sm font-normal text-titulares text-left`
  - `Producto *` — `min-w-[180px]`
  - `Descripción` — `min-w-[200px]`
  - `Cantidad *` — `w-[100px]`
  - `Valor unitario (USD) *` — `w-[140px]`
  - `Total (USD)` — `w-[120px]`
  - `Acciones` — `w-[56px]`
- Filas: `key={row.id}` (UUID — NUNCA `key={index}`). Clases: `border-b border-acento/50 hover:bg-acento/20 transition-colors duration-150`
- Inputs dentro de td: `h-9 border-0 bg-transparent focus:bg-white focus:ring-1 focus:ring-principal rounded px-2 w-full`
- Celda Total por fila (read-only): `bg-fondo font-bold text-titulares px-3 text-right`. Calcular con `calcTotalFila(row).toFixed(2)`.
- Botón Trash2: cuando `productos.length === 1` → `disabled`, aria-label="No es posible eliminar el único producto", clases `text-texto/20 cursor-not-allowed min-h-[44px] min-w-[44px] flex items-center justify-center`. Cuando activo: `min-h-[44px] min-w-[44px] flex items-center justify-center rounded-lg text-red-500 hover:text-red-700 hover:bg-red-50 transition-colors`, aria-label="Eliminar producto".

Fila de totales al pie (fuera de tbody, dentro de tfoot o como último tr con clases distintas):
- `border-t-2 border-acento bg-fondo`
- TD con "FOB Total" en `text-right text-sm font-normal text-titulares`
- TD con valor USD: `text-base font-bold text-titulares`
- TD con valor ARS (pasado como prop `tipoCambio` desde Step1Form): `text-sm font-normal text-titulares/60`

Botón "+ Agregar producto" debajo de la tabla:
```
<button
  type="button"
  onClick={onAdd}
  className="flex items-center gap-2 text-sm font-normal text-principal hover:text-titulares transition-colors min-h-[44px]"
>
  <Plus size={16} />
  + Agregar producto
</button>
```

**Archivo 3: src/components/wizard/Step1Form.tsx**

Client Component con 'use client'. Toda la lógica de estado del Step 1 vive aquí.

Estado local:
```typescript
const [info, setInfo] = useState<InfoGeneralState>({
  referenciaOC: '', estado: 'borrador', proveedor: '', emailProveedor: '',
  emailDespachante: '', paisOrigen: '', fechaOC: '', llegadaEstimada: '',
  tipoCambio: '', divisa: 'ARS/USD', notas: ''
})
const [productos, setProductos] = useState<ProductRow[]>([
  { id: crypto.randomUUID(), producto: '', descripcion: '', cantidad: '', valorUSD: '' }
])
```

Handlers de tabla (per Research §Pattern 1):
```typescript
const addProducto = () =>
  setProductos(prev => [...prev, { id: crypto.randomUUID(), producto: '', descripcion: '', cantidad: '', valorUSD: '' }])

const removeProducto = (id: string) =>
  setProductos(prev => prev.filter(r => r.id !== id))

const updateProducto = (id: string, field: keyof Omit<ProductRow, 'id'>, value: string) =>
  setProductos(prev => prev.map(r => r.id === id ? { ...r, [field]: value } : r))
```

Handler onContinuar (per D-05):
```typescript
const handleContinuar = () => {
  const step1Data: Step1Data = { info, productos }
  sessionStorage.setItem('oc-step1-draft', JSON.stringify(step1Data))
  router.push('/importador/oc/nueva?step=2')
}
```

Layout del formulario de info general: `grid grid-cols-1 sm:grid-cols-2 gap-4`

Campos de Info General (per OC1-01 de REQUIREMENTS.md):
1. Referencia OC — input text, requerido
2. Estado — select con opciones: `borrador | en_proceso | en_transito | en_aduana | entregada | cancelada`
3. Proveedor — input text, requerido
4. Mail proveedor — input email, requerido
5. Mail despachante — input email, requerido
6. País de origen — input text, requerido
7. Fecha OC — input type="date", requerido
8. Llegada estimada — input type="date", opcional
9. Tipo de cambio + Divisa — input number (`tipoCambio`) + select (`divisa`: ARS/USD, ARS/EUR) en la misma fila
10. Notas — textarea, opcional, `col-span-full sm:col-span-2`

Clases de input del proyecto (de FilterBar.tsx — usar exactamente):
```
w-full px-4 py-2 rounded-lg border border-acento bg-white text-base text-texto placeholder:text-texto/50 focus:outline-none focus:ring-2 focus:ring-principal/30 focus:border-principal transition-colors duration-150
```
Clases de select del proyecto (de FilterBar.tsx — usar exactamente):
```
w-full px-4 py-2 rounded-lg border border-acento bg-white text-base text-texto focus:outline-none focus:ring-2 focus:ring-principal/30 focus:border-principal transition-colors duration-150 cursor-pointer appearance-none
```

Estado de error: cuando el usuario hace click en "Continuar a Paso 2" y el formulario es inválido, mostrar borde `border-red-500` en campos vacíos requeridos y texto `<p className="mt-1 text-xs text-red-600">Este campo es obligatorio</p>` debajo del campo. Para cantidad/valorUSD inválidos: "Ingresa un valor mayor a 0". Para tipoCambio: "Ingresa un tipo de cambio válido".

Mostrar tabla ProductosTable con los props correspondientes.

FOB Total debajo de la tabla: valor USD calculado con `calcFOBTotal(productos)` y subtítulo ARS con `usdToARS`.

Footer de navegación:
```
<div className="flex items-center justify-end mt-8 pt-6 border-t border-acento">
  <button
    type="button"
    onClick={handleContinuar}
    disabled={!isStep1Valid(info, productos)}
    className={cn(
      'px-6 py-3 rounded-lg font-bold min-h-[44px] transition-colors',
      isStep1Valid(info, productos)
        ? 'bg-principal text-white hover:bg-titulares'
        : 'bg-principal/40 text-white/70 cursor-not-allowed'
    )}
  >
    Continuar a Paso 2
  </button>
</div>
```

Título del step (per D-01 — sin indicador de progreso):
```
<h1 className="text-2xl font-bold text-titulares">Paso 1: Información General</h1>
```

Contenedor general del wizard:
```
<div className="max-w-4xl mx-auto px-4 sm:px-6 py-8 sm:py-12 flex flex-col gap-8">
```
  </action>
  <verify>
    <automated>cd C:\Users\Equipo\Documents\DrivaOC && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx existe y es async: `grep -c "export default async function NuevaOCPage" src/app/(importador)/importador/oc/nueva/page.tsx` devuelve 1
    - page.tsx usa await searchParams: `grep -c "await searchParams" src/app/(importador)/importador/oc/nueva/page.tsx` devuelve 1
    - page.tsx NO tiene 'use client': `grep -c "'use client'" src/app/(importador)/importador/oc/nueva/page.tsx` devuelve 0
    - Step1Form.tsx tiene 'use client': `grep -c "'use client'" src/components/wizard/Step1Form.tsx` devuelve 1
    - Step1Form.tsx usa sessionStorage para onContinuar: `grep -c "sessionStorage.setItem" src/components/wizard/Step1Form.tsx` devuelve 1
    - ProductosTable.tsx usa key={row.id} (no key={index}): `grep -c "key={row.id}" src/components/wizard/ProductosTable.tsx` devuelve 1
    - ProductosTable.tsx importa calcTotalFila de wizard-calculations: `grep -c "calcTotalFila" src/components/wizard/ProductosTable.tsx` devuelve >= 1
    - ProductosTable.tsx tiene min-h-[44px] en botón Trash2: `grep -c "min-h-\[44px\]" src/components/wizard/ProductosTable.tsx` devuelve >= 1
    - Step1Form.tsx importa isStep1Valid: `grep -c "isStep1Valid" src/components/wizard/Step1Form.tsx` devuelve >= 1
    - npx tsc --noEmit no reporta errores en los archivos nuevos
  </acceptance_criteria>
  <done>page.tsx del wizard como Server Component async con await searchParams; Step1Form con estado completo, validación y onContinuar con sessionStorage; ProductosTable con add/remove/recálculo en tiempo real y key={row.id} estable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| input→React state | Valores de texto libre ingresados por el importador llegan a useState |
| React state→sessionStorage | Step1Data serializado a JSON en el browser |
| sessionStorage→Step2Form | Datos deserializados al montar Step 2 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | inputs del wizard | accept | React escapa automáticamente valores en JSX; no se usa dangerouslySetInnerHTML en ningún componente del wizard. Sin backend en Phase 3, no hay consecuencias de seguridad real. |
| T-03-02 | Tampering | sessionStorage | accept | En Phase 3 no hay backend — los datos de sessionStorage solo afectan la UI del cliente. Phase 5 validará todo server-side antes de persistir. |
| T-03-03 | Elevation of Privilege | /importador/oc/nueva | accept | Middleware de Phase 1 protege /importador/* — el wizard hereda esa protección sin cambios necesarios. |
| T-03-04 | Tampering | decimal.js inputs | mitigate | Todos los campos numéricos se inicializan con `new Decimal(value || '0')` para evitar NaN/Infinity. isStep1Valid verifica que tipoCambio > 0 antes de permitir cualquier división. |
</threat_model>

<verification>
Después de completar ambas tareas, verificar:
1. `npm run build` o `npx next build` no reporta errores de TypeScript ni de módulos
2. `npm run dev` arranca sin errores
3. Navegar a `http://localhost:3000/importador/oc/nueva` muestra el formulario de Step 1
4. Agregar 2 productos, ingresar cantidades y valores, verificar que el Total de cada fila y el FOB Total se actualizan en tiempo real
5. Con solo 1 producto, el botón Trash2 debe mostrarse en estado deshabilitado (`text-texto/20 cursor-not-allowed`)
6. Sin rellenar campos requeridos, el botón "Continuar a Paso 2" debe estar deshabilitado
7. Al rellenar todos los campos requeridos + mínimo 1 producto válido, el botón se habilita
</verification>

<success_criteria>
- decimal.js instalado y verificado en package.json
- wizard-types.ts exporta los 7 tipos con campos numéricos como string
- wizard-calculations.ts exporta las 13 funciones usando decimal.js exclusivamente para aritmética
- page.tsx es Server Component async, lee searchParams como Promise, pasa initialStep a WizardPage
- Step1Form.tsx renderiza todos los campos de OC1-01 en grid sm:grid-cols-2
- ProductosTable.tsx permite agregar/eliminar filas con key={row.id}, recalcula totales en tiempo real, botón Trash2 deshabilitado con 1 sola fila
- isStep1Valid controla el estado disabled del botón "Continuar a Paso 2"
- onContinuar serializa Step1Data a sessionStorage['oc-step1-draft'] y navega a ?step=2
</success_criteria>

<output>
Después de completar, crear `.planning/phases/03-oc-wizard-ui/03-01-SUMMARY.md` con:
- Archivos creados/modificados
- Decisiones de implementación tomadas (D-01, D-05, D-07, D-09, D-10)
- Interfaces exportadas por wizard-types.ts y wizard-calculations.ts (listado completo)
- STORAGE_KEY usado: 'oc-step1-draft'
- Cualquier desviación del plan y su justificación
</output>
