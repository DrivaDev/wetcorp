---
phase: 03-oc-wizard-ui
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/components/wizard/ResumenStep1.tsx
  - src/components/wizard/GastosCard.tsx
  - src/components/wizard/OtrosGastosSection.tsx
  - src/components/wizard/Step2Form.tsx
autonomous: true
requirements:
  - OC2-01
  - OC2-02
  - OC2-03
  - OC2-04
  - OC2-05
  - OC2-06

must_haves:
  truths:
    - "Step 2 muestra el resumen read-only de la info general y la lista de productos del Step 1"
    - "El importador puede ingresar los gastos de Despacho (SIM, Derechos, Otros) en ARS"
    - "El importador puede ingresar los gastos de Despachante (Terminal, Flete int USD, Flete interno ARS, SENASA, Despachante) en sus divisas correctas"
    - "El importador puede ingresar los gastos Adicionales (Depósito fiscal, Digitalización, Estancia camión, IIBB) en ARS"
    - "El importador puede agregar y eliminar gastos libres en la sección Otros gastos (Descripción + Monto + Divisa ARS/USD)"
    - "Cada card de sección muestra su subtotal en tiempo real calculado con decimal.js"
    - "El total global de gastos se muestra en USD equivalente + subtítulo en ARS, recalculado en tiempo real"
  artifacts:
    - path: "src/components/wizard/ResumenStep1.tsx"
      provides: "Componente read-only que muestra info general y productos del Step 1"
    - path: "src/components/wizard/GastosCard.tsx"
      provides: "Card reutilizable para cada sección de gastos fijos con inputs + subtotal"
    - path: "src/components/wizard/OtrosGastosSection.tsx"
      provides: "Sección dinámica de gastos libres (agregar/eliminar) con select de divisa"
    - path: "src/components/wizard/Step2Form.tsx"
      provides: "Formulario completo de Step 2 con estado de gastos, botones Volver/Guardar OC, y exposición de datos para las ValueCards del plan 03-03"
  key_links:
    - from: "src/components/wizard/Step2Form.tsx"
      to: "sessionStorage['oc-step1-draft']"
      via: "useEffect al montar — recupera Step1Data y redirige a step=1 si está vacío"
      pattern: "sessionStorage.getItem.*oc-step1-draft"
    - from: "src/components/wizard/Step2Form.tsx"
      to: "src/lib/wizard-calculations.ts"
      via: "calcSubtotalDespacho, calcSubtotalDespachante, calcSubtotalAdicionales, calcSubtotalOtros, calcTotalGastos"
      pattern: "calcSubtotal|calcTotalGastos"
    - from: "src/components/wizard/GastosCard.tsx"
      to: "src/lib/wizard-calculations.ts"
      via: "subtotal calculado y pasado como prop"
      pattern: "subtotal"
---

<objective>
Construir los componentes de Step 2: el resumen read-only del Step 1, las 4 cards de gastos (3 fijos + 1 dinámica) con subtotales en tiempo real, y el formulario completo Step2Form que orquesta el estado de gastos y expone los valores para las ValueCards (plan 03-03).

Purpose: Step 2 es el núcleo financiero del wizard. Sin estos componentes, no es posible capturar los gastos de importación ni calcular el Costo Landed Total.

Output: ResumenStep1.tsx, GastosCard.tsx, OtrosGastosSection.tsx, Step2Form.tsx
</objective>

<execution_context>
@C:\Users\Equipo\Documents\DrivaOC\.planning\phases\03-oc-wizard-ui\execute-plan.md
@C:\Users\Equipo\Documents\DrivaOC\.planning\phases\03-oc-wizard-ui\summary.md
</execution_context>

<context>
@C:\Users\Equipo\Documents\DrivaOC\.planning\ROADMAP.md
@C:\Users\Equipo\Documents\DrivaOC\.planning\STATE.md

@C:\Users\Equipo\Documents\DrivaOC\.planning\phases\03-oc-wizard-ui\03-CONTEXT.md
@C:\Users\Equipo\Documents\DrivaOC\.planning\phases\03-oc-wizard-ui\03-RESEARCH.md
@C:\Users\Equipo\Documents\DrivaOC\.planning\phases\03-oc-wizard-ui\03-UI-SPEC.md
@C:\Users\Equipo\Documents\DrivaOC\.planning\phases\03-oc-wizard-ui\03-PATTERNS.md

@C:\Users\Equipo\Documents\DrivaOC\.planning\phases\03-oc-wizard-ui\03-01-SUMMARY.md

<interfaces>
<!-- Tipos y contratos de wizard-types.ts y wizard-calculations.ts (creados en plan 03-01). -->

De src/lib/wizard-types.ts:
```typescript
export interface ProductRow {
  id: string
  producto: string
  descripcion: string
  cantidad: string    // string — input controlado
  valorUSD: string    // string — input controlado
}

export interface InfoGeneralState {
  referenciaOC: string
  estado: EstadoOC
  proveedor: string
  emailProveedor: string
  emailDespachante: string
  paisOrigen: string
  fechaOC: string
  llegadaEstimada: string
  tipoCambio: string  // string — input controlado
  divisa: 'ARS/USD' | 'ARS/EUR'
  notas: string
}

export interface GastosDespacho {
  sim: string; derechos: string; otros: string
}

export interface GastosDespachante {
  terminal: string; fleteInternacional: string  // USD
  fleteInterno: string; senasa: string; despachante: string
}

export interface GastosAdicionales {
  depositoFiscal: string; digitalizacion: string
  estanciaCamion: string; iibb: string
}

export interface OtroGastoRow {
  id: string; descripcion: string; monto: string; divisa: 'ARS' | 'USD'
}

export interface Step1Data {
  info: InfoGeneralState
  productos: ProductRow[]
}
```

De src/lib/wizard-calculations.ts (funciones relevantes para Step 2):
```typescript
export function arsToUSD(montoPesos: string, tipoCambio: string): Decimal
export function usdToARS(montoUSD: string, tipoCambio: string): Decimal
export function calcSubtotalDespacho(g: GastosDespacho, tc: string): Decimal
export function calcSubtotalDespachante(g: GastosDespachante, tc: string): Decimal
export function calcSubtotalAdicionales(g: GastosAdicionales, tc: string): Decimal
export function calcSubtotalOtros(rows: OtroGastoRow[], tc: string): Decimal
export function calcTotalGastos(despacho, despachante, adicionales, otros, tc): Decimal
export function calcFOBTotal(rows: ProductRow[]): Decimal
export function calcLandedCost(fobUSD: Decimal, gastosUSD: Decimal): Decimal
export function formatUSD(d: Decimal): string  // "USD X.XX"
export function formatARS(d: Decimal): string  // "$ X.XX"
```

De src/lib/mock-ocs.ts (para badge de estado read-only en ResumenStep1):
```typescript
export type EstadoOC = 'borrador' | 'en_proceso' | 'en_transito' | 'en_aduana' | 'entregada' | 'cancelada'
```

De src/components/dashboard/FilterBar.tsx (clases de input/select del proyecto):
```typescript
const inputClass = 'w-full px-4 py-2 rounded-lg border border-acento bg-white text-base text-texto placeholder:text-texto/50 focus:outline-none focus:ring-2 focus:ring-principal/30 focus:border-principal transition-colors duration-150'
const selectClass = 'w-full px-4 py-2 rounded-lg border border-acento bg-white text-base text-texto focus:outline-none focus:ring-2 focus:ring-principal/30 focus:border-principal transition-colors duration-150 cursor-pointer appearance-none'
```

De src/components/dashboard/StatCard.tsx (patrón de card container para GastosCard):
```typescript
<div className="bg-white border border-acento rounded-xl p-6 flex flex-col gap-3">
```

Constante de sessionStorage (definida en plan 03-01):
```typescript
const STORAGE_KEY = 'oc-step1-draft'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear ResumenStep1.tsx y GastosCard.tsx</name>
  <files>
    src/components/wizard/ResumenStep1.tsx
    src/components/wizard/GastosCard.tsx
  </files>
  <read_first>
    - src/lib/wizard-types.ts — interfaces Step1Data, InfoGeneralState, ProductRow (recién creadas en 03-01)
    - src/lib/wizard-calculations.ts — calcTotalFila, formatUSD (para tabla de productos en el resumen)
    - src/components/dashboard/StatCard.tsx — patrón de card container con border-acento
    - src/components/dashboard/OCTable.tsx — patrón de tabla para lista de productos read-only
    - .planning/phases/03-oc-wizard-ui/03-UI-SPEC.md §Cards de Sección de Gastos — clases exactas del contenedor, título, grid, subtotal
    - .planning/phases/03-oc-wizard-ui/03-CONTEXT.md §D-11, §D-12 — secciones de gastos y subtotales por card
  </read_first>
  <action>
**Archivo 1: src/components/wizard/ResumenStep1.tsx**

Client Component con 'use client'. Recibe `step1Data: Step1Data` como prop. Muestra info general en modo read-only y tabla de productos.

Props:
```typescript
interface ResumenStep1Props {
  step1Data: Step1Data
}
```

Sección info general — grid `grid grid-cols-1 sm:grid-cols-2 gap-4 mb-6`:
- Mostrar los campos relevantes per OC2-01: Proveedor, País de origen, Fecha OC, Llegada estimada, Tipo de cambio + divisa, Estado (badge), Notas
- Cada campo: label `text-sm font-normal text-titulares` + valor en `<p className="text-base font-normal text-texto bg-fondo px-3 py-2 rounded-lg border border-acento/50">` (estilo read-only de UI-SPEC.md)
- Badge de estado: reusar los colores de badge establecidos en Phase 2 (importarlos o replicarlos). Los colores de badge de Phase 2 son: borrador=gray, en_proceso=blue, en_transito=orange, en_aduana=yellow, entregada=green, cancelada=red.

Sección tabla de productos — título `text-base font-bold text-titulares mb-3` con texto "Productos":
- Tabla read-only con las mismas columnas que ProductosTable pero sin inputs ni botones de acción
- `overflow-x-auto rounded-xl border border-acento`
- Mostrar Total por fila: `calcTotalFila(row).toFixed(2)`
- Fila de totales al pie: FOB Total = `calcFOBTotal(step1Data.productos)` en USD + equivalente ARS

**Archivo 2: src/components/wizard/GastosCard.tsx**

Client Component con 'use client'. Card genérica para las 3 secciones de gastos fijos (Despacho, Despachante, Gastos adicionales). Recibe la configuración de sus campos por props.

Props:
```typescript
interface GastoField {
  key: string
  label: string
  divisa: 'ARS' | 'USD'   // solo informativo para el label del campo
}

interface GastosCardProps {
  titulo: string
  campos: GastoField[]
  values: Record<string, string>
  subtotalUSD: Decimal      // calculado por el padre (Step2Form)
  tipoCambio: string        // para mostrar subtotal en ARS
  onChange: (key: string, value: string) => void
}
```

Estructura del componente (per UI-SPEC.md §Cards de Sección de Gastos):
```
contenedor: rounded-xl border border-acento bg-white p-6 flex flex-col gap-4
título: text-base font-bold text-titulares
grid campos: grid grid-cols-1 sm:grid-cols-2 gap-4
  cada campo:
    label: text-sm font-normal text-texto mb-1
    input number: clases del inputClass del proyecto + w-full
subtotal (al pie de la card):
  mt-4 pt-4 border-t border-acento/50 flex justify-between items-center
  label "Subtotal": text-sm font-normal text-titulares
  valor: text-base font-bold text-titulares (subtotalUSD.toFixed(2) en USD)
  subtítulo ARS: text-sm font-normal text-titulares/60 (subtotalUSD × tipoCambio)
```

El campo de divisa en la label del input: incluir el sufijo de divisa entre paréntesis para claridad visual — ej: `label="Flete internacional (USD)"`, `label="SIM (ARS)"`. Esto es informativo — el valor siempre se ingresa como número sin signo de divisa.

Para los inputs dentro de GastosCard, aplicar el inputClass del proyecto sin la clase de ancho fijo `sm:w-56` (usar solo `w-full`):
```
w-full px-4 py-2 rounded-lg border border-acento bg-white text-base text-texto placeholder:text-texto/50 focus:outline-none focus:ring-2 focus:ring-principal/30 focus:border-principal transition-colors duration-150
```

Placeholder de cada campo numérico: `"0.00"`.
  </action>
  <verify>
    <automated>cd C:\Users\Equipo\Documents\DrivaOC && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - ResumenStep1.tsx tiene 'use client': `grep -c "'use client'" src/components/wizard/ResumenStep1.tsx` devuelve 1
    - ResumenStep1.tsx importa calcTotalFila y calcFOBTotal: `grep -c "calcTotalFila\|calcFOBTotal" src/components/wizard/ResumenStep1.tsx` devuelve >= 1
    - ResumenStep1.tsx usa clases read-only bg-fondo en los campos de info general: `grep -c "bg-fondo" src/components/wizard/ResumenStep1.tsx` devuelve >= 1
    - GastosCard.tsx acepta un prop subtotalUSD de tipo Decimal: `grep -c "subtotalUSD" src/components/wizard/GastosCard.tsx` devuelve >= 1
    - GastosCard.tsx muestra "Subtotal" en el footer de la card: `grep -c "Subtotal" src/components/wizard/GastosCard.tsx` devuelve >= 1
    - GastosCard.tsx tiene 'use client': `grep -c "'use client'" src/components/wizard/GastosCard.tsx` devuelve 1
    - GastosCard.tsx usa border-acento en el contenedor: `grep -c "border-acento" src/components/wizard/GastosCard.tsx` devuelve >= 1
    - npx tsc --noEmit no reporta errores en los archivos nuevos
  </acceptance_criteria>
  <done>ResumenStep1 muestra info general + tabla de productos en modo read-only desde Step1Data; GastosCard es reutilizable para las 3 secciones de gastos fijos con inputs, subtotal y layout consistente.</done>
</task>

<task type="auto">
  <name>Task 2: Crear OtrosGastosSection.tsx + Step2Form.tsx</name>
  <files>
    src/components/wizard/OtrosGastosSection.tsx
    src/components/wizard/Step2Form.tsx
  </files>
  <read_first>
    - src/lib/wizard-types.ts — OtroGastoRow, GastosDespacho, GastosDespachante, GastosAdicionales, Step1Data
    - src/lib/wizard-calculations.ts — calcSubtotalDespacho, calcSubtotalDespachante, calcSubtotalAdicionales, calcSubtotalOtros, calcTotalGastos, calcFOBTotal, usdToARS, formatUSD, formatARS
    - src/components/wizard/GastosCard.tsx — props interface recién creada (GastosCardProps)
    - src/components/wizard/ResumenStep1.tsx — props interface recién creada (ResumenStep1Props)
    - src/components/layout/Sidebar.tsx — patrón de useEffect con storage (líneas 28-35) para adaptar a sessionStorage
    - .planning/phases/03-oc-wizard-ui/03-UI-SPEC.md §Cards de Sección de Gastos §Otros gastos dinámica, §Jerarquía de Botones, §Layout del Wizard
    - .planning/phases/03-oc-wizard-ui/03-CONTEXT.md §D-03, §D-04, §D-11, §D-12 — Volver, Guardar OC, 4 secciones, subtotales
    - .planning/phases/03-oc-wizard-ui/03-RESEARCH.md §Pattern 2 — sessionStorage en useEffect + redirect si vacío (D-06)
  </read_first>
  <action>
**Archivo 1: src/components/wizard/OtrosGastosSection.tsx**

Client Component con 'use client'. Recibe el array de gastos y los handlers desde Step2Form como props.

Props:
```typescript
interface OtrosGastosSectionProps {
  rows: OtroGastoRow[]
  subtotalUSD: Decimal
  tipoCambio: string
  onAdd: () => void
  onRemove: (id: string) => void
  onChange: (id: string, field: keyof Omit<OtroGastoRow, 'id'>, value: string) => void
}
```

Estructura (per UI-SPEC.md §Cards de Sección de Gastos §Otros gastos dinámica):
- Contenedor card: `rounded-xl border border-acento bg-white p-6 flex flex-col gap-4`
- Título: `text-base font-bold text-titulares` → "Otros gastos"
- Lista de ítems: `flex flex-col gap-3`
  - Cada ítem: `flex items-center gap-3`
    - Input Descripción: `flex-1 h-10 px-3 py-2 rounded-lg border border-acento bg-white text-base text-texto focus:outline-none focus:ring-2 focus:ring-principal/30 focus:border-principal` — placeholder: "Descripción del gasto"
    - Input Monto: `w-[120px] h-10 px-3 py-2 rounded-lg border border-acento bg-white text-base text-texto focus:outline-none focus:ring-2 focus:ring-principal/30 focus:border-principal` — type="number", min="0", placeholder="0.00"
    - Select Divisa: `w-[90px] h-10 px-3 rounded-lg border border-acento bg-white text-base text-texto focus:outline-none focus:ring-2 focus:ring-principal/30 cursor-pointer appearance-none` — opciones: ARS, USD
    - Botón Trash2: `min-h-[44px] min-w-[44px] flex items-center justify-center rounded-lg text-red-500 hover:text-red-700 hover:bg-red-50 transition-colors` aria-label="Eliminar gasto"
- Botón "+ Agregar gasto": `flex items-center gap-2 text-sm font-normal text-principal hover:text-titulares transition-colors min-h-[44px]` con ícono `Plus` size={16}
- Subtotal al pie: igual que en GastosCard — `mt-4 pt-4 border-t border-acento/50 flex justify-between items-center`

Nota: `key={row.id}` en cada ítem (UUID estable, nunca key={index}).

**Archivo 2: src/components/wizard/Step2Form.tsx**

Client Component con 'use client'. Este es el componente más complejo — orquesta todo el estado de Step 2.

Estado inicial:
```typescript
const [step1Data, setStep1Data] = useState<Step1Data | null>(null)
const [gastosDespacho, setGastosDespacho] = useState<GastosDespacho>({
  sim: '', derechos: '', otros: ''
})
const [gastosDespachante, setGastosDespachante] = useState<GastosDespachante>({
  terminal: '', fleteInternacional: '', fleteInterno: '', senasa: '', despachante: ''
})
const [gastosAdicionales, setGastosAdicionales] = useState<GastosAdicionales>({
  depositoFiscal: '', digitalizacion: '', estanciaCamion: '', iibb: ''
})
const [otrosGastos, setOtrosGastos] = useState<OtroGastoRow[]>([])
const [toastVisible, setToastVisible] = useState(false)
```

useEffect para recuperar sessionStorage al montar (per D-06 y Research §Pattern 2):
```typescript
useEffect(() => {
  const raw = sessionStorage.getItem('oc-step1-draft')
  if (!raw) {
    router.replace('/importador/oc/nueva?step=1')
    return
  }
  const data: Step1Data = JSON.parse(raw)
  setStep1Data(data)
}, [])
```

Mientras step1Data es null: mostrar un spinner o pantalla en blanco (el redirect de useEffect se ejecuta antes del primer paint en la mayoría de los casos, pero para evitar flash: `if (!step1Data) return null`).

tipoCambio extraído de step1Data: `const tipoCambio = step1Data?.info.tipoCambio ?? '1'`

Cálculos derivados (computados en render — no en estado):
```typescript
const subtotalDespachoUSD   = calcSubtotalDespacho(gastosDespacho, tipoCambio)
const subtotalDespachante   = calcSubtotalDespachante(gastosDespachante, tipoCambio)
const subtotalAdicionales   = calcSubtotalAdicionales(gastosAdicionales, tipoCambio)
const subtotalOtros         = calcSubtotalOtros(otrosGastos, tipoCambio)
const totalGastosUSD        = calcTotalGastos(gastosDespacho, gastosDespachante, gastosAdicionales, otrosGastos, tipoCambio)
const fobUSD                = step1Data ? calcFOBTotal(step1Data.productos) : new Decimal(0)
```

Handlers para las secciones fijas (una función genérica por sección):
```typescript
const updateDespacho     = (key: string, val: string) => setGastosDespacho(prev => ({ ...prev, [key]: val }))
const updateDespachante  = (key: string, val: string) => setGastosDespachante(prev => ({ ...prev, [key]: val }))
const updateAdicionales  = (key: string, val: string) => setGastosAdicionales(prev => ({ ...prev, [key]: val }))
```

Handlers para Otros gastos:
```typescript
const addOtroGasto = () =>
  setOtrosGastos(prev => [...prev, { id: crypto.randomUUID(), descripcion: '', monto: '', divisa: 'ARS' }])

const removeOtroGasto = (id: string) =>
  setOtrosGastos(prev => prev.filter(r => r.id !== id))

const updateOtroGasto = (id: string, field: keyof Omit<OtroGastoRow, 'id'>, value: string) =>
  setOtrosGastos(prev => prev.map(r => r.id === id ? { ...r, [field]: value } : r))
```

Handler Volver (per D-03 — NO limpiar sessionStorage):
```typescript
const handleVolver = () => router.push('/importador/oc/nueva?step=1')
```

Handler Guardar OC (per D-04 — toast + redirect, sin llamada a backend en Phase 3):
```typescript
const handleGuardar = () => {
  setToastVisible(true)
  setTimeout(() => {
    setToastVisible(false)
    router.push('/importador/dashboard')
  }, 2000)
}
```

Toast de éxito inline en el componente (per Research §Toast Simple y UI-SPEC.md §Toast de Éxito):
```typescript
{toastVisible && (
  <div className="fixed top-4 right-4 z-50 bg-white border border-acento rounded-xl px-4 py-3 shadow-lg flex items-center gap-3 text-sm font-normal text-texto">
    <CheckCircle2 size={20} className="text-principal" />
    <div>
      <p className="font-bold">OC guardada exitosamente</p>
      <p className="text-texto/60">Redirigiendo al dashboard...</p>
    </div>
  </div>
)}
```

Configuración de los campos para GastosCard:

Despacho:
```typescript
const camposDespacho = [
  { key: 'sim', label: 'SIM (ARS)', divisa: 'ARS' as const },
  { key: 'derechos', label: 'Derechos (ARS)', divisa: 'ARS' as const },
  { key: 'otros', label: 'Otros (ARS)', divisa: 'ARS' as const },
]
```

Despachante:
```typescript
const camposDespachante = [
  { key: 'terminal', label: 'Terminal (ARS)', divisa: 'ARS' as const },
  { key: 'fleteInternacional', label: 'Flete internacional (USD)', divisa: 'USD' as const },
  { key: 'fleteInterno', label: 'Flete interno (ARS)', divisa: 'ARS' as const },
  { key: 'senasa', label: 'SENASA (ARS)', divisa: 'ARS' as const },
  { key: 'despachante', label: 'Despachante (ARS)', divisa: 'ARS' as const },
]
```

Adicionales:
```typescript
const camposAdicionales = [
  { key: 'depositoFiscal', label: 'Depósito fiscal (ARS)', divisa: 'ARS' as const },
  { key: 'digitalizacion', label: 'Digitalización (ARS)', divisa: 'ARS' as const },
  { key: 'estanciaCamion', label: 'Estancia de camión (ARS)', divisa: 'ARS' as const },
  { key: 'iibb', label: 'IIBB (ARS)', divisa: 'ARS' as const },
]
```

Título del step:
```
<h1 className="text-2xl font-bold text-titulares">Paso 2: Gastos y Documentos</h1>
```

Total global de gastos (entre las secciones de gastos y las ValueCards/Slots del plan 03-03):
```
<div className="flex items-center justify-between p-4 rounded-xl bg-fondo border border-acento">
  <span className="text-sm font-bold text-titulares">Total gastos</span>
  <div className="text-right">
    <p className="text-base font-bold text-titulares">{formatUSD(totalGastosUSD)}</p>
    <p className="text-sm font-normal text-titulares/60">{formatARS(usdToARS(totalGastosUSD.toString(), tipoCambio))}</p>
  </div>
</div>
```

Step2Form también debe exponer los valores computados para que WizardPage (plan 03-03) pueda pasarlos a ValueCards. Como WizardPage es el padre que monta Step2Form, Step2Form debe aceptar opcionalmente un callback `onValuesChange` para notificar al padre de los valores actualizados:

```typescript
interface Step2FormProps {
  // No recibe step1Data por prop — lo lee desde sessionStorage en useEffect
}
```

Alternativa: Step2Form incluye directamente las ValueCards dentro de sí mismo (más simple, sin callback). Preferir esta opción — el plan 03-03 le pasa `fobUSD` y `totalGastosUSD` a ValueCards directamente desde dentro de Step2Form.

Layout del Step2Form (en orden vertical):
1. Título "Paso 2: Gastos y Documentos"
2. ResumenStep1 (si step1Data no es null)
3. GastosCard — Despacho
4. GastosCard — Despachante
5. GastosCard — Gastos adicionales
6. OtrosGastosSection
7. Total global de gastos
8. (Placeholder para ValueCards y DocumentSlots — serán insertados en plan 03-03)
9. Footer navegación: botón "Volver" (izquierda) + botón "Guardar OC" (derecha)

Footer de navegación:
```
<div className="flex items-center justify-between mt-8 pt-6 border-t border-acento">
  <button
    type="button"
    onClick={handleVolver}
    className="border border-principal text-principal font-normal px-6 py-3 rounded-lg hover:bg-acento/30 transition-colors min-h-[44px]"
  >
    Volver
  </button>
  <button
    type="button"
    onClick={handleGuardar}
    className="bg-principal text-white font-bold px-6 py-3 rounded-lg hover:bg-titulares transition-colors min-h-[44px]"
  >
    Guardar OC
  </button>
</div>
```

Contenedor general del wizard (mismo que Step1Form):
```
<div className="max-w-4xl mx-auto px-4 sm:px-6 py-8 sm:py-12 flex flex-col gap-8">
```
  </action>
  <verify>
    <automated>cd C:\Users\Equipo\Documents\DrivaOC && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - OtrosGastosSection.tsx tiene 'use client': `grep -c "'use client'" src/components/wizard/OtrosGastosSection.tsx` devuelve 1
    - OtrosGastosSection.tsx usa key={row.id}: `grep -c "key={row.id}" src/components/wizard/OtrosGastosSection.tsx` devuelve 1
    - OtrosGastosSection.tsx tiene select con opciones ARS y USD: `grep -c "ARS\|USD" src/components/wizard/OtrosGastosSection.tsx` devuelve >= 2
    - Step2Form.tsx tiene 'use client': `grep -c "'use client'" src/components/wizard/Step2Form.tsx` devuelve 1
    - Step2Form.tsx lee sessionStorage con la clave 'oc-step1-draft': `grep -c "oc-step1-draft" src/components/wizard/Step2Form.tsx` devuelve >= 1
    - Step2Form.tsx llama router.replace para redirigir si sessionStorage vacío: `grep -c "router.replace" src/components/wizard/Step2Form.tsx` devuelve 1
    - Step2Form.tsx importa calcTotalGastos: `grep -c "calcTotalGastos" src/components/wizard/Step2Form.tsx` devuelve >= 1
    - Step2Form.tsx no llama a ninguna API real en handleGuardar: `grep -v "^//" src/components/wizard/Step2Form.tsx | grep -c "fetch\|axios\|api" | cat` — resultado debe ser 0
    - Step2Form.tsx tiene los 4 campos de camposDespachante incluyendo fleteInternacional: `grep -c "fleteInternacional" src/components/wizard/Step2Form.tsx` devuelve >= 1
    - toast de éxito usa texto "OC guardada exitosamente": `grep -c "OC guardada exitosamente" src/components/wizard/Step2Form.tsx` devuelve 1
    - npx tsc --noEmit no reporta errores en los archivos nuevos
  </acceptance_criteria>
  <done>OtrosGastosSection con lista dinámica de gastos libres con select de divisa ARS/USD; Step2Form completo con estado de gastos, subtotales en tiempo real, botones Volver/Guardar OC, toast de éxito, redirect mock al dashboard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sessionStorage→Step2Form | Datos de Step1 deserializados desde JSON al montar Step 2 |
| inputs de gastos→React state | Valores de ARS/USD ingresados por el importador |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering | sessionStorage→Step2Form | accept | En Phase 3 sin backend, datos manipulados en sessionStorage solo afectan la UI del cliente. Phase 5 validará inputs del servidor. Aggravant: JSON.parse sin validación de schema — aceptable para Phase 3. |
| T-03-06 | Tampering | división por tipoCambio | mitigate | arsToUSD verifica `tc.isZero()` antes de dividir — previene división por cero. tipoCambio validado > 0 por isStep1Valid en Step 1. |
| T-03-07 | Tampering | inputs de gastos como NaN | mitigate | Todas las funciones de calc usan `new Decimal(value || '0')` — NaN y undefined se tratan como 0. Nunca se pasan a backend en Phase 3. |
</threat_model>

<verification>
Después de completar ambas tareas:
1. `npx tsc --noEmit` no reporta errores
2. `npm run dev` arranca sin errores
3. Navegar a `/importador/oc/nueva?step=2` sin haber pasado por step=1 → debe redirigir a `?step=1`
4. Completar Step 1 → navegar a Step 2 → verificar que el resumen muestra los datos correctos
5. Ingresar valores en los campos de gastos → verificar que los subtotales de cada card se actualizan en tiempo real
6. Agregar un ítem en "Otros gastos" con divisa USD → verificar que el subtotal de esa sección cambia
7. Click en "Volver" → debe navegar a step=1 con datos intactos (NO limpiar sessionStorage)
8. Click en "Guardar OC" → toast debe aparecer y luego redirigir a /importador/dashboard
</verification>

<success_criteria>
- ResumenStep1 muestra todos los campos de OC2-01 (proveedor, país, fechas, tipo de cambio, estado, notas) + tabla de productos read-only
- GastosCard es reutilizable con props de configuración, muestra inputs + subtotal en tiempo real
- OtrosGastosSection permite agregar/eliminar filas con select de divisa ARS/USD y key={row.id} estable
- Step2Form recupera Step1Data de sessionStorage en useEffect, redirige a step=1 si está vacío
- Los 4 grupos de gastos (Despacho, Despachante, Adicionales, Otros) con todos sus campos según OC2-02/03/04/05
- Total global de gastos visible en USD + ARS, recalculado en tiempo real (OC2-06)
- Botón Volver navega a step=1 sin limpiar sessionStorage (D-03)
- Botón Guardar OC muestra toast de éxito y redirige al dashboard (D-04)
</success_criteria>

<output>
Después de completar, crear `.planning/phases/03-oc-wizard-ui/03-02-SUMMARY.md` con:
- Archivos creados
- Decisiones de implementación (D-03, D-04, D-06, D-11, D-12)
- STORAGE_KEY confirmado: 'oc-step1-draft'
- Estructura de campos de cada GastosCard (camposDespacho, camposDespachante, camposAdicionales)
- Nota sobre el handler handleGuardar sin llamada a API real (Phase 3)
- Cualquier desviación del plan y su justificación
</output>
