---
phase: 06-files-integrations
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/components/emails/OCNotificationEmail.tsx
  - src/actions/oc.ts
autonomous: true
requirements:
  - NOTIF-01
  - NOTIF-02
  - NOTIF-03

must_haves:
  truths:
    - "Al crear o actualizar una OC con estado distinto de 'borrador', se envía email a todos los miembros excepto al editor"
    - "Al subir un documento (updateOCDocumento), también se dispara el email"
    - "El template tiene logo horizontal, colores de marca (#0061a6, #62b446), referencia OC, proveedor, estado, fecha, notas y botón Ver OC"
    - "El envío es fire-and-forget — no bloquea el retorno del Server Action"
    - "Si el email falla, la operación principal (guardar OC / subir doc) igual tiene éxito; el error queda en console.error"
    - "Borradores no generan notificaciones"
  artifacts:
    - path: "src/components/emails/OCNotificationEmail.tsx"
      provides: "Template React Email con identidad visual Driva Dev"
      exports: ["OCNotificationEmail"]
    - path: "src/actions/oc.ts"
      provides: "Helper sendOCNotification con try/catch interno, llamado con void desde createOC/updateOC/updateOCInfo/updateOCDocumento"
      contains: "sendOCNotification"
  key_links:
    - from: "src/actions/oc.ts createOC/updateOC/updateOCInfo/updateOCDocumento"
      to: "sendOCNotification"
      via: "void sendOCNotification(id, userId)"
      pattern: "void sendOCNotification"
    - from: "sendOCNotification"
      to: "Resend API"
      via: "resend.emails.send({ react: OCNotificationEmail(...) })"
      pattern: "resend.emails.send"
    - from: "sendOCNotification"
      to: "Clerk clerkClient"
      via: "clerkClient().users.getUser para obtener email del importador"
      pattern: "clerkClient.*getUser"
---

<objective>
Implementar las notificaciones por email: template React Email con identidad visual Driva Dev, helper `sendOCNotification` con lógica de destinatarios por rol, e inyección fire-and-forget en `createOC`, `updateOC`, `updateOCInfo` y `updateOCDocumento`.

Purpose: Sin este plan, los proveedores y despachantes no reciben notificaciones al actualizarse una OC.
Output: `OCNotificationEmail.tsx` como template React Email, `sendOCNotification` como helper interno en `src/actions/oc.ts`.
</objective>

<execution_context>
@C:/Users/Equipo/Documents/DrivaOC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Equipo/Documents/DrivaOC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/Equipo/Documents/DrivaOC/.planning/PROJECT.md
@C:/Users/Equipo/Documents/DrivaOC/.planning/ROADMAP.md
@C:/Users/Equipo/Documents/DrivaOC/.planning/STATE.md
@C:/Users/Equipo/Documents/DrivaOC/.planning/phases/06-files-integrations/06-01-SUMMARY.md

<interfaces>
<!-- Tipos y contratos extraídos del codebase para esta tarea. -->

De src/actions/oc.ts (patrón obtener email Clerk — líneas 245-250):
```typescript
const clerkUser = await (await clerkClient()).users.getUser(userId)
const emails = clerkUser.emailAddresses
  .map(e => e.emailAddress?.toLowerCase() ?? '')
  .filter(Boolean)
```

De src/actions/oc.ts (patrón retorno createOC — línea 211):
```typescript
return { data: { id: oc._id.toString() } }
// Agregar ANTES del return: void sendOCNotification(oc._id.toString(), userId)
```

De src/actions/oc.ts (patrón retorno updateOC/updateOCInfo/updateOCDocumento — línea 323):
```typescript
return { data: { id } }
// Agregar ANTES del return: void sendOCNotification(id, userId)
```

De src/lib/mock-ocs.ts (estados de OC):
```typescript
type EstadoOC = 'borrador' | 'en_proceso' | 'en_transito' | 'en_aduana' | 'entregada' | 'cancelada'
// Solo enviar email si estado !== 'borrador'
```

De @react-email/components (primitivas disponibles):
```typescript
import { Body, Container, Heading, Text, Button, Img, Html, Section, Hr } from '@react-email/components'
// Sin 'use client' — se renderiza server-side por Resend
```

Lógica de destinatarios (D-07 de CONTEXT.md):
```
- Si importador edita (rol === 'importador'):
    recipients = [...oc.emailsProveedor, ...oc.emailsDespachante]
- Si proveedor edita (rol === 'proveedor'):
    recipients = [emailDelImportador, ...oc.emailsDespachante, ...otros emails en emailsProveedor que no sean el editor]
- Si despachante edita (rol === 'despachante'):
    recipients = [emailDelImportador, ...oc.emailsProveedor, ...otros emails en emailsDespachante que no sean el editor]
- Filtrar editor de la lista: editorEmails = emails del userId en Clerk; recipients.filter(r => !editorEmails.includes(r))
- Si recipients vacío después de filtrar: no enviar (oc sin miembros externos)
```

Email del importador: obtener de Clerk con `(await clerkClient()).users.getUser(oc.importadorId)` → primer emailAddress.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: Template OCNotificationEmail con identidad visual</name>
  <read_first>
    - src/components/dashboard/DeleteModal.tsx (patrón de componente funcional tipado con interface explícita)
    - CLAUDE.md sección Brand Identity (colores: #0061a6, #004a80, #62b446, #f0f7ff, #1C1917)
  </read_first>
  <files>
    src/components/emails/OCNotificationEmail.tsx
  </files>
  <action>
Crear `src/components/emails/OCNotificationEmail.tsx`. Es un componente nuevo — no existe en el codebase. No tiene `'use client'` (se renderiza server-side por Resend).

```typescript
import { Body, Container, Heading, Text, Button, Img, Html, Hr } from '@react-email/components'

interface OCResumen {
  referenciaOC: string
  proveedor: string
  estado: string
  fechaOC: string
  notas: string
}

interface OCNotificationEmailProps {
  oc: OCResumen
  link: string
}

export function OCNotificationEmail({ oc, link }: OCNotificationEmailProps) {
  return (
    <Html lang="es">
      <Body style={{ backgroundColor: '#f0f7ff', fontFamily: 'sans-serif', margin: 0, padding: 0 }}>
        <Container style={{ maxWidth: 600, margin: '32px auto', backgroundColor: '#ffffff', borderRadius: 8, padding: 32, border: '1px solid #e5e7eb' }}>
          <Img
            src="https://sistema-comex.vercel.app/logo-horizontal.svg"
            alt="Sistema integral COMEX"
            height={36}
            style={{ marginBottom: 24 }}
          />
          <Hr style={{ borderColor: '#0061a6', marginBottom: 24 }} />
          <Heading style={{ color: '#004a80', fontSize: 20, fontWeight: 700, margin: '0 0 16px' }}>
            Actualización: OC {oc.referenciaOC}
          </Heading>
          <Text style={{ color: '#1C1917', fontSize: 14, margin: '0 0 8px' }}>
            <strong>Proveedor:</strong> {oc.proveedor}
          </Text>
          <Text style={{ color: '#1C1917', fontSize: 14, margin: '0 0 8px' }}>
            <strong>Estado:</strong> {oc.estado}
          </Text>
          <Text style={{ color: '#1C1917', fontSize: 14, margin: '0 0 8px' }}>
            <strong>Fecha OC:</strong> {oc.fechaOC}
          </Text>
          {oc.notas && (
            <Text style={{ color: '#1C1917', fontSize: 14, margin: '0 0 16px' }}>
              <strong>Notas:</strong> {oc.notas}
            </Text>
          )}
          <Button
            href={link}
            style={{
              backgroundColor: '#0061a6',
              color: '#ffffff',
              borderRadius: 6,
              padding: '12px 24px',
              fontSize: 14,
              fontWeight: 600,
              textDecoration: 'none',
              display: 'inline-block',
            }}
          >
            Ver OC en el sistema
          </Button>
          <Hr style={{ borderColor: '#e5e7eb', marginTop: 32, marginBottom: 16 }} />
          <Text style={{ color: '#1C1917', fontSize: 12, margin: 0 }}>
            Sistema integral COMEX — Desarrollado por{' '}
            <a href="https://drivadev.com.ar" style={{ color: '#62b446' }}>Driva Dev</a>
          </Text>
        </Container>
      </Body>
    </Html>
  )
}
```

El `src` del logo debe quedar como placeholder — el executor del plan lo ajustará al dominio real de producción. Agregar comentario en ningún lugar; la URL es auto-descriptiva.
</action>
  <verify>
    <automated>cd C:/Users/Equipo/Documents/DrivaOC && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/emails/OCNotificationEmail.tsx existe
    - src/components/emails/OCNotificationEmail.tsx contiene `export function OCNotificationEmail`
    - src/components/emails/OCNotificationEmail.tsx contiene `import { Body, Container, Heading` (primitivas React Email)
    - src/components/emails/OCNotificationEmail.tsx NO contiene `'use client'`
    - src/components/emails/OCNotificationEmail.tsx contiene `backgroundColor: '#f0f7ff'` (fondo de marca)
    - src/components/emails/OCNotificationEmail.tsx contiene `color: '#004a80'` (color titulares)
    - src/components/emails/OCNotificationEmail.tsx contiene `oc.notas` con renderizado condicional
    - src/components/emails/OCNotificationEmail.tsx contiene `href={link}` en el botón
    - `npx tsc --noEmit` sale con código 0
  </acceptance_criteria>
  <done>Template React Email con identidad visual completa: logo, colores de marca, campos de la OC, botón "Ver OC en el sistema", footer Driva Dev.</done>
</task>

<task type="auto">
  <name>Tarea 2: Helper sendOCNotification + inyección fire-and-forget en Server Actions</name>
  <read_first>
    - src/actions/oc.ts (archivo completo — imports líneas 1-15, createOC líneas 164-221, updateOC líneas 263-324, updateOCInfo líneas 397-443, updateOCDocumento recién creado en 06-01)
  </read_first>
  <files>
    src/actions/oc.ts
  </files>
  <action>
**1. Agregar imports al bloque de imports existente** en `src/actions/oc.ts` (después del bloque actual):
```typescript
import { Resend } from 'resend'
import { OCNotificationEmail } from '@/components/emails/OCNotificationEmail'
```

**2. Agregar helper `sendOCNotification`** justo antes del final del archivo (después de `updateOCInfo`). Es una función interna sin `export`:

```typescript
async function sendOCNotification(ocId: string, editorUserId: string): Promise<void> {
  try {
    await connectDB()
    const doc = await OC.findById(ocId).lean() as Record<string, unknown> & {
      importadorId: string
      estado: string
      emailsProveedor: string[]
      emailsDespachante: string[]
      referenciaOC: string
      proveedor: string
      fechaOC: string
      notas: string
    } | null
    if (!doc) return
    if (doc.estado === 'borrador') return

    const editorClerkUser = await (await clerkClient()).users.getUser(editorUserId)
    const editorEmails = editorClerkUser.emailAddresses
      .map(e => e.emailAddress?.toLowerCase() ?? '')
      .filter(Boolean)
    const editorRole = (editorClerkUser.publicMetadata as { role?: string })?.role

    const importadorClerkUser = await (await clerkClient()).users.getUser(doc.importadorId)
    const importadorEmail = importadorClerkUser.emailAddresses[0]?.emailAddress?.toLowerCase() ?? ''

    let recipients: string[] = []
    if (editorRole === 'importador') {
      recipients = [...doc.emailsProveedor, ...doc.emailsDespachante]
    } else if (editorRole === 'proveedor') {
      const otrosProveedor = doc.emailsProveedor.filter(e => !editorEmails.includes(e))
      recipients = [importadorEmail, ...doc.emailsDespachante, ...otrosProveedor]
    } else if (editorRole === 'despachante') {
      const otrosDespachante = doc.emailsDespachante.filter(e => !editorEmails.includes(e))
      recipients = [importadorEmail, ...doc.emailsProveedor, ...otrosDespachante]
    }

    recipients = recipients.filter(r => r !== '' && !editorEmails.includes(r))
    if (recipients.length === 0) return

    const resend = new Resend(process.env.RESEND_API_KEY)
    const baseUrl = process.env.NEXT_PUBLIC_APP_URL ?? 'https://sistema-comex.vercel.app'
    const link = `${baseUrl}/importador/oc/${ocId}`

    await resend.emails.send({
      from: process.env.RESEND_FROM_EMAIL!,
      to: recipients,
      subject: `Actualización OC ${doc.referenciaOC}`,
      react: OCNotificationEmail({
        oc: {
          referenciaOC: doc.referenciaOC,
          proveedor: doc.proveedor,
          estado: doc.estado,
          fechaOC: doc.fechaOC,
          notas: doc.notas,
        },
        link,
      }),
    })
  } catch (err) {
    console.error('[sendOCNotification] failed:', err)
  }
}
```

**3. Inyectar `void sendOCNotification(...)` en los Server Actions.** Regla: agregar la llamada ANTES del `return { data: ... }` final en cada función. Solo cuando el estado es distinto de 'borrador' (la función ya hace esa verificación internamente).

En `createOC` (línea 211), antes de `return { data: { id: oc._id.toString() } }`:
```typescript
void sendOCNotification(oc._id.toString(), userId)
return { data: { id: oc._id.toString() } }
```

En `updateOC` (línea 323), antes de `return { data: { id } }`:
```typescript
void sendOCNotification(id, userId)
return { data: { id } }
```

En `updateOCInfo` (línea 443), antes de `return { data: { id } }`:
```typescript
void sendOCNotification(id, userId)
return { data: { id } }
```

En `updateOCDocumento` (recién creado en 06-01), antes de `return { data: { id } }` dentro del try:
```typescript
void sendOCNotification(id, userId)
return { data: { id } }
```

**Nota sobre NEXT_PUBLIC_APP_URL:** Agregar esta variable a `.env.example` si no existe:
```bash
NEXT_PUBLIC_APP_URL=https://sistema-comex.vercel.app
```
</action>
  <verify>
    <automated>cd C:/Users/Equipo/Documents/DrivaOC && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/actions/oc.ts contiene `import { Resend } from 'resend'`
    - src/actions/oc.ts contiene `import { OCNotificationEmail } from '@/components/emails/OCNotificationEmail'`
    - src/actions/oc.ts contiene `async function sendOCNotification(` (sin export — es helper interno)
    - src/actions/oc.ts contiene `console.error('[sendOCNotification] failed:', err)` dentro del catch
    - src/actions/oc.ts contiene `if (doc.estado === 'borrador') return` dentro de sendOCNotification
    - src/actions/oc.ts contiene 4 ocurrencias de `void sendOCNotification` (una en cada Server Action mutador)
    - src/actions/oc.ts contiene `await resend.emails.send({` dentro de sendOCNotification
    - src/actions/oc.ts NO contiene `await sendOCNotification` (debe ser void, no await)
    - `npx tsc --noEmit` sale con código 0
  </acceptance_criteria>
  <done>sendOCNotification con lógica de destinatarios por rol, fire-and-forget con try/catch interno, inyectado en createOC + updateOC + updateOCInfo + updateOCDocumento.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server Action → Resend API | Emails salen desde el servidor con RESEND_API_KEY privado |
| Server Action → Clerk API | Obtención de emails de usuarios via clerkClient server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Information Disclosure | sendOCNotification | mitigate | El helper filtra al editor de los destinatarios antes de enviar: `recipients.filter(r => !editorEmails.includes(r))` |
| T-06-06 | Denial of Service | sendOCNotification | accept | Fire-and-forget con try/catch interno — un fallo en Resend no afecta el flujo de guardado. Aceptado: Resend tiene SLA del 99.9% |
| T-06-07 | Information Disclosure | sendOCNotification | mitigate | Solo se envía email cuando `estado !== 'borrador'` — borradores no notifican a terceros |
| T-06-08 | Spoofing | RESEND_FROM_EMAIL | accept | El dominio del remitente debe estar verificado en Resend Dashboard (bloqueante pre-ejecución) |
</threat_model>

<verification>
```bash
# TypeScript sin errores
cd C:/Users/Equipo/Documents/DrivaOC && npx tsc --noEmit

# Template existe
ls src/components/emails/OCNotificationEmail.tsx

# sendOCNotification definida
grep -n "async function sendOCNotification" src/actions/oc.ts

# 4 inyecciones void (una por Server Action)
grep -c "void sendOCNotification" src/actions/oc.ts
# debe retornar 4

# No await en fire-and-forget
grep -v "void" src/actions/oc.ts | grep -c "await sendOCNotification"
# debe retornar 0

# Guard de borrador presente
grep -n "estado.*borrador" src/actions/oc.ts
```
</verification>

<success_criteria>
1. Template React Email existe con identidad visual completa y todos los campos de D-08 (referencia, proveedor, estado, fecha, notas, link)
2. `sendOCNotification` con lógica de destinatarios según rol del editor (D-07)
3. Fire-and-forget con `void` — no bloquea ningún Server Action
4. Borradores no generan notificaciones (guard interno en sendOCNotification)
5. Errores de Resend logueados con `console.error` pero no propagan al caller
6. `npx tsc --noEmit` sin errores
</success_criteria>

<output>
Al completar, crear `.planning/phases/06-files-integrations/06-02-SUMMARY.md`
</output>
