---
phase: 06-files-integrations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/models/OC.ts
  - src/lib/mock-ocs.ts
  - src/actions/oc.ts
  - src/app/api/sign-cloudinary-params/route.ts
  - src/components/wizard/DocumentSlots.tsx
  - .env.example
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-03

must_haves:
  truths:
    - "El slot 'packingList' existe en el schema Mongoose y se serializa en serializeOC"
    - "El botón Adjuntar en DocumentSlots abre CldUploadWidget con signatureEndpoint"
    - "Si el slot ya tiene URL, aparece un modal de confirmación antes de abrir el widget"
    - "Al confirmar el upload, updateOCDocumento guarda la URL en MongoDB en documentos.{slot}"
    - "Todos los roles pueden subir documentos (guard multi-rol en updateOCDocumento)"
    - "En modo readOnly, los slots muestran un link de descarga si tienen URL, y 'Sin archivo' si no"
  artifacts:
    - path: "src/app/api/sign-cloudinary-params/route.ts"
      provides: "POST handler firmado con CLOUDINARY_API_SECRET"
      contains: "api_sign_request"
    - path: "src/lib/models/OC.ts"
      provides: "Campo packingList en schema Mongoose"
      contains: "packingList"
    - path: "src/lib/mock-ocs.ts"
      provides: "Campo packingList en tipo OCDetalle"
      contains: "packingList"
    - path: "src/components/wizard/DocumentSlots.tsx"
      provides: "Upload real a Cloudinary con modal de confirmación"
      contains: "CldUploadWidget"
  key_links:
    - from: "src/components/wizard/DocumentSlots.tsx"
      to: "/api/sign-cloudinary-params"
      via: "signatureEndpoint prop de CldUploadWidget"
      pattern: "signatureEndpoint.*sign-cloudinary-params"
    - from: "src/components/wizard/DocumentSlots.tsx"
      to: "src/actions/oc.ts updateOCDocumento"
      via: "onSuccess callback del widget"
      pattern: "updateOCDocumento"
    - from: "src/actions/oc.ts updateOCDocumento"
      to: "MongoDB OC.documentos"
      via: "OC.findByIdAndUpdate con $set"
      pattern: "\\$set.*documentos"
---

<objective>
Habilitar el upload real de PDFs a Cloudinary: firma server-side, CldUploadWidget en DocumentSlots, y persistencia de URL en MongoDB. Incluye la corrección del gap crítico de `packingList` en el schema y la actualización de `.env.example` con las variables `NEXT_PUBLIC_` requeridas por next-cloudinary.

Purpose: Sin este plan, DocumentSlots solo guarda localmente y el campo `packingList` se pierde silenciosamente en MongoDB.
Output: Route Handler de firma, `updateOCDocumento` Server Action, `DocumentSlots` con upload real y modal de confirmación.
</objective>

<execution_context>
@C:/Users/Equipo/Documents/DrivaOC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Equipo/Documents/DrivaOC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/Equipo/Documents/DrivaOC/.planning/PROJECT.md
@C:/Users/Equipo/Documents/DrivaOC/.planning/ROADMAP.md
@C:/Users/Equipo/Documents/DrivaOC/.planning/STATE.md

<interfaces>
<!-- Tipos y contratos que el executor necesita. Extraídos del codebase. -->

De src/lib/models/OC.ts (schema Mongoose documentos — líneas 73-80):
```typescript
documentos: {
  facturaProveedor: { type: String, default: null },
  facturaDespachante: { type: String, default: null },
  conocimientoEmbarque: { type: String, default: null },
  certificadoOrigen: { type: String, default: null },
  certificadoAnalisis: { type: String, default: null },
  // packingList FALTA — debe agregarse
  otro: { type: String, default: null },
},
```

De src/lib/mock-ocs.ts (interface OCDetalle.documentos — líneas 47-54):
```typescript
documentos: {
  facturaProveedor: string | null
  facturaDespachante: string | null
  conocimientoEmbarque: string | null
  certificadoOrigen: string | null
  certificadoAnalisis: string | null
  // packingList FALTA — debe agregarse
  otro: string | null
}
```

De src/actions/oc.ts (serializeOC líneas 153-161):
```typescript
documentos: {
  facturaProveedor: d.documentos?.facturaProveedor ?? null,
  facturaDespachante: d.documentos?.facturaDespachante ?? null,
  conocimientoEmbarque: d.documentos?.conocimientoEmbarque ?? null,
  certificadoOrigen: d.documentos?.certificadoOrigen ?? null,
  certificadoAnalisis: d.documentos?.certificadoAnalisis ?? null,
  // packingList FALTA — debe agregarse
  otro: d.documentos?.otro ?? null,
},
```

De src/actions/oc.ts (patrón auth + IDOR guard existente — líneas 274-281):
```typescript
const { userId } = await auth()
if (!userId) return { error: 'No autorizado' }
await connectDB()
const existing = await OC.findById(id).lean() as { importadorId?: string } | null
if (!existing || existing.importadorId !== userId) return { error: 'Sin acceso' }
```

De src/components/wizard/DocumentSlots.tsx (interface props existente — líneas 29-40):
```typescript
interface DocumentSlotsProps {
  readOnly?: boolean
  documentos?: {
    facturaProveedor: string | null
    facturaDespachante: string | null
    conocimientoEmbarque: string | null
    certificadoOrigen: string | null
    certificadoAnalisis?: string | null
    packingList?: string | null
    otro: string | null
  }
}
```

De src/components/dashboard/DeleteModal.tsx (estructura del modal para referencia visual):
```typescript
// Modal de confirmación — copiar estructura CSS
<div className="fixed inset-0 z-50 bg-texto/30 flex items-center justify-center p-4">
  <div className="bg-white rounded-xl border border-acento p-6 max-w-sm w-full flex flex-col gap-4">
    <div className="flex gap-3 justify-end">
      <button onClick={onCancel} className="border border-acento text-texto hover:bg-fondo px-4 py-2 rounded-lg font-normal min-h-[44px] transition-colors duration-150">
        Cancelar
      </button>
      <button onClick={onConfirm} className="bg-principal text-white hover:bg-titulares px-4 py-2 rounded-lg font-medium min-h-[44px] transition-colors duration-150">
        Reemplazar
      </button>
    </div>
  </div>
</div>
```

De src/app/api/webhooks/clerk/route.ts (patrón Route Handler):
```typescript
export const runtime = 'nodejs'
// Primera línea obligatoria — antes de cualquier import
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: Corregir gap packingList + Route Handler de firma Cloudinary</name>
  <read_first>
    - src/lib/models/OC.ts (schema completo — ver campo documentos líneas 73-80)
    - src/lib/mock-ocs.ts (interface OCDetalle.documentos líneas 47-54)
    - src/actions/oc.ts (serializeOC líneas 153-161; patrón auth+guard líneas 274-281)
    - src/app/api/webhooks/clerk/route.ts (patrón Route Handler con runtime = 'nodejs')
    - .env.example (vars de Cloudinary actuales — líneas 13-16)
  </read_first>
  <files>
    src/lib/models/OC.ts,
    src/lib/mock-ocs.ts,
    src/actions/oc.ts,
    src/app/api/sign-cloudinary-params/route.ts,
    .env.example
  </files>
  <action>
**1. src/lib/models/OC.ts** — En el bloque `documentos` del schema (después de `certificadoAnalisis`), agregar:
```typescript
packingList: { type: String, default: null },
```
El campo va entre `certificadoAnalisis` y `otro`.

**2. src/lib/mock-ocs.ts** — En la interface `OCDetalle`, bloque `documentos` (después de `certificadoAnalisis: string | null`), agregar:
```typescript
packingList: string | null
```
También buscar los objetos `documentos` en `MOCK_OCS_DETALLE` (si existen) y agregar `packingList: null`.

**3. src/actions/oc.ts** — En la función `serializeOC`, bloque `documentos` (después de `certificadoAnalisis`), agregar:
```typescript
packingList: d.documentos?.packingList ?? null,
```
Luego, al final del archivo, agregar el nuevo Server Action `updateOCDocumento`:
```typescript
export async function updateOCDocumento(
  id: string,
  slot: string,
  url: string
): Promise<{ data: { id: string } } | { error: string }> {
  const VALID_SLOTS = [
    'facturaProveedor', 'facturaDespachante', 'conocimientoEmbarque',
    'certificadoOrigen', 'certificadoAnalisis', 'packingList', 'otro',
  ] as const
  if (!VALID_SLOTS.includes(slot as typeof VALID_SLOTS[number])) {
    return { error: 'Slot inválido' }
  }

  const { userId, sessionClaims } = await auth()
  if (!userId) return { error: 'No autorizado' }
  const rol = (sessionClaims?.metadata as { role?: string })?.role

  await connectDB()

  const existing = await OC.findById(id).lean() as {
    importadorId?: string
    emailsProveedor?: string[]
    emailsDespachante?: string[]
    estado?: string
  } | null
  if (!existing) return { error: 'OC no encontrada' }

  if (rol === 'importador') {
    if (existing.importadorId !== userId) return { error: 'Sin acceso' }
  } else if (rol === 'proveedor' || rol === 'despachante') {
    const clerkUser = await (await clerkClient()).users.getUser(userId)
    const emails = clerkUser.emailAddresses
      .map(e => e.emailAddress?.toLowerCase() ?? '')
      .filter(Boolean)
    const allowed = rol === 'proveedor'
      ? (existing.emailsProveedor ?? []).some(e => emails.includes(e))
      : (existing.emailsDespachante ?? []).some(e => emails.includes(e))
    if (!allowed) return { error: 'Sin acceso' }
  } else {
    return { error: 'Sin acceso' }
  }

  try {
    await OC.findByIdAndUpdate(id, { $set: { [`documentos.${slot}`]: url } })
    return { data: { id } }
  } catch {
    return { error: 'Error al guardar el documento' }
  }
}
```

**4. src/app/api/sign-cloudinary-params/route.ts** — Crear el archivo nuevo:
```typescript
export const runtime = 'nodejs'

import { v2 as cloudinary } from 'cloudinary'

cloudinary.config({
  cloud_name: process.env.CLOUDINARY_CLOUD_NAME,
  api_key: process.env.CLOUDINARY_API_KEY,
  api_secret: process.env.CLOUDINARY_API_SECRET,
})

export async function POST(request: Request) {
  try {
    const body = await request.json()
    const { paramsToSign } = body as { paramsToSign: Record<string, string> }
    const signature = cloudinary.utils.api_sign_request(
      paramsToSign,
      process.env.CLOUDINARY_API_SECRET!
    )
    return Response.json({ signature })
  } catch (err) {
    console.error('[sign-cloudinary-params]', err)
    return new Response('Error', { status: 500 })
  }
}
```

**5. .env.example** — Reemplazar el bloque Cloudinary existente:
```bash
# Cloudinary (setup in Phase 6)
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=     # público — CldUploadWidget en cliente
NEXT_PUBLIC_CLOUDINARY_API_KEY=        # público — CldUploadWidget en cliente
CLOUDINARY_API_KEY=                    # privado — API Route de firma
CLOUDINARY_CLOUD_NAME=                 # privado — API Route de firma
CLOUDINARY_API_SECRET=                 # PRIVADO — nunca NEXT_PUBLIC_
```
</action>
  <verify>
    <automated>cd C:/Users/Equipo/Documents/DrivaOC && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/models/OC.ts contiene `packingList: { type: String, default: null }` en el bloque documentos
    - src/lib/mock-ocs.ts contiene `packingList: string | null` en la interface OCDetalle
    - src/actions/oc.ts contiene `packingList: d.documentos?.packingList ?? null` en serializeOC
    - src/actions/oc.ts contiene la función `updateOCDocumento` exportada con `export async function updateOCDocumento`
    - src/actions/oc.ts contiene `VALID_SLOTS` array con los 7 slots y validación `!VALID_SLOTS.includes`
    - src/app/api/sign-cloudinary-params/route.ts existe y su primera línea es `export const runtime = 'nodejs'`
    - src/app/api/sign-cloudinary-params/route.ts contiene `api_sign_request`
    - .env.example contiene `NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=`
    - .env.example contiene `CLOUDINARY_API_SECRET=`
    - `npx tsc --noEmit` sale con código 0 (sin errores de tipos)
  </acceptance_criteria>
  <done>Schema Mongoose con packingList, serializeOC actualizado, updateOCDocumento Server Action creado, Route Handler de firma creado, .env.example corregido.</done>
</task>

<task type="auto">
  <name>Tarea 2: DocumentSlots con upload real a Cloudinary + modal de confirmación</name>
  <read_first>
    - src/components/wizard/DocumentSlots.tsx (componente completo actual)
    - src/components/dashboard/DeleteModal.tsx (estructura CSS del modal de confirmación)
    - src/components/oc-detail/OCDetailHeader.tsx (patrón de clases CSS de botones)
  </read_first>
  <files>
    src/components/wizard/DocumentSlots.tsx
  </files>
  <action>
Reescribir `DocumentSlots.tsx` completamente. El componente pasa de usar `<input type="file">` local a usar `CldUploadWidget` con firma server-side. Las props se extienden con `ocId` (necesario para llamar `updateOCDocumento`).

**Props nuevas:**
```typescript
interface DocumentSlotsProps {
  readOnly?: boolean
  ocId?: string
  documentos?: {
    facturaProveedor: string | null
    facturaDespachante: string | null
    conocimientoEmbarque: string | null
    certificadoOrigen: string | null
    certificadoAnalisis?: string | null
    packingList?: string | null
    otro: string | null
  }
}
```

**Lógica del componente:**
- Estado: `const [confirmSlot, setConfirmSlot] = useState<string | null>(null)` — slot pendiente de confirmar reemplazo
- Estado: `const [openSlot, setOpenSlot] = useState<string | null>(null)` — slot cuyo widget debe abrirse
- Función `handleUploadAttempt(slot: string, existingUrl: string | null | undefined)`: si `existingUrl` existe, llama `setConfirmSlot(slot)`; si no, llama `setOpenSlot(slot)`.
- El `CldUploadWidget` se renderiza una vez fuera del map, controlado por `openSlot`. Cuando `openSlot !== null`, el widget hace `open()` automáticamente en un `useEffect` o directamente con el children prop.
- `onSuccess` del widget: recibir `result.info as { secure_url: string }`, llamar `updateOCDocumento(ocId!, openSlot!, url)`, luego limpiar `setOpenSlot(null)`.
- Modal de confirmación: se muestra cuando `confirmSlot !== null`. Botón "Cancelar" → `setConfirmSlot(null)`. Botón "Reemplazar" → `setOpenSlot(confirmSlot)` + `setConfirmSlot(null)`.

**En modo readOnly:** Los slots que tienen URL muestran un link `<a href={url} target="_blank" rel="noopener noreferrer">` con el nombre del slot en lugar del botón de upload.

**Slot "otro" (dinámico):** En modo no-readOnly, mantener el botón "Agregar otro documento". Los slots dinámicos también deben poder subir a Cloudinary (usar `slot: 'otro'` para todos, ya que el schema solo tiene un campo `otro`).

**Imports necesarios:**
```typescript
'use client'
import { useState, useEffect } from 'react'
import { Plus, FileText, ExternalLink } from 'lucide-react'
import { CldUploadWidget } from 'next-cloudinary'
import { updateOCDocumento } from '@/actions/oc'
```

**Estructura del widget (patrón de next-cloudinary signed upload):**
```typescript
<CldUploadWidget
  signatureEndpoint="/api/sign-cloudinary-params"
  options={{ resourceType: 'raw', folder: 'drivaoc-docs' }}
  onSuccess={(result) => {
    const url = (result.info as { secure_url: string }).secure_url
    if (openSlot && ocId) {
      updateOCDocumento(ocId, openSlot, url).then(() => setOpenSlot(null))
    }
  }}
  onClose={() => setOpenSlot(null)}
>
  {({ open }) => {
    // eslint-disable-next-line react-hooks/rules-of-hooks
    useEffect(() => {
      if (openSlot) open()
    }, [openSlot])
    return null
  }}
</CldUploadWidget>
```

Nota: Si el patrón `useEffect` dentro del children da advertencia de hooks, alternativa: renderizar el widget inline en cada `DocumentRow` con un `open` condicional basado en `openSlot === slot`.

**Modal de confirmación** (inline en DocumentSlots, sin extraer a componente):
```typescript
{confirmSlot !== null && (
  <div className="fixed inset-0 z-50 bg-texto/30 flex items-center justify-center p-4">
    <div className="bg-white rounded-xl border border-acento p-6 max-w-sm w-full flex flex-col gap-4">
      <p className="text-base font-medium text-titulares">¿Reemplazar el archivo existente?</p>
      <p className="text-sm font-normal text-texto/70">Esta acción reemplazará el PDF actual del slot.</p>
      <div className="flex gap-3 justify-end">
        <button
          type="button"
          onClick={() => setConfirmSlot(null)}
          className="border border-acento text-texto hover:bg-fondo px-4 py-2 rounded-lg font-normal min-h-[44px] transition-colors duration-150"
        >
          Cancelar
        </button>
        <button
          type="button"
          onClick={() => { setOpenSlot(confirmSlot); setConfirmSlot(null) }}
          className="bg-principal text-white hover:bg-titulares px-4 py-2 rounded-lg font-medium min-h-[44px] transition-colors duration-150"
        >
          Reemplazar
        </button>
      </div>
    </div>
  </div>
)}
```
</action>
  <verify>
    <automated>cd C:/Users/Equipo/Documents/DrivaOC && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/wizard/DocumentSlots.tsx contiene `import { CldUploadWidget } from 'next-cloudinary'`
    - src/components/wizard/DocumentSlots.tsx contiene `import { updateOCDocumento } from '@/actions/oc'`
    - src/components/wizard/DocumentSlots.tsx contiene `signatureEndpoint="/api/sign-cloudinary-params"`
    - src/components/wizard/DocumentSlots.tsx contiene `resourceType: 'raw'`
    - src/components/wizard/DocumentSlots.tsx contiene `confirmSlot` (estado para modal)
    - src/components/wizard/DocumentSlots.tsx contiene `¿Reemplazar el archivo existente?` (texto del modal)
    - src/components/wizard/DocumentSlots.tsx contiene `ocId` en la interface de props
    - src/components/wizard/DocumentSlots.tsx NO contiene `useRef` ni `<input type="file"`
    - `npx tsc --noEmit` sale con código 0
  </acceptance_criteria>
  <done>DocumentSlots usa CldUploadWidget con firma, muestra modal de confirmación al reemplazar, llama updateOCDocumento al completar el upload, y en modo readOnly muestra links de descarga.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/sign-cloudinary-params | El cliente envía paramsToSign; el servidor firma con el secret nunca expuesto |
| Browser → Cloudinary CDN | Upload directo con firma; sin pasar por el servidor de la app |
| Browser → updateOCDocumento (Server Action) | El cliente envía id + slot + url; el servidor verifica acceso antes de guardar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | updateOCDocumento | mitigate | Whitelist VALID_SLOTS de 7 valores; cualquier slot fuera de la lista retorna error 'Slot inválido' antes de tocar la DB |
| T-06-02 | Tampering | updateOCDocumento | mitigate | Guard multi-rol: importador verifica importadorId === userId; proveedor/despachante verifica email en emailsProveedor/emailsDespachante via Clerk |
| T-06-03 | Information Disclosure | CLOUDINARY_API_SECRET | mitigate | Secret solo en la API Route server-side; .env.example usa NEXT_PUBLIC_ únicamente para cloud_name y api_key |
| T-06-04 | Tampering | /api/sign-cloudinary-params | accept | El endpoint no verifica quién pide la firma — cualquier usuario autenticado puede obtener una firma. Aceptado porque Cloudinary valida el upload contra el preset/folder y los archivos son PDFs internos, no datos sensibles |
</threat_model>

<verification>
```bash
# TypeScript sin errores
cd C:/Users/Equipo/Documents/DrivaOC && npx tsc --noEmit

# packingList en schema
grep -n "packingList" src/lib/models/OC.ts

# packingList en tipo OCDetalle
grep -n "packingList" src/lib/mock-ocs.ts

# packingList en serializeOC
grep -n "packingList" src/actions/oc.ts

# updateOCDocumento exportado
grep -n "export async function updateOCDocumento" src/actions/oc.ts

# Route Handler de firma existe
grep -n "api_sign_request" src/app/api/sign-cloudinary-params/route.ts

# CldUploadWidget en DocumentSlots
grep -n "CldUploadWidget" src/components/wizard/DocumentSlots.tsx

# Modal de confirmación
grep -n "confirmSlot" src/components/wizard/DocumentSlots.tsx
```
</verification>

<success_criteria>
1. Schema Mongoose tiene `packingList` — uploads de packing list se persisten en MongoDB
2. `serializeOC` serializa `packingList` — la URL llega al cliente desde la DB
3. `updateOCDocumento` acepta todos los roles (importador, proveedor, despachante) con IDOR guard multi-rol
4. Route Handler `/api/sign-cloudinary-params` firma correctamente con `CLOUDINARY_API_SECRET` server-side
5. `DocumentSlots` con `CldUploadWidget` y modal de confirmación antes de reemplazar
6. `npx tsc --noEmit` sin errores
</success_criteria>

<output>
Al completar, crear `.planning/phases/06-files-integrations/06-01-SUMMARY.md`
</output>
