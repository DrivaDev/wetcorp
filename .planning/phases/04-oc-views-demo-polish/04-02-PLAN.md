---
phase: 04-oc-views-demo-polish
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/components/oc-detail/OCDetailHeader.tsx
  - src/components/oc-detail/OCDetailActions.tsx
  - src/components/oc-detail/OCDetailView.tsx
  - src/app/(importador)/importador/oc/[id]/page.tsx
  - src/app/(importador)/importador/oc/[id]/not-found.tsx
  - src/app/(importador)/importador/oc/[id]/loading.tsx
  - src/app/(importador)/importador/oc/[id]/editar/page.tsx
  - src/app/(importador)/importador/oc/[id]/editar/EditWizardLoader.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Existe /importador/oc/[id] con vista completa read-only (datos, productos, gastos, impuestos, documentos, valores)"
    - "El header muestra breadcrumb Dashboard / OC-XXX + EstadoBadge + botón Editar + botón Eliminar"
    - "El botón Eliminar (solo importador) abre el DeleteModal existente"
    - "Un id inexistente renderiza la página 404 custom 'OC no encontrada' con botón Volver al dashboard"
    - "Existe /importador/oc/[id]/editar que pre-carga la OC en sessionStorage y monta el wizard"
    - "Existe loading.tsx con skeleton de animate-pulse (sin spinner) que imita el layout real"
  artifacts:
    - path: "src/app/(importador)/importador/oc/[id]/page.tsx"
      provides: "Detail page Server Component que busca OCDetalle y calcula ValueCards"
      contains: "notFound"
    - path: "src/components/oc-detail/OCDetailHeader.tsx"
      provides: "Header con breadcrumb + EstadoBadge + acciones"
      contains: "EstadoBadge"
    - path: "src/components/oc-detail/OCDetailActions.tsx"
      provides: "Client Component con DeleteModal para el botón Eliminar"
      contains: "'use client'"
    - path: "src/app/(importador)/importador/oc/[id]/editar/EditWizardLoader.tsx"
      provides: "Pre-load de sessionStorage + WizardPage"
      contains: "oc-step1-draft"
    - path: "src/app/(importador)/importador/oc/[id]/not-found.tsx"
      provides: "404 custom del route group importador"
      contains: "OC no encontrada"
  key_links:
    - from: "src/app/(importador)/importador/oc/[id]/page.tsx"
      to: "src/lib/mock-ocs.ts"
      via: "MOCK_OCS_DETALLE.find por id"
      pattern: "MOCK_OCS_DETALLE"
    - from: "src/app/(importador)/importador/oc/[id]/page.tsx"
      to: "src/lib/wizard-calculations.ts"
      via: "calcFOBTotal / calcTotalGastos / calcSubtotalImpuestos"
      pattern: "calcFOBTotal"
    - from: "src/app/(importador)/importador/oc/[id]/editar/EditWizardLoader.tsx"
      to: "src/components/wizard/WizardPage.tsx"
      via: "sessionStorage['oc-step1-draft'] + render WizardPage"
      pattern: "WizardPage"
---

<objective>
Crear la experiencia completa de detalle y edición de OC para el rol importador: la página `/importador/oc/[id]` (vista read-only reutilizando los componentes del wizard), el header con breadcrumb + badge + acciones (Editar/Eliminar), la página 404 custom, el skeleton de loading, y la página `/importador/oc/[id]/editar` con el `EditWizardLoader` que pre-carga sessionStorage.

Purpose: Cierra el loop de demo para el importador (success criteria 1, 2 y 5 de la fase). Implementa D-01, D-02, D-06, D-07, D-09, D-11.
Output: Vista detalle + edición + 404 + loading del importador, reutilizando componentes de Phase 3 y el plan 04-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-oc-views-demo-polish/04-CONTEXT.md
@.planning/phases/04-oc-views-demo-polish/04-UI-SPEC.md
@.planning/phases/04-oc-views-demo-polish/04-PATTERNS.md
@.planning/phases/04-oc-views-demo-polish/04-01-SUMMARY.md

<interfaces>
<!-- Disponibles tras plan 04-01 -->
```typescript
// src/lib/mock-ocs.ts
export interface OCDetalle extends OC { /* ...productos, gastos, impuestos, documentos, tipoCambio, etc. */ }
export const MOCK_OCS_DETALLE: OCDetalle[]
// src/components/ui/EstadoBadge.tsx
export function EstadoBadge({ estado }: { estado: EstadoOC }): JSX.Element
// GastosCard, OtrosGastosSection, DocumentSlots ahora aceptan readOnly?: boolean
```

<!-- Cálculos (src/lib/wizard-calculations.ts) -->
```typescript
calcFOBTotal(rows: ProductRow[]): Decimal
calcSubtotalDespacho(g: GastosDespacho, tc: string): Decimal
calcSubtotalDespachante(g: GastosDespachante, tc: string): Decimal
calcSubtotalAdicionales(g: GastosAdicionales, tc: string): Decimal
calcSubtotalOtros(rows: OtroGastoRow[], tc: string): Decimal
calcTotalGastos(despacho, despachante, adicionales, otros, tc): Decimal
calcSubtotalImpuestos(imp: Impuestos, tc: string): Decimal
```

<!-- ValueCards props -->
```typescript
interface ValueCardsProps { fobUSD: Decimal; totalGastosUSD: Decimal; totalImpuestosUSD: Decimal; tipoCambio: string }
```

<!-- GastoField campos (de Step2Form.tsx) — reusar verbatim para los GastosCard del detalle -->
```typescript
const camposDespacho = [{key:'sim',label:'SIM (ARS)',divisa:'ARS'},{key:'derechos',label:'Derechos (ARS)',divisa:'ARS'},{key:'otros',label:'Otros (ARS)',divisa:'ARS'}]
const camposDespachante = [{key:'terminal',label:'Terminal (ARS)',divisa:'ARS'},{key:'fleteInternacional',label:'Flete internacional (USD)',divisa:'USD'},{key:'fleteInterno',label:'Flete interno (ARS)',divisa:'ARS'},{key:'senasa',label:'SENASA (ARS)',divisa:'ARS'},{key:'despachante',label:'Despachante (ARS)',divisa:'ARS'}]
const camposAdicionales = [{key:'depositoFiscal',label:'Depósito fiscal (ARS)',divisa:'ARS'},{key:'digitalizacion',label:'Digitalización (ARS)',divisa:'ARS'},{key:'estanciaCamion',label:'Estancia de camión (ARS)',divisa:'ARS'}]
const camposImpuestos = [{key:'iva',label:'IVA (ARS)',divisa:'ARS'},{key:'ivaAd',label:'IVA ad (ARS)',divisa:'ARS'},{key:'iibb',label:'IIBB (ARS)',divisa:'ARS'},{key:'iigg',label:'IIGG (ARS)',divisa:'ARS'}]
```

<!-- DeleteModal existente (src/components/dashboard/DeleteModal.tsx) -->
```typescript
<DeleteModal open={boolean} ocNumero={string} proveedor={string} onConfirm={() => void} onCancel={() => void} />
```

<!-- WizardPage (src/components/wizard/WizardPage.tsx) -->
```typescript
export function WizardPage({ initialStep }: { initialStep: string }): JSX.Element
```

<!-- Mapeo OCDetalle → Step1Data para EditWizardLoader -->
<!-- info: referenciaOC, estado, proveedor, emailsProveedor, despacho, emailsDespachante, paisOrigen, fechaOC, llegadaEstimada, fechaPago:'', tipoCambio, divisa, notas | productos: oc.productos -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: OCDetailHeader + OCDetailActions + OCDetailView (componentes compartidos del detalle)</name>
  <files>src/components/oc-detail/OCDetailHeader.tsx, src/components/oc-detail/OCDetailActions.tsx, src/components/oc-detail/OCDetailView.tsx</files>
  <read_first>
    - src/components/dashboard/OCTable.tsx (patrón de DeleteModal wiring líneas 89, 175-181; botones de acción)
    - src/components/dashboard/DeleteModal.tsx (firma del modal)
    - src/components/ui/EstadoBadge.tsx (creado en 04-01)
    - src/components/wizard/ResumenStep1.tsx, GastosCard.tsx, OtrosGastosSection.tsx, ValueCards.tsx, DocumentSlots.tsx (componentes reutilizables en read-only)
    - src/components/wizard/Step2Form.tsx (orden de secciones + arrays de campos GastoField)
    - .planning/phases/04-oc-views-demo-polish/04-UI-SPEC.md (sección "OCDetailHeader Component" + "OC Detail Page")
  </read_first>
  <action>
    Crear tres componentes en `src/components/oc-detail/`. Estos son reutilizados por las páginas de los 3 roles (plan 03 los importa también), por eso son rol-agnósticos vía props.

    A — `OCDetailHeader.tsx` (Server Component, SIN 'use client'). Props: `{ oc: OCDetalle; rol: 'importador' | 'proveedor' | 'despachante' }`. Estructura per UI-SPEC:
    ```
    <header className="bg-white border-b border-acento px-4 sm:px-8 py-4">
      <div className="max-w-5xl mx-auto flex flex-col gap-2">
        <nav className="text-sm font-normal text-texto/60">  // breadcrumb
          <Link href={`/${rol}/dashboard`} className="hover:text-principal">Dashboard</Link>
          <span className="mx-1 text-texto/40">/</span>
          <span className="text-texto">{oc.numeroOC}</span>
        </nav>
        <div className="flex items-center justify-between gap-4 flex-wrap">
          <div className="flex items-center gap-3">
            <h1 className="text-lg font-bold text-titulares">{oc.numeroOC}</h1>
            <EstadoBadge estado={oc.estado} />
          </div>
          <div className="flex items-center gap-2">
            <Link href={`/${rol}/oc/${oc.id}/editar`} className="flex items-center gap-2 px-4 py-2 rounded-lg border border-principal text-principal font-medium text-sm hover:bg-principal hover:text-white transition-colors min-h-[44px]">
              <Pencil size={16} /> Editar
            </Link>
            {rol === 'importador' && <OCDetailActions numeroOC={oc.numeroOC} proveedor={oc.proveedor} />}
          </div>
        </div>
      </div>
    </header>
    ```
    Importar `Pencil` de lucide-react, `EstadoBadge` de `@/components/ui/EstadoBadge`, `OCDetailActions` (sibling), `Link` de next/link, tipo `OCDetalle` de `@/lib/mock-ocs`.

    B — `OCDetailActions.tsx` (Client Component, CON `'use client'`). Props: `{ numeroOC: string; proveedor: string }`. Maneja el estado del DeleteModal igual que OCTable:
    ```typescript
    'use client'
    import { useState } from 'react'
    import { Trash2 } from 'lucide-react'
    import { DeleteModal } from '@/components/dashboard/DeleteModal'
    export function OCDetailActions({ numeroOC, proveedor }: { numeroOC: string; proveedor: string }) {
      const [open, setOpen] = useState(false)
      return (
        <>
          <button onClick={() => setOpen(true)} className="flex items-center gap-2 px-4 py-2 rounded-lg border border-red-300 text-red-600 font-medium text-sm hover:bg-red-50 transition-colors min-h-[44px]" aria-label="Eliminar OC">
            <Trash2 size={16} /> Eliminar
          </button>
          <DeleteModal open={open} ocNumero={numeroOC} proveedor={proveedor} onConfirm={() => setOpen(false)} onCancel={() => setOpen(false)} />
        </>
      )
    }
    ```
    Nota: la eliminación real es Phase 5 (deferred); aquí onConfirm solo cierra el modal (comportamiento mock, igual que OCTable).

    C — `OCDetailView.tsx` (Server Component, SIN 'use client'). Props: `{ oc: OCDetalle }`. Renderiza el cuerpo read-only reutilizando los componentes del wizard. Calcula los valores con decimal.js (NO en los componentes hijos). Estructura:
    ```typescript
    import { Decimal } from 'decimal.js'  // solo si se necesita tipar; las funciones ya devuelven Decimal
    import { calcFOBTotal, calcSubtotalDespacho, calcSubtotalDespachante, calcSubtotalAdicionales, calcSubtotalOtros, calcTotalGastos, calcSubtotalImpuestos } from '@/lib/wizard-calculations'
    import { ResumenStep1 } from '@/components/wizard/ResumenStep1'
    import { GastosCard } from '@/components/wizard/GastosCard'
    import { OtrosGastosSection } from '@/components/wizard/OtrosGastosSection'
    import { ValueCards } from '@/components/wizard/ValueCards'
    import { DocumentSlots } from '@/components/wizard/DocumentSlots'
    ```
    - Construir `step1Data = { info: { referenciaOC: oc.referenciaOC, estado: oc.estado, proveedor: oc.proveedor, emailsProveedor: oc.emailsProveedor, despacho: oc.despacho, emailsDespachante: oc.emailsDespachante, paisOrigen: oc.paisOrigen, fechaOC: oc.fechaOC, llegadaEstimada: oc.llegadaEstimada, fechaPago: '', tipoCambio: oc.tipoCambio, divisa: oc.divisa, notas: oc.notas }, productos: oc.productos }` tipado como `Step1Data`.
    - Calcular: `fobUSD = calcFOBTotal(oc.productos)`, subtotales de cada sección, `totalGastosUSD = calcTotalGastos(oc.gastosDespacho, oc.gastosDespachante, oc.gastosAdicionales, oc.otrosGastos, oc.tipoCambio)`, `totalImpuestosUSD = calcSubtotalImpuestos(oc.impuestos, oc.tipoCambio)`.
    - Definir los arrays `camposDespacho/camposDespachante/camposAdicionales/camposImpuestos` (copiar verbatim del bloque <interfaces>; tipar como `GastoField[]`).
    - Render dentro de `<main className="px-4 sm:px-8 py-6 max-w-5xl mx-auto flex flex-col gap-6">`:
      - `<ResumenStep1 step1Data={step1Data} />`
      - `<h2 className="text-base font-bold text-titulares">Gastos de importación</h2>`
      - `<GastosCard titulo="Despacho" campos={camposDespacho} values={oc.gastosDespacho as unknown as Record<string,string>} subtotalUSD={subtotalDespacho} tipoCambio={oc.tipoCambio} readOnly />` (y análogos Despachante con `subtotalDespachante`, "Gastos adicionales" con `subtotalAdicionales`).
      - `<OtrosGastosSection rows={oc.otrosGastos} subtotalUSD={subtotalOtros} tipoCambio={oc.tipoCambio} readOnly />`
      - `<h2 ...>Impuestos</h2>` + `<GastosCard titulo="Impuestos" campos={camposImpuestos} values={oc.impuestos as unknown as Record<string,string>} subtotalUSD={totalImpuestosUSD} tipoCambio={oc.tipoCambio} readOnly />`
      - `<DocumentSlots readOnly documentos={oc.documentos} />`
      - `<h2 ...>Valores Finales</h2>` + `<ValueCards fobUSD={fobUSD} totalGastosUSD={totalGastosUSD} totalImpuestosUSD={totalImpuestosUSD} tipoCambio={oc.tipoCambio} />`

    TypeScript strict, sin `any` (el cast `as unknown as Record<string,string>` es el patrón ya usado en Step2Form). Importar `Step1Data` y `GastoField` de los módulos correctos.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/oc-detail/OCDetailHeader.tsx` contiene `EstadoBadge` y `Dashboard`
    - `src/components/oc-detail/OCDetailHeader.tsx` NO contiene `'use client'`
    - `src/components/oc-detail/OCDetailActions.tsx` contiene `'use client'` y `DeleteModal`
    - `src/components/oc-detail/OCDetailView.tsx` contiene `calcFOBTotal` y `readOnly`
    - `src/components/oc-detail/OCDetailView.tsx` contiene `Valores Finales`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Header rol-agnóstico con breadcrumb/badge/Editar y Eliminar condicional; OCDetailActions maneja DeleteModal; OCDetailView renderiza todas las secciones en read-only con valores calculados en decimal.js; tsc pasa.</done>
</task>

<task type="auto">
  <name>Task 2: Páginas detail + not-found + loading del importador</name>
  <files>src/app/(importador)/importador/oc/[id]/page.tsx, src/app/(importador)/importador/oc/[id]/not-found.tsx, src/app/(importador)/importador/oc/[id]/loading.tsx</files>
  <read_first>
    - src/app/(importador)/importador/oc/nueva/page.tsx (patrón page.tsx + await de params/searchParams en Next.js 16)
    - src/components/oc-detail/OCDetailHeader.tsx y OCDetailView.tsx (creados en Task 1 — consumidos aquí)
    - src/components/dashboard/OCTable.tsx (SkeletonCells animate-pulse bg-acento/30 — patrón de skeleton)
    - .planning/phases/04-oc-views-demo-polish/04-UI-SPEC.md (secciones "404 / Not Found Page" + "Skeleton Layout")
  </read_first>
  <action>
    A — `page.tsx` (Server Component async):
    ```typescript
    import { notFound } from 'next/navigation'
    import { MOCK_OCS_DETALLE } from '@/lib/mock-ocs'
    import { OCDetailHeader } from '@/components/oc-detail/OCDetailHeader'
    import { OCDetailView } from '@/components/oc-detail/OCDetailView'

    interface PageProps { params: Promise<{ id: string }> }

    export default async function OCDetailPage({ params }: PageProps) {
      const { id } = await params
      const oc = MOCK_OCS_DETALLE.find((o) => o.id === id)
      if (!oc) notFound()
      return (
        <div className="bg-fondo min-h-screen">
          <OCDetailHeader oc={oc} rol="importador" />
          <OCDetailView oc={oc} />
        </div>
      )
    }
    ```
    CRÍTICO: `params` es Promise en Next.js 16 — siempre `await params`. NO agregar `'use client'`.

    B — `not-found.tsx` (Server Component) per D-11 / UI-SPEC:
    ```typescript
    import Link from 'next/link'
    import { FileSearch, ArrowLeft } from 'lucide-react'
    export default function OCNotFound() {
      return (
        <div className="flex flex-col items-center justify-center py-32 gap-6 text-center px-4">
          <FileSearch size={64} className="text-acento" />
          <div className="flex flex-col gap-2">
            <h1 className="text-lg font-bold text-titulares">OC no encontrada</h1>
            <p className="text-base font-normal text-texto/70 max-w-sm">No pudimos encontrar la orden de compra que buscás. Puede que haya sido eliminada o que el enlace sea incorrecto.</p>
          </div>
          <Link href="/importador/dashboard" className="inline-flex items-center gap-2 px-6 py-3 rounded-lg bg-principal text-white font-medium hover:bg-titulares transition-colors duration-150 min-h-[44px]">
            <ArrowLeft size={18} /> Volver al dashboard
          </Link>
        </div>
      )
    }
    ```

    C — `loading.tsx` (Server Component) per D-09 / UI-SPEC "Skeleton Layout". Replicar el layout real con barras `animate-pulse bg-acento/30` (NO spinner). Estructura mínima:
    - Header skeleton: contenedor `bg-white border-b border-acento px-4 sm:px-8 py-4`, dentro `max-w-5xl mx-auto flex flex-col gap-2`: una barra `h-4 w-32 rounded bg-acento/30 animate-pulse` (breadcrumb) y una fila con `h-7 w-24 rounded bg-acento/30 animate-pulse` + `h-6 w-20 rounded-full bg-acento/40 animate-pulse`.
    - `<main className="px-4 sm:px-8 py-6 max-w-5xl mx-auto flex flex-col gap-6 animate-pulse">` con:
      - Card "Datos OC": `rounded-xl border border-acento bg-white p-6` con título `h-5 w-28 rounded bg-acento/30 mb-4` y grid `grid-cols-2 gap-4` con 6 barras `h-10 rounded-lg bg-acento/20`.
      - 3 cards de gastos: cada una `rounded-xl border border-acento bg-white p-6`, título `h-5 w-36 rounded bg-acento/30 mb-4`, grid de 4 barras `h-10 rounded-lg bg-acento/20`.
      - ValueCards skeleton: `grid grid-cols-2 sm:grid-cols-4 gap-3` con 4 bloques `rounded-xl border border-acento bg-white p-4 h-20 bg-acento/20`.
      - DocumentSlots skeleton: `rounded-xl border border-acento bg-white divide-y divide-acento/40` con 4 filas `flex items-center gap-3 px-4 py-3` cada una con barras `h-4 w-4 bg-acento/30`, `h-4 flex-1 bg-acento/20`, `h-8 w-20 rounded-lg bg-acento/30`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(importador)/importador/oc/[id]/page.tsx` contiene `await params` y `notFound`
    - `src/app/(importador)/importador/oc/[id]/page.tsx` contiene `rol="importador"`
    - `src/app/(importador)/importador/oc/[id]/not-found.tsx` contiene `OC no encontrada` y `FileSearch`
    - `src/app/(importador)/importador/oc/[id]/loading.tsx` contiene `animate-pulse` y NO contiene la cadena `spinner` ni `animate-spin`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>La ruta /importador/oc/[id] renderiza el detalle completo; id inexistente dispara la 404 custom del route group; loading.tsx muestra skeleton sin spinner; tsc pasa.</done>
</task>

<task type="auto">
  <name>Task 3: Página de edición + EditWizardLoader del importador</name>
  <files>src/app/(importador)/importador/oc/[id]/editar/page.tsx, src/app/(importador)/importador/oc/[id]/editar/EditWizardLoader.tsx</files>
  <read_first>
    - src/components/wizard/Step1Form.tsx (lee sessionStorage['oc-step1-draft'] en useEffect — fuente de verdad del key y shape)
    - src/components/wizard/WizardPage.tsx (firma initialStep)
    - src/lib/mock-ocs.ts (OCDetalle + MOCK_OCS_DETALLE tras 04-01)
    - src/lib/wizard-types.ts (Step1Data, InfoGeneralState — shape destino del mapeo)
    - .planning/phases/04-oc-views-demo-polish/04-RESEARCH.md (sección "Mapping: OCDetalle → Step1Data")
  </read_first>
  <action>
    A — `editar/page.tsx` (Server Component async) per D-06:
    ```typescript
    import { EditWizardLoader } from './EditWizardLoader'
    interface PageProps { params: Promise<{ id: string }> }
    export default async function EditarOCPage({ params }: PageProps) {
      const { id } = await params
      return <EditWizardLoader ocId={id} rol="importador" />
    }
    ```

    B — `EditWizardLoader.tsx` (Client Component, CON `'use client'`). Lee la OCDetalle, mapea a Step1Data, escribe sessionStorage['oc-step1-draft'] y monta WizardPage:
    ```typescript
    'use client'
    import { useEffect } from 'react'
    import { useRouter } from 'next/navigation'
    import { WizardPage } from '@/components/wizard/WizardPage'
    import { MOCK_OCS_DETALLE } from '@/lib/mock-ocs'
    import type { Step1Data } from '@/lib/wizard-types'

    export function EditWizardLoader({ ocId, rol }: { ocId: string; rol: string }) {
      const router = useRouter()
      useEffect(() => {
        const oc = MOCK_OCS_DETALLE.find((o) => o.id === ocId)
        if (!oc) { router.replace(`/${rol}/dashboard`); return }
        const step1Data: Step1Data = {
          info: {
            referenciaOC: oc.referenciaOC,
            estado: oc.estado,
            proveedor: oc.proveedor,
            emailsProveedor: oc.emailsProveedor,
            despacho: oc.despacho,
            emailsDespachante: oc.emailsDespachante,
            paisOrigen: oc.paisOrigen,
            fechaOC: oc.fechaOC,
            llegadaEstimada: oc.llegadaEstimada,
            fechaPago: '',
            tipoCambio: oc.tipoCambio,
            divisa: oc.divisa,
            notas: oc.notas,
          },
          productos: oc.productos,
        }
        sessionStorage.setItem('oc-step1-draft', JSON.stringify(step1Data))
      }, [ocId, rol, router])
      return <WizardPage initialStep="1" />
    }
    ```
    Nota (D-07): al Guardar OC en modo edición se reusa el comportamiento mock del wizard (toast + redirect). Los gastos del Step2 NO se pre-cargan en Phase 4 (comportamiento mock aceptado, ver Assumption A1 del RESEARCH). No modificar Step1Form/Step2Form.

    TypeScript strict, sin `any`. El mapeo debe usar exactamente los campos de InfoGeneralState (`fechaOC` ya viene en DD/MM/AAAA desde OCDetalle, NO usar `oc.fecha`).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(importador)/importador/oc/[id]/editar/page.tsx` contiene `await params` y `EditWizardLoader`
    - `src/app/(importador)/importador/oc/[id]/editar/EditWizardLoader.tsx` contiene `'use client'`
    - `EditWizardLoader.tsx` contiene `sessionStorage.setItem('oc-step1-draft'`
    - `EditWizardLoader.tsx` contiene `WizardPage` y `rol="importador"` se pasa desde page.tsx
    - `EditWizardLoader.tsx` mapea `fechaOC: oc.fechaOC` (no `oc.fecha`)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>/importador/oc/[id]/editar pre-carga la OC en sessionStorage y monta el wizard en step 1; id inexistente redirige al dashboard; tsc pasa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser ↔ datos mock | Rutas protegidas por middleware Clerk (Phase 1); datos exclusivamente mock client/server-side; sin nuevas APIs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Elevation of Privilege | /importador/oc/[id]* | accept | Acceso ya gobernado por middleware de Clerk (Phase 1); Phase 4 no añade lógica de autorización. Permisos granulares diferidos a Phase 5 |
| T-04-04 | Tampering | EditWizardLoader sessionStorage | accept | sessionStorage es local al browser del usuario autenticado; no persiste ni cruza trust boundary. La persistencia real (con validación server-side) es Phase 5 |
| T-04-05 | Denial of Service | notFound() en id inexistente | mitigate | page.tsx llama notFound() cuando MOCK_OCS_DETALLE.find no encuentra el id, evitando render de undefined; renderiza 404 controlado |
</threat_model>

<verification>
- `npx tsc --noEmit` pasa sin errores
- `/importador/oc/3` y `/importador/oc/4` renderizan detalle completo con valores calculados (OCs con datos ricos)
- `/importador/oc/999` renderiza la 404 custom
- `/importador/oc/3/editar` monta el wizard con datos pre-cargados
- loading.tsx usa solo animate-pulse (sin spinner)
</verification>

<success_criteria>
- Success criteria 1, 2 y 5 de la fase cubiertos para el rol importador
- Vista detalle reutiliza componentes del wizard en read-only sin duplicar lógica de cálculo
- D-01, D-02, D-06, D-07, D-09, D-11 implementados para importador
</success_criteria>

<output>
After completion, create `.planning/phases/04-oc-views-demo-polish/04-02-SUMMARY.md`
</output>
