---
phase: 04-oc-views-demo-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/mock-ocs.ts
  - src/components/ui/EstadoBadge.tsx
  - src/components/wizard/GastosCard.tsx
  - src/components/wizard/OtrosGastosSection.tsx
  - src/components/wizard/DocumentSlots.tsx
  - src/components/dashboard/EmptyState.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "OC-003 y OC-004 tienen datos financieros completos (productos, gastos, impuestos, tipoCambio 1200) en MOCK_OCS_DETALLE"
    - "GastosCard renderiza valores como texto (no inputs) cuando recibe readOnly={true}"
    - "OtrosGastosSection oculta botones agregar/eliminar y muestra texto cuando readOnly={true}"
    - "DocumentSlots oculta botones de upload y muestra nombre de documento cuando readOnly={true}"
    - "EstadoBadge es un Server Component reutilizable que renderiza el pill de estado correcto"
    - "EmptyState muestra el copy del UI-SPEC diferenciado por rol y por filtros activos"
  artifacts:
    - path: "src/lib/mock-ocs.ts"
      provides: "Interface OCDetalle + array MOCK_OCS_DETALLE con 10 OCs (OC-003/OC-004 con datos ricos)"
      contains: "export interface OCDetalle"
    - path: "src/components/ui/EstadoBadge.tsx"
      provides: "Componente Server EstadoBadge con BADGE_MAP y ESTADO_LABELS"
      contains: "export function EstadoBadge"
    - path: "src/components/wizard/GastosCard.tsx"
      provides: "GastosCard con prop readOnly opcional"
      contains: "readOnly"
    - path: "src/components/wizard/OtrosGastosSection.tsx"
      provides: "OtrosGastosSection con prop readOnly opcional"
      contains: "readOnly"
    - path: "src/components/wizard/DocumentSlots.tsx"
      provides: "DocumentSlots con props readOnly y documentos"
      contains: "DocumentSlotsProps"
  key_links:
    - from: "src/lib/mock-ocs.ts"
      to: "src/lib/wizard-types.ts"
      via: "import de ProductRow, GastosDespacho, GastosDespachante, GastosAdicionales, Impuestos, OtroGastoRow"
      pattern: "from './wizard-types'"
    - from: "src/components/ui/EstadoBadge.tsx"
      to: "src/lib/mock-ocs.ts"
      via: "import type EstadoOC"
      pattern: "import type \\{ EstadoOC \\}"
---

<objective>
Construir la base compartida de la Phase 4: expandir los datos mock con la interface `OCDetalle` (datos financieros completos para OC-003 y OC-004), agregar modo `readOnly` opt-in a los tres componentes del wizard que tienen inputs (`GastosCard`, `OtrosGastosSection`, `DocumentSlots`), extraer `EstadoBadge` como Server Component reutilizable, y actualizar el copy de `EmptyState` al contrato del UI-SPEC.

Purpose: Todos los componentes y datos que las páginas de detalle/edición (plans 02 y 03) consumen deben existir primero. Este plan es Wave 1; los demás dependen de él.
Output: `mock-ocs.ts` extendido, `EstadoBadge.tsx` nuevo, `GastosCard`/`OtrosGastosSection`/`DocumentSlots` con prop `readOnly`, `EmptyState` con copy correcto.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-oc-views-demo-polish/04-CONTEXT.md
@.planning/phases/04-oc-views-demo-polish/04-UI-SPEC.md
@.planning/phases/04-oc-views-demo-polish/04-PATTERNS.md

<interfaces>
<!-- Tipos del wizard que OCDetalle debe consumir (src/lib/wizard-types.ts) -->
```typescript
export interface ProductRow { id: string; producto: string; descripcion: string; cantidad: string; valorUSD: string }
export interface GastosDespacho { sim: string; derechos: string; otros: string }
export interface GastosDespachante { terminal: string; fleteInternacional: string; fleteInterno: string; senasa: string; despachante: string }
export interface GastosAdicionales { depositoFiscal: string; digitalizacion: string; estanciaCamion: string }
export interface Impuestos { iva: string; ivaAd: string; iibb: string; iigg: string }
export interface OtroGastoRow { id: string; descripcion: string; monto: string; divisa: 'ARS' | 'USD' }
```

<!-- readOnlyClass exacto definido en ResumenStep1.tsx — reusar verbatim -->
```typescript
const readOnlyClass =
  'text-base font-normal text-texto bg-fondo px-3 py-2 rounded-lg border border-acento/50'
```

<!-- Clases del badge extraídas verbatim de OCTable.tsx getBadgeClasses() -->
```typescript
const base = 'text-sm px-2 py-0.5 rounded-full'
// borrador: `${base} bg-fondo text-titulares border border-acento font-light`
// en_proceso: `${base} bg-acento/50 text-titulares font-light`
// en_transito: `${base} bg-acento text-titulares font-normal`
// en_aduana: `${base} bg-principal/20 text-titulares font-normal`
// entregada: `${base} bg-principal/10 text-principal font-normal`
// cancelada: `${base} bg-texto/10 text-texto font-light line-through`
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expandir mock-ocs.ts con OCDetalle y MOCK_OCS_DETALLE</name>
  <files>src/lib/mock-ocs.ts</files>
  <read_first>
    - src/lib/mock-ocs.ts (estado actual: interface OC thin + array MOCK_OCS de 10 OCs)
    - src/lib/wizard-types.ts (tipos ProductRow, GastosDespacho, GastosDespachante, GastosAdicionales, Impuestos, OtroGastoRow que OCDetalle reusa)
    - .planning/phases/04-oc-views-demo-polish/04-RESEARCH.md (sección "OCDetalle Interface" + "Datos ricos requeridos")
  </read_first>
  <action>
    En `src/lib/mock-ocs.ts`, después de la interface `OC` existente (línea 19), agregar el import de tipos del wizard y la interface `OCDetalle`, e implementa per D-04/D-05:

    1. Al inicio del archivo agregar:
    ```typescript
    import type {
      ProductRow,
      GastosDespacho,
      GastosDespachante,
      GastosAdicionales,
      Impuestos,
      OtroGastoRow,
    } from './wizard-types'
    ```

    2. Agregar la interface (extiende OC):
    ```typescript
    export interface OCDetalle extends OC {
      referenciaOC: string
      emailsProveedor: string[]
      emailsDespachante: string[]
      despacho: string
      paisOrigen: string
      fechaOC: string          // DD/MM/AAAA
      llegadaEstimada: string  // DD/MM/AAAA
      tipoCambio: string       // e.g. "1200"
      divisa: 'ARS/USD' | 'ARS/EUR'
      notas: string
      productos: ProductRow[]
      gastosDespacho: GastosDespacho
      gastosDespachante: GastosDespachante
      gastosAdicionales: GastosAdicionales
      impuestos: Impuestos
      otrosGastos: OtroGastoRow[]
      documentos: {
        facturaProveedor: string | null
        facturaDespachante: string | null
        conocimientoEmbarque: string | null
        certificadoOrigen: string | null
        otro: string | null
      }
    }
    ```

    3. Agregar `export const MOCK_OCS_DETALLE: OCDetalle[]` con las 10 OCs (mismos id/numeroOC/proveedor/estado/fecha/despachante/emailProveedor/emailDespachante/numeroDespacho que MOCK_OCS — copiar esos campos verbatim de cada entrada existente).

       - OC-003 (id '3', Comercial Andina SRL, en_transito) y OC-004 (id '4', Distribuidora Norte, en_aduana) deben tener DATOS RICOS realistas (D-05):
         - `tipoCambio: '1200'`, `divisa: 'ARS/USD'`
         - `referenciaOC` igual al numeroOC, `despacho` igual al numeroDespacho existente
         - `emailsProveedor: ['proveedor@empresa.com']`, `emailsDespachante: ['despachante@logistica.com']`
         - `paisOrigen` realista (ej 'China' para OC-003, 'Brasil' para OC-004)
         - `fechaOC` y `llegadaEstimada` en formato DD/MM/AAAA (ej OC-003: fechaOC '20/03/2025', llegadaEstimada '15/04/2025'; OC-004: '10/04/2025' y '05/05/2025')
         - `productos`: 2-3 ProductRow con id (crypto.randomUUID no — usar string fijo tipo 'p1','p2'), producto, descripcion, cantidad y valorUSD coherentes (ej '100' unidades a '45.50' USD)
         - `gastosDespacho`: { sim: '85000', derechos: '320000', otros: '40000' }
         - `gastosDespachante`: { terminal: '150000', fleteInternacional: '3200', fleteInterno: '90000', senasa: '25000', despachante: '180000' }
         - `gastosAdicionales`: { depositoFiscal: '60000', digitalizacion: '15000', estanciaCamion: '45000' }
         - `impuestos`: { iva: '1100000', ivaAd: '420000', iibb: '210000', iigg: '95000' }
         - `otrosGastos`: 1 fila ej { id: 'o1', descripcion: 'Seguro de carga', monto: '850', divisa: 'USD' }
         - `documentos`: facturaProveedor con un nombre de archivo string ej 'factura-andina.pdf', el resto null (varía entre las 2)
       - Las otras 8 OCs (id 1,2,5,6,7,8,9,10): estado VACÍO en secciones financieras:
         - `tipoCambio: '1200'`, `divisa: 'ARS/USD'`, `referenciaOC` = numeroOC, `despacho` = numeroDespacho existente
         - `emailsProveedor: [oc.emailProveedor]`, `emailsDespachante: [oc.emailDespachante]`
         - `paisOrigen: ''`, `fechaOC` y `llegadaEstimada` derivadas de `fecha` ISO a DD/MM/AAAA o '' si se prefiere
         - `productos: []`, `gastosDespacho: { sim: '', derechos: '', otros: '' }`, `gastosDespachante: { terminal: '', fleteInternacional: '', fleteInterno: '', senasa: '', despachante: '' }`, `gastosAdicionales: { depositoFiscal: '', digitalizacion: '', estanciaCamion: '' }`, `impuestos: { iva: '', ivaAd: '', iibb: '', iigg: '' }`, `otrosGastos: []`
         - `documentos`: todos los campos null
         - `notas: ''`

    NO eliminar ni modificar `MOCK_OCS` ni `OC` ni `EstadoOC` — solo agregar. TypeScript strict: sin `any`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/mock-ocs.ts` contiene `export interface OCDetalle extends OC`
    - `src/lib/mock-ocs.ts` contiene `export const MOCK_OCS_DETALLE`
    - `src/lib/mock-ocs.ts` contiene `from './wizard-types'`
    - `src/lib/mock-ocs.ts` contiene `tipoCambio: '1200'` al menos 2 veces
    - `npx tsc --noEmit` exits 0
    - grep `id: '3'` y `id: '4'` presentes en MOCK_OCS_DETALLE con arrays productos no vacíos
  </acceptance_criteria>
  <done>OCDetalle definida y MOCK_OCS_DETALLE con 10 entradas; OC-003 y OC-004 con datos financieros completos; el resto con secciones financieras vacías; tsc pasa.</done>
</task>

<task type="auto">
  <name>Task 2: Crear EstadoBadge + actualizar copy de EmptyState</name>
  <files>src/components/ui/EstadoBadge.tsx, src/components/dashboard/EmptyState.tsx</files>
  <read_first>
    - src/components/dashboard/OCTable.tsx (getBadgeClasses líneas 19-39 + ESTADO_LABELS — fuente de las clases del badge)
    - src/components/dashboard/EmptyState.tsx (copy actual a reemplazar)
    - .planning/phases/04-oc-views-demo-polish/04-UI-SPEC.md (sección "EstadoBadge Color System" + "Empty State Component" + tabla Copywriting Contract)
  </read_first>
  <action>
    PARTE A — Crear `src/components/ui/EstadoBadge.tsx` como Server Component (SIN `'use client'`):
    ```typescript
    import type { EstadoOC } from '@/lib/mock-ocs'

    const base = 'text-sm px-2 py-0.5 rounded-full'

    const BADGE_MAP: Record<EstadoOC, string> = {
      borrador:    `${base} bg-fondo text-titulares border border-acento font-light`,
      en_proceso:  `${base} bg-acento/50 text-titulares font-light`,
      en_transito: `${base} bg-acento text-titulares font-normal`,
      en_aduana:   `${base} bg-principal/20 text-titulares font-normal`,
      entregada:   `${base} bg-principal/10 text-principal font-normal`,
      cancelada:   `${base} bg-texto/10 text-texto font-light line-through`,
    }

    const ESTADO_LABELS: Record<EstadoOC, string> = {
      borrador: 'Borrador',
      en_proceso: 'En proceso',
      en_transito: 'En tránsito',
      en_aduana: 'En aduana',
      entregada: 'Entregada',
      cancelada: 'Cancelada',
    }

    export function EstadoBadge({ estado }: { estado: EstadoOC }) {
      return <span className={BADGE_MAP[estado]}>{ESTADO_LABELS[estado]}</span>
    }
    ```

    PARTE B — Actualizar `src/components/dashboard/EmptyState.tsx` con el copy del UI-SPEC. La firma actual `{ rol, hasFilters }` se mantiene. Reemplazar el bloque de copy hardcodeado por lógica diferenciada por rol Y filtros:
    - Si `hasFilters`: heading `'Sin resultados'`, body `'Probá cambiando el filtro de estado o el nombre del proveedor.'`, sin CTA.
    - Si NO hasFilters y `rol === 'importador'`: heading `'Todavía no hay OCs'`, body `'¡Creá tu primera orden de compra!'`, CTA `'Nueva OC'` → `/importador/oc/nueva` (mantener el `<Link>` existente con icono `<PlusCircle size={18} />`).
    - Si NO hasFilters y rol es `'proveedor'` o `'despachante'`: heading `'No hay OCs asignadas'`, body `'Cuando un importador te asigne una OC, aparecerá aquí.'`, sin CTA.

    Mantener: el icono `<FileText size={48} className="text-acento" />`, contenedor `flex flex-col items-center justify-center py-16 gap-4 text-center`, heading `text-lg font-bold text-titulares` (el UI-SPEC usa text-lg; actualizar el `text-xl` actual a `text-lg`), body `text-base font-normal text-texto/70 max-w-sm`. La condición del CTA pasa a ser `!hasFilters && rol === 'importador'`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/ui/EstadoBadge.tsx` existe y contiene `export function EstadoBadge`
    - `src/components/ui/EstadoBadge.tsx` NO contiene `'use client'`
    - `src/components/dashboard/EmptyState.tsx` contiene la cadena `Todavía no hay OCs`
    - `src/components/dashboard/EmptyState.tsx` contiene la cadena `Sin resultados`
    - `src/components/dashboard/EmptyState.tsx` contiene la cadena `No hay OCs asignadas`
    - `src/components/dashboard/EmptyState.tsx` contiene la cadena `text-lg font-bold text-titulares`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>EstadoBadge creado como Server Component con clases verbatim de OCTable; EmptyState muestra los 4 estados de copy del UI-SPEC; tsc pasa.</done>
</task>

<task type="auto">
  <name>Task 3: Agregar prop readOnly a GastosCard, OtrosGastosSection y DocumentSlots</name>
  <files>src/components/wizard/GastosCard.tsx, src/components/wizard/OtrosGastosSection.tsx, src/components/wizard/DocumentSlots.tsx</files>
  <read_first>
    - src/components/wizard/GastosCard.tsx (interface GastosCardProps, render de inputs)
    - src/components/wizard/OtrosGastosSection.tsx (callbacks onAdd/onRemove/onChange, botones agregar/eliminar)
    - src/components/wizard/DocumentSlots.tsx (DocumentRow con upload, sin props actualmente)
    - src/components/wizard/ResumenStep1.tsx (readOnlyClass exacto línea 19)
    - .planning/phases/04-oc-views-demo-polish/04-UI-SPEC.md (sección "Read-Only Mode Visual Treatment")
  </read_first>
  <action>
    Modo read-only OPT-IN: el comportamiento existente del wizard de creación NO cambia. Definir en cada archivo:
    ```typescript
    const readOnlyClass =
      'text-base font-normal text-texto bg-fondo px-3 py-2 rounded-lg border border-acento/50'
    ```

    A — `GastosCard.tsx`:
    - Cambiar la interface: `onChange?: (key: string, value: string) => void` (opcional) y agregar `readOnly?: boolean`.
    - En el render de cada campo, cuando `readOnly` es true mostrar `<p className={readOnlyClass}>{values[campo.key] || '—'}</p>` en lugar del `<input>`. Cuando es false/undefined, mantener el `<input>` actual pero llamar `onChange?.(campo.key, e.target.value)` (optional chaining).
    - El subtotal footer se mantiene igual en ambos modos.

    B — `OtrosGastosSection.tsx`:
    - Cambiar la interface: `onAdd?`, `onRemove?`, `onChange?` todos opcionales, agregar `readOnly?: boolean`.
    - Cuando `readOnly`:
      - Reemplazar los 3 inputs/select de cada fila por `<p>` read-only: descripción y `monto + divisa` (ej `<p className={readOnlyClass}>{row.descripcion || '—'}</p>` y `<p className={readOnlyClass}>{row.monto} {row.divisa}</p>`).
      - Ocultar el botón Trash2 por fila (línea 57-64).
      - Ocultar el botón "Agregar gasto" (línea 69-76).
    - Los handlers internos usan optional chaining: `onChange?.(...)`, `onAdd?.()`, `onRemove?.(...)`.
    - El subtotal footer se mantiene.

    C — `DocumentSlots.tsx`:
    - Agregar interface y props:
    ```typescript
    interface DocumentSlotsProps {
      readOnly?: boolean
      documentos?: {
        facturaProveedor: string | null
        facturaDespachante: string | null
        conocimientoEmbarque: string | null
        certificadoOrigen: string | null
        otro: string | null
      }
    }
    export function DocumentSlots({ readOnly, documentos }: DocumentSlotsProps = {}) {
    ```
    - Pasar `readOnly` y un `fileName` a cada `DocumentRow`. Mapear los FIXED_SLOTS a las keys de `documentos`: 'Factura proveedor'→facturaProveedor, 'Factura despachante'→facturaDespachante, 'Conocimiento de embarque'→conocimientoEmbarque, 'Certificado de Origen'→certificadoOrigen.
    - En `DocumentRow`, agregar props `readOnly?: boolean` y `fileNameProp?: string | null`. Cuando `readOnly`:
      - Mostrar `<span>{fileNameProp ?? 'Sin archivo adjunto'}</span>` (usar el nombre pasado, no el state local).
      - Ocultar el botón "Adjuntar/Cambiar" (líneas 62-69) y el botón X / onRemove (líneas 71-80).
    - Cuando `readOnly`, ocultar el botón "Agregar otro documento" (líneas 121-128).
    - El modo de creación (sin props) se mantiene idéntico al actual.

    TypeScript strict: sin `any`. No agregar `'use client'` extra (estos ya lo tienen).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/wizard/GastosCard.tsx` contiene `readOnly?: boolean`
    - `src/components/wizard/GastosCard.tsx` contiene `onChange?:`
    - `src/components/wizard/OtrosGastosSection.tsx` contiene `readOnly?: boolean`
    - `src/components/wizard/OtrosGastosSection.tsx` contiene `onAdd?:`
    - `src/components/wizard/DocumentSlots.tsx` contiene `interface DocumentSlotsProps`
    - `src/components/wizard/DocumentSlots.tsx` contiene `documentos?:`
    - Los 3 archivos contienen la cadena `readOnlyClass` o reusan el patrón read-only
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Los tres componentes aceptan readOnly como prop opt-in; en modo read-only ocultan inputs/botones y muestran texto; el wizard de creación sigue funcionando sin cambios; tsc pasa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser ↔ datos mock | Phase 4 usa exclusivamente datos mock client-side; no hay nuevas rutas de API ni input de usuario persistido |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | MOCK_OCS_DETALLE | accept | Datos ficticios sin PII real; no se persiste ni transmite. Riesgo nulo en fase mock |
| T-04-02 | Tampering | readOnly props en componentes wizard | accept | readOnly es solo presentación UI; no impone autorización (eso es Phase 5 con Server Actions). Sin trust boundary de seguridad en esta fase |
</threat_model>

<verification>
- `npx tsc --noEmit` pasa sin errores (TypeScript strict, sin any)
- `MOCK_OCS_DETALLE` exporta 10 OCs; OC-003 y OC-004 con productos/gastos/impuestos poblados
- El wizard de creación (`/importador/oc/nueva`) sigue renderizando inputs editables (regresión: GastosCard/OtrosGastosSection/DocumentSlots sin readOnly mantienen comportamiento)
</verification>

<success_criteria>
- OCDetalle + MOCK_OCS_DETALLE disponibles para que plans 02/03 los consuman
- GastosCard, OtrosGastosSection, DocumentSlots soportan readOnly
- EstadoBadge reutilizable como Server Component
- EmptyState con copy del UI-SPEC
- tsc sin errores
</success_criteria>

<output>
After completion, create `.planning/phases/04-oc-views-demo-polish/04-01-SUMMARY.md`
</output>
