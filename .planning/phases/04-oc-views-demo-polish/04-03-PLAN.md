---
phase: 04-oc-views-demo-polish
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/app/(proveedor)/proveedor/oc/[id]/page.tsx
  - src/app/(proveedor)/proveedor/oc/[id]/not-found.tsx
  - src/app/(proveedor)/proveedor/oc/[id]/loading.tsx
  - src/app/(proveedor)/proveedor/oc/[id]/editar/page.tsx
  - src/app/(proveedor)/proveedor/oc/[id]/editar/EditWizardLoader.tsx
  - src/app/(despachante)/despachante/oc/[id]/page.tsx
  - src/app/(despachante)/despachante/oc/[id]/not-found.tsx
  - src/app/(despachante)/despachante/oc/[id]/loading.tsx
  - src/app/(despachante)/despachante/oc/[id]/editar/page.tsx
  - src/app/(despachante)/despachante/oc/[id]/editar/EditWizardLoader.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Proveedor ve /proveedor/oc/[id] con vista read-only completa SIN botón Eliminar y CON botón Editar"
    - "Despachante ve /despachante/oc/[id] análogo: read-only completo sin Eliminar, con Editar"
    - "Ambos roles tienen /[rol]/oc/[id]/editar que pre-carga el wizard"
    - "Ambos roles tienen 404 custom que vuelve a su dashboard correspondiente"
    - "Ambos roles tienen loading skeleton sin spinner"
  artifacts:
    - path: "src/app/(proveedor)/proveedor/oc/[id]/page.tsx"
      provides: "Detail page proveedor (rol='proveedor', sin Eliminar)"
      contains: "rol=\"proveedor\""
    - path: "src/app/(despachante)/despachante/oc/[id]/page.tsx"
      provides: "Detail page despachante (rol='despachante', sin Eliminar)"
      contains: "rol=\"despachante\""
    - path: "src/app/(proveedor)/proveedor/oc/[id]/editar/EditWizardLoader.tsx"
      provides: "Pre-load sessionStorage proveedor"
      contains: "rol=\"proveedor\""
    - path: "src/app/(despachante)/despachante/oc/[id]/editar/EditWizardLoader.tsx"
      provides: "Pre-load sessionStorage despachante"
      contains: "rol=\"despachante\""
  key_links:
    - from: "src/app/(proveedor)/proveedor/oc/[id]/page.tsx"
      to: "src/components/oc-detail/OCDetailHeader.tsx"
      via: "render OCDetailHeader rol='proveedor' (sin OCDetailActions)"
      pattern: "OCDetailHeader"
    - from: "src/app/(despachante)/despachante/oc/[id]/page.tsx"
      to: "src/components/oc-detail/OCDetailView.tsx"
      via: "render OCDetailView"
      pattern: "OCDetailView"
---

<objective>
Crear las páginas de detalle, edición, 404 y loading para los roles proveedor y despachante, reutilizando los componentes compartidos (`OCDetailHeader`, `OCDetailView`) y el patrón `EditWizardLoader` que se establecieron para el importador en el plan 04-02. La única diferencia funcional: proveedor y despachante NO tienen botón Eliminar (D-03/D-08), pero SÍ tienen botón Editar.

Purpose: Cubre success criteria 3 y 4 de la fase. Implementa D-03 y D-08 (proveedor/despachante con vista read-only + edición, sin eliminación).
Output: 10 archivos (5 por rol) bajo los route groups (proveedor) y (despachante), análogos a los del importador con layout Navbar heredado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-oc-views-demo-polish/04-CONTEXT.md
@.planning/phases/04-oc-views-demo-polish/04-UI-SPEC.md
@.planning/phases/04-oc-views-demo-polish/04-PATTERNS.md
@.planning/phases/04-oc-views-demo-polish/04-01-SUMMARY.md
@.planning/phases/04-oc-views-demo-polish/04-02-SUMMARY.md

<interfaces>
<!-- Componentes compartidos del detalle (creados en plan 04-02) -->
```typescript
// src/components/oc-detail/OCDetailHeader.tsx (Server Component)
// rol-agnóstico: cuando rol !== 'importador', NO renderiza el botón Eliminar
export function OCDetailHeader({ oc, rol }: { oc: OCDetalle; rol: 'importador' | 'proveedor' | 'despachante' }): JSX.Element
// src/components/oc-detail/OCDetailView.tsx (Server Component)
export function OCDetailView({ oc }: { oc: OCDetalle }): JSX.Element
```

<!-- Patrón EditWizardLoader (del plan 04-02 — replicar por rol) -->
```typescript
'use client'
export function EditWizardLoader({ ocId, rol }: { ocId: string; rol: string }) { /* mapea OCDetalle→Step1Data, escribe sessionStorage['oc-step1-draft'], render WizardPage initialStep="1" */ }
```

<!-- Layout heredado: (proveedor) y (despachante) usan Navbar (no Sidebar) — sin cambios necesarios -->
```typescript
// src/app/(proveedor)/layout.tsx → <Navbar rol="proveedor" /> + <Footer />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Páginas detail + not-found + loading + editar del PROVEEDOR</name>
  <files>src/app/(proveedor)/proveedor/oc/[id]/page.tsx, src/app/(proveedor)/proveedor/oc/[id]/not-found.tsx, src/app/(proveedor)/proveedor/oc/[id]/loading.tsx, src/app/(proveedor)/proveedor/oc/[id]/editar/page.tsx, src/app/(proveedor)/proveedor/oc/[id]/editar/EditWizardLoader.tsx</files>
  <read_first>
    - src/app/(importador)/importador/oc/[id]/page.tsx (analog directo — creado en 04-02)
    - src/app/(importador)/importador/oc/[id]/not-found.tsx (analog — adaptar href al dashboard de proveedor)
    - src/app/(importador)/importador/oc/[id]/loading.tsx (analog — copiar verbatim)
    - src/app/(importador)/importador/oc/[id]/editar/page.tsx y EditWizardLoader.tsx (analogs — cambiar rol)
    - src/components/oc-detail/OCDetailHeader.tsx (confirma que rol !== importador omite Eliminar)
    - src/app/(proveedor)/layout.tsx (confirma layout Navbar heredado)
  </read_first>
  <action>
    Replicar las páginas del importador (04-02) adaptando rol y rutas a proveedor. NO se renderiza botón Eliminar porque `OCDetailHeader` lo condiciona a `rol === 'importador'` (D-03/D-08).

    A — `page.tsx` (Server Component async): idéntico al del importador pero `rol="proveedor"`:
    ```typescript
    import { notFound } from 'next/navigation'
    import { MOCK_OCS_DETALLE } from '@/lib/mock-ocs'
    import { OCDetailHeader } from '@/components/oc-detail/OCDetailHeader'
    import { OCDetailView } from '@/components/oc-detail/OCDetailView'
    interface PageProps { params: Promise<{ id: string }> }
    export default async function OCDetailPage({ params }: PageProps) {
      const { id } = await params
      const oc = MOCK_OCS_DETALLE.find((o) => o.id === id)
      if (!oc) notFound()
      return (
        <div className="bg-fondo min-h-screen">
          <OCDetailHeader oc={oc} rol="proveedor" />
          <OCDetailView oc={oc} />
        </div>
      )
    }
    ```

    B — `not-found.tsx`: copiar del importador, cambiar `href="/importador/dashboard"` por `href="/proveedor/dashboard"`. Mismo copy "OC no encontrada", mismo FileSearch/ArrowLeft.

    C — `loading.tsx`: copiar verbatim el skeleton del importador (mismo layout de barras animate-pulse; no depende del rol).

    D — `editar/page.tsx` (Server Component async):
    ```typescript
    import { EditWizardLoader } from './EditWizardLoader'
    interface PageProps { params: Promise<{ id: string }> }
    export default async function EditarOCPage({ params }: PageProps) {
      const { id } = await params
      return <EditWizardLoader ocId={id} rol="proveedor" />
    }
    ```

    E — `editar/EditWizardLoader.tsx` (Client Component): copiar verbatim el del importador (es rol-agnóstico vía prop `rol`; usa `router.replace('/${rol}/dashboard')` y escribe `oc-step1-draft`). Misma lógica de mapeo OCDetalle→Step1Data con `fechaOC: oc.fechaOC`.

    TypeScript strict, sin `any`. `await params` (Next.js 16). Solo EditWizardLoader lleva `'use client'`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(proveedor)/proveedor/oc/[id]/page.tsx` contiene `rol="proveedor"` y `notFound`
    - `src/app/(proveedor)/proveedor/oc/[id]/not-found.tsx` contiene `href="/proveedor/dashboard"` y `OC no encontrada`
    - `src/app/(proveedor)/proveedor/oc/[id]/loading.tsx` contiene `animate-pulse` y NO contiene `animate-spin`
    - `src/app/(proveedor)/proveedor/oc/[id]/editar/page.tsx` contiene `rol="proveedor"` y `await params`
    - `src/app/(proveedor)/proveedor/oc/[id]/editar/EditWizardLoader.tsx` contiene `'use client'` y `sessionStorage.setItem('oc-step1-draft'`
    - Ninguna de las páginas de proveedor renderiza `OCDetailActions` ni botón Eliminar directamente
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>El proveedor accede a detalle (sin Eliminar, con Editar), edición, 404 y loading bajo su route group con Navbar; tsc pasa.</done>
</task>

<task type="auto">
  <name>Task 2: Páginas detail + not-found + loading + editar del DESPACHANTE</name>
  <files>src/app/(despachante)/despachante/oc/[id]/page.tsx, src/app/(despachante)/despachante/oc/[id]/not-found.tsx, src/app/(despachante)/despachante/oc/[id]/loading.tsx, src/app/(despachante)/despachante/oc/[id]/editar/page.tsx, src/app/(despachante)/despachante/oc/[id]/editar/EditWizardLoader.tsx</files>
  <read_first>
    - src/app/(proveedor)/proveedor/oc/[id]/page.tsx (analog directo — creado en Task 1 de este plan)
    - src/app/(proveedor)/proveedor/oc/[id]/not-found.tsx (analog — adaptar href al dashboard de despachante)
    - src/app/(proveedor)/proveedor/oc/[id]/loading.tsx (analog — copiar verbatim)
    - src/app/(proveedor)/proveedor/oc/[id]/editar/page.tsx y EditWizardLoader.tsx (analogs — cambiar rol)
    - src/app/(despachante)/layout.tsx (confirma layout Navbar heredado)
  </read_first>
  <action>
    Replicar exactamente las páginas del proveedor (Task 1) adaptando rol y rutas a despachante. Tampoco renderiza botón Eliminar (D-03/D-08).

    A — `page.tsx`: idéntico al de proveedor pero `rol="despachante"`. Importa `MOCK_OCS_DETALLE`, `OCDetailHeader`, `OCDetailView`; `await params`; `notFound()` si no existe; render `<OCDetailHeader oc={oc} rol="despachante" />` + `<OCDetailView oc={oc} />` dentro de `<div className="bg-fondo min-h-screen">`.

    B — `not-found.tsx`: copiar del proveedor, cambiar `href` a `/despachante/dashboard`. Mismo copy "OC no encontrada".

    C — `loading.tsx`: copiar verbatim el skeleton (rol-agnóstico).

    D — `editar/page.tsx`: igual al de proveedor pero `rol="despachante"`.

    E — `editar/EditWizardLoader.tsx`: copiar verbatim (rol-agnóstico vía prop); page.tsx pasa `rol="despachante"`.

    TypeScript strict, sin `any`. `await params`. Solo EditWizardLoader lleva `'use client'`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(despachante)/despachante/oc/[id]/page.tsx` contiene `rol="despachante"` y `notFound`
    - `src/app/(despachante)/despachante/oc/[id]/not-found.tsx` contiene `href="/despachante/dashboard"` y `OC no encontrada`
    - `src/app/(despachante)/despachante/oc/[id]/loading.tsx` contiene `animate-pulse` y NO contiene `animate-spin`
    - `src/app/(despachante)/despachante/oc/[id]/editar/page.tsx` contiene `rol="despachante"` y `await params`
    - `src/app/(despachante)/despachante/oc/[id]/editar/EditWizardLoader.tsx` contiene `'use client'` y `sessionStorage.setItem('oc-step1-draft'`
    - Ninguna página de despachante renderiza `OCDetailActions` ni botón Eliminar
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>El despachante accede a detalle (sin Eliminar, con Editar), edición, 404 y loading bajo su route group con Navbar; tsc pasa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser ↔ datos mock | Rutas protegidas por middleware Clerk (Phase 1) scoped por route group; datos mock; sin nuevas APIs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Elevation of Privilege | botón Eliminar omitido para proveedor/despachante | mitigate | OCDetailHeader condiciona el botón Eliminar a `rol === 'importador'`; proveedor/despachante no ven la acción destructiva (D-08). La autorización server-side real es Phase 5 |
| T-04-07 | Information Disclosure | /proveedor/oc/[id] y /despachante/oc/[id] | accept | Datos mock sin PII; el scoping real por rol (qué OCs ve cada usuario) se implementa con queries en Phase 5. En fase mock todos los roles pueden ver cualquier id |
| T-04-08 | Denial of Service | notFound() en id inexistente | mitigate | page.tsx llama notFound() cuando find no encuentra el id; renderiza 404 controlado del route group |
</threat_model>

<verification>
- `npx tsc --noEmit` pasa sin errores
- `/proveedor/oc/3` y `/despachante/oc/4` renderizan detalle completo SIN botón Eliminar, CON botón Editar
- `/proveedor/oc/3/editar` y `/despachante/oc/3/editar` montan el wizard pre-cargado
- `/proveedor/oc/999` y `/despachante/oc/999` renderizan 404 con botón al dashboard correcto
- loading.tsx usa solo animate-pulse
</verification>

<success_criteria>
- Success criteria 3 y 4 de la fase cubiertos
- D-03 y D-08 implementados: proveedor/despachante con vista + edición, sin eliminación
- Reutilización total de OCDetailHeader/OCDetailView sin duplicar lógica de render
</success_criteria>

<output>
After completion, create `.planning/phases/04-oc-views-demo-polish/04-03-SUMMARY.md`
</output>
