---
phase: 05-backend-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - next.config.ts
  - src/lib/mongodb.ts
  - src/lib/models/OC.ts
  - src/lib/models/User.ts
  - src/app/api/webhooks/clerk/route.ts
  - src/middleware.ts
  - .env.example
autonomous: true
requirements:
  - AUTH-02

must_haves:
  truths:
    - "Mongoose se conecta a MongoDB Atlas usando el singleton global (maxPoolSize 5, bufferCommands false)"
    - "Los modelos OC y User son importables desde Server Actions sin error 'Cannot overwrite model once compiled'"
    - "El webhook POST /api/webhooks/clerk crea un documento User en MongoDB al recibir user.created"
    - "El middleware no bloquea /api/webhooks/clerk con 401"
  artifacts:
    - path: "src/lib/mongodb.ts"
      provides: "singleton connectDB con global.mongooseCache"
      contains: "global.mongooseCache"
    - path: "src/lib/models/OC.ts"
      provides: "Mongoose model OC con schema completo"
      contains: "mongoose.models.OC ?? mongoose.model"
    - path: "src/lib/models/User.ts"
      provides: "Mongoose model User"
      contains: "mongoose.models.User ?? mongoose.model"
    - path: "src/app/api/webhooks/clerk/route.ts"
      provides: "Route Handler que crea User en MongoDB"
      contains: "verifyWebhook"
  key_links:
    - from: "src/app/api/webhooks/clerk/route.ts"
      to: "src/lib/models/User"
      via: "User.create"
      pattern: "User\\.create"
    - from: "src/middleware.ts"
      to: "ruta pública /api/webhooks/clerk"
      via: "createRouteMatcher"
      pattern: "api/webhooks/clerk"

user_setup:
  - service: mongodb-atlas
    why: "Persistencia de OCs y usuarios"
    env_vars:
      - name: MONGODB_URI
        source: "MongoDB Atlas Dashboard → Database → Connect → Drivers → connection string"
  - service: clerk-webhook
    why: "Sincronización de usuarios al crear cuenta"
    env_vars:
      - name: CLERK_WEBHOOK_SIGNING_SECRET
        source: "Clerk Dashboard → Webhooks → crear endpoint apuntando a /api/webhooks/clerk → Signing Secret"
    dashboard_config:
      - task: "Crear webhook endpoint en Clerk"
        location: "Clerk Dashboard → Webhooks → Add Endpoint → URL: https://{dominio}/api/webhooks/clerk → Events: user.created"
---

<objective>
Instalar Mongoose, crear el singleton de conexión, los modelos OC y User, el Route Handler del webhook de Clerk, y actualizar el middleware para que el webhook sea accesible públicamente.

Purpose: Toda la capa de persistencia de Phase 5 depende de estos artefactos. Plans 02 y 03 importan directamente desde aquí.
Output: `src/lib/mongodb.ts`, `src/lib/models/OC.ts`, `src/lib/models/User.ts`, `src/app/api/webhooks/clerk/route.ts`, middleware actualizado, next.config.ts con serverExternalPackages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-backend-core/05-CONTEXT.md
@.planning/phases/05-backend-core/05-RESEARCH.md
@.planning/phases/05-backend-core/05-PATTERNS.md
</context>

<interfaces>
<!-- Tipos existentes que el modelo OC debe ser compatible con -->

De src/lib/wizard-types.ts:
```typescript
export interface ProductRow {
  id: string
  producto: string
  descripcion: string
  cantidad: string
  valorUSD: string
}

export interface InfoGeneralState {
  referenciaOC: string
  estado: EstadoOC
  proveedor: string
  emailsProveedor: string[]
  despacho: string
  fechaDespacho: string
  emailsDespachante: string[]
  paisOrigen: string
  fechaOC: string
  llegadaEstimada: string
  fechaPago: string
  tipoCambio: string
  divisa: 'ARS/USD' | 'ARS/EUR'
  notas: string
}

export interface GastosDespacho { sim: string; derechos: string; otros: string; tasaEstadistica?: string }
export interface GastosDespachante { terminal: string; fleteInternacional: string; fleteInterno: string; senasa: string; despachante: string; gastosOperativos?: string; gastosBancarios?: string }
export interface GastosAdicionales { depositoFiscal: string; digitalizacion: string; estanciaCamion: string }
export interface Impuestos { iva: string; ivaAd: string; iibb: string; iigg: string }
export interface OtroGastoRow { id: string; descripcion: string; monto: string; divisa: 'ARS' | 'USD' }
```

De src/lib/mock-ocs.ts (interface OCDetalle — referencia completa de campos):
```typescript
export type EstadoOC = 'borrador' | 'en_proceso' | 'en_transito' | 'en_aduana' | 'entregada' | 'cancelada'

export interface OCDetalle {
  referenciaOC: string
  emailsProveedor: string[]
  emailsDespachante: string[]
  despacho: string; paisOrigen: string; fechaOC: string; llegadaEstimada: string
  tipoCambio: string; divisa: 'ARS/USD' | 'ARS/EUR'; notas: string
  productos: ProductRow[]
  gastosDespacho: GastosDespacho; gastosDespachante: GastosDespachante
  gastosAdicionales: GastosAdicionales; impuestos: Impuestos
  otrosGastos: OtroGastoRow[]
  documentos: { facturaProveedor: string|null; facturaDespachante: string|null; conocimientoEmbarque: string|null; certificadoOrigen: string|null; otro: string|null }
}
```

De src/middleware.ts (estructura actual del createRouteMatcher):
```typescript
const isPublicRoute = createRouteMatcher(['/sign-in(.*)', '/sign-up(.*)'])
// Agregar '/api/webhooks/clerk' a este array
```
</interfaces>

<tasks>

<task type="auto">
  <name>Tarea 1: Instalar Mongoose y configurar next.config.ts + mongodb.ts + modelos</name>
  <read_first>
    - package.json (verificar que mongoose NO está instalado aún)
    - next.config.ts (agregar serverExternalPackages)
    - src/lib/wizard-types.ts (interfaces a mapear en el schema de OC)
    - src/lib/mock-ocs.ts (OCDetalle — campo documentos incluye certificadoAnalisis per commit 1b3d6ba)
  </read_first>
  <files>
    package.json,
    next.config.ts,
    src/lib/mongodb.ts,
    src/lib/models/OC.ts,
    src/lib/models/User.ts
  </files>
  <action>
**1. Instalar Mongoose:**
```bash
npm install mongoose@9.6.3
```

**2. Actualizar next.config.ts** — agregar serverExternalPackages para que Mongoose no sea bundleado por Webpack:
```typescript
import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  serverExternalPackages: ['mongoose'],
}

export default nextConfig
```

**3. Crear src/lib/mongodb.ts** — singleton mandatorio de CLAUDE.md:
```typescript
import mongoose from 'mongoose'

declare global {
  var mongooseCache: {
    conn: typeof mongoose | null
    promise: Promise<typeof mongoose> | null
  }
}

const MONGODB_URI = process.env.MONGODB_URI!

if (!global.mongooseCache) {
  global.mongooseCache = { conn: null, promise: null }
}

export async function connectDB() {
  if (global.mongooseCache.conn) return global.mongooseCache.conn
  if (!global.mongooseCache.promise) {
    global.mongooseCache.promise = mongoose.connect(MONGODB_URI, {
      maxPoolSize: 5,
      bufferCommands: false,
    })
  }
  global.mongooseCache.conn = await global.mongooseCache.promise
  return global.mongooseCache.conn
}
```

**4. Crear src/lib/models/User.ts:**
```typescript
import mongoose, { Schema } from 'mongoose'

const UserSchema = new Schema(
  {
    clerkId: { type: String, required: true, unique: true, index: true },
    email: { type: String, required: true, lowercase: true },
    rol: {
      type: String,
      enum: ['importador', 'proveedor', 'despachante'],
      required: true,
    },
  },
  { timestamps: true }
)

export const User = mongoose.models.User ?? mongoose.model('User', UserSchema)
```

**5. Crear src/lib/models/OC.ts** — schema completo con subdocumentos embebidos (valores monetarios como Number en centavos, excepto donde se indique string para fechas y referencias):

```typescript
import mongoose, { Schema } from 'mongoose'

const ProductRowSchema = new Schema(
  {
    producto: { type: String, required: true },
    descripcion: { type: String, default: '' },
    cantidad: { type: Number, required: true },
    valorUSD: { type: Number, required: true },
  },
  { _id: false }
)

const OtroGastoRowSchema = new Schema(
  {
    descripcion: { type: String, default: '' },
    monto: { type: Number, default: 0 },
    divisa: { type: String, enum: ['ARS', 'USD'], default: 'ARS' },
  },
  { _id: false }
)

const OCSchema = new Schema(
  {
    importadorId: { type: String, required: true, index: true },
    referenciaOC: { type: String, required: true },
    estado: {
      type: String,
      enum: ['borrador', 'en_proceso', 'en_transito', 'en_aduana', 'entregada', 'cancelada'],
      required: true,
      default: 'borrador',
    },
    proveedor: { type: String, default: '' },
    emailsProveedor: { type: [String], default: [] },
    emailsDespachante: { type: [String], default: [] },
    despacho: { type: String, default: '' },
    fechaDespacho: { type: String, default: '' },
    paisOrigen: { type: String, default: '' },
    fechaOC: { type: String, default: '' },
    llegadaEstimada: { type: String, default: '' },
    fechaPago: { type: String, default: '' },
    tipoCambio: { type: Number, default: 0 },
    divisa: { type: String, enum: ['ARS/USD', 'ARS/EUR'], default: 'ARS/USD' },
    notas: { type: String, default: '' },
    productos: { type: [ProductRowSchema], default: [] },
    gastosDespacho: {
      sim: { type: Number, default: 0 },
      derechos: { type: Number, default: 0 },
      tasaEstadistica: { type: Number, default: 0 },
      otros: { type: Number, default: 0 },
    },
    gastosDespachante: {
      terminal: { type: Number, default: 0 },
      fleteInternacional: { type: Number, default: 0 },
      fleteInterno: { type: Number, default: 0 },
      senasa: { type: Number, default: 0 },
      despachante: { type: Number, default: 0 },
      gastosOperativos: { type: Number, default: 0 },
      gastosBancarios: { type: Number, default: 0 },
    },
    gastosAdicionales: {
      depositoFiscal: { type: Number, default: 0 },
      digitalizacion: { type: Number, default: 0 },
      estanciaCamion: { type: Number, default: 0 },
    },
    impuestos: {
      iva: { type: Number, default: 0 },
      ivaAd: { type: Number, default: 0 },
      iibb: { type: Number, default: 0 },
      iigg: { type: Number, default: 0 },
    },
    otrosGastos: { type: [OtroGastoRowSchema], default: [] },
    otrosImpuestos: { type: [OtroGastoRowSchema], default: [] },
    documentos: {
      facturaProveedor: { type: String, default: null },
      facturaDespachante: { type: String, default: null },
      conocimientoEmbarque: { type: String, default: null },
      certificadoOrigen: { type: String, default: null },
      certificadoAnalisis: { type: String, default: null },
      otro: { type: String, default: null },
    },
  },
  { timestamps: true }
)

OCSchema.index({ importadorId: 1, referenciaOC: 1 }, { unique: true })

export const OC = mongoose.models.OC ?? mongoose.model('OC', OCSchema)
```

Notar que `certificadoAnalisis` está incluido en `documentos` (agregado en commit 1b3d6ba de Phase 4).
  </action>
  <verify>
    <automated>cd /mnt/c/Users/Equipo/Documents/DrivaOC && grep -c "mongoose" package.json</automated>
  </verify>
  <acceptance_criteria>
    - package.json contiene `"mongoose"` en dependencies
    - next.config.ts contiene `serverExternalPackages: ['mongoose']`
    - src/lib/mongodb.ts contiene exactamente `global.mongooseCache` y `maxPoolSize: 5` y `bufferCommands: false`
    - src/lib/models/OC.ts contiene `mongoose.models.OC ?? mongoose.model('OC', OCSchema)`
    - src/lib/models/OC.ts contiene `OCSchema.index({ importadorId: 1, referenciaOC: 1 }, { unique: true })`
    - src/lib/models/OC.ts contiene `certificadoAnalisis` en el bloque `documentos`
    - src/lib/models/User.ts contiene `mongoose.models.User ?? mongoose.model('User', UserSchema)`
    - `npx tsc --noEmit` no retorna errores relacionados con los archivos nuevos
  </acceptance_criteria>
  <done>Mongoose instalado, singleton y modelos creados, TypeScript strict sin errores en los 3 archivos nuevos.</done>
</task>

<task type="auto">
  <name>Tarea 2: Webhook Clerk y middleware público</name>
  <read_first>
    - src/middleware.ts (ver createRouteMatcher actual — línea 4 tiene isPublicRoute con sign-in y sign-up)
    - .env.example (agregar MONGODB_URI y CLERK_WEBHOOK_SIGNING_SECRET)
  </read_first>
  <files>
    src/middleware.ts,
    src/app/api/webhooks/clerk/route.ts,
    .env.example
  </files>
  <action>
**1. Actualizar src/middleware.ts** — agregar `/api/webhooks/clerk` como ruta pública (el webhook llega sin cookie de sesión; la firma se verifica con CLERK_WEBHOOK_SIGNING_SECRET, no con JWT):

Cambiar la línea 4 de:
```typescript
const isPublicRoute  = createRouteMatcher(['/sign-in(.*)', '/sign-up(.*)'])
```
a:
```typescript
const isPublicRoute  = createRouteMatcher(['/sign-in(.*)', '/sign-up(.*)', '/api/webhooks/clerk'])
```

No cambiar ninguna otra línea del middleware.

**2. Crear src/app/api/webhooks/clerk/route.ts** — Route Handler que verifica la firma con verifyWebhook de Clerk v7 y crea el usuario en MongoDB:

```typescript
export const runtime = 'nodejs'

import { verifyWebhook } from '@clerk/nextjs/webhooks'
import { NextRequest } from 'next/server'
import { connectDB } from '@/lib/mongodb'
import { User } from '@/lib/models/User'

export async function POST(req: NextRequest) {
  try {
    const evt = await verifyWebhook(req)

    if (evt.type === 'user.created') {
      const { id, email_addresses, public_metadata } = evt.data
      const email = email_addresses[0]?.email_address ?? ''
      const rol = (public_metadata as { role?: string }).role ?? 'importador'

      await connectDB()
      await User.create({
        clerkId: id,
        email: email.toLowerCase(),
        rol,
      })
    }

    return new Response('OK', { status: 200 })
  } catch (err) {
    console.error('Webhook error:', err)
    return new Response('Error verifying webhook', { status: 400 })
  }
}
```

**3. Actualizar .env.example** — agregar las dos env vars nuevas de esta fase después de las existentes:
```
MONGODB_URI=mongodb+srv://<usuario>:<password>@cluster0.xxxxx.mongodb.net/drivaoc?retryWrites=true&w=majority
CLERK_WEBHOOK_SIGNING_SECRET=whsec_xxxxxxxxxxxxxxxxxxxx
```
  </action>
  <verify>
    <automated>grep -c "api/webhooks/clerk" /mnt/c/Users/Equipo/Documents/DrivaOC/src/middleware.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/middleware.ts contiene `/api/webhooks/clerk` dentro del array de createRouteMatcher de isPublicRoute
    - src/app/api/webhooks/clerk/route.ts contiene `export const runtime = 'nodejs'` en la primera línea exportada
    - src/app/api/webhooks/clerk/route.ts contiene `verifyWebhook` importado de `@clerk/nextjs/webhooks`
    - src/app/api/webhooks/clerk/route.ts contiene `User.create` dentro del bloque `evt.type === 'user.created'`
    - src/app/api/webhooks/clerk/route.ts contiene `await connectDB()` antes de `User.create`
    - .env.example contiene `MONGODB_URI=`
    - .env.example contiene `CLERK_WEBHOOK_SIGNING_SECRET=`
    - `npx tsc --noEmit` no retorna errores en route.ts
  </acceptance_criteria>
  <done>Webhook accesible públicamente, verifica firma, crea User en MongoDB. Middleware no bloquea la ruta del webhook.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → /api/webhooks/clerk | Peticiones externas de Clerk llegan sin sesión de usuario |
| Server Action → MongoDB | Queries ejecutadas con userId del JWT de Clerk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Spoofing | POST /api/webhooks/clerk | mitigate | `verifyWebhook(req)` de `@clerk/nextjs/webhooks` usa HMAC-SHA256 con timing-safe comparison contra `CLERK_WEBHOOK_SIGNING_SECRET`; retorna 400 si falla |
| T-05-02 | Information Disclosure | src/lib/models/User.ts | accept | El modelo solo almacena clerkId, email y rol — sin datos sensibles adicionales; bajo riesgo |
| T-05-03 | Tampering | OCSchema índice único | mitigate | `OCSchema.index({ importadorId: 1, referenciaOC: 1 }, { unique: true })` — MongoDB rechaza duplicados a nivel de índice, incluso ante race conditions |
</threat_model>

<verification>
Después de ejecutar este plan:

1. `npm run build` sin errores de TypeScript ni de Webpack relacionados con Mongoose
2. En development: `npm run dev` inicia sin warnings de "Cannot overwrite model once compiled"
3. Con `.env.local` configurado con `MONGODB_URI` real: el servidor arranca y `connectDB()` resuelve sin errores
4. Con `CLERK_WEBHOOK_SIGNING_SECRET` configurado: enviar test event desde Clerk Dashboard → Webhooks → "Send test event (user.created)" retorna 200 y crea documento en colección `users` de MongoDB Atlas
5. Verificar en Atlas: colección `users` tiene documento con `clerkId`, `email` (lowercase), `rol`
</verification>

<success_criteria>
- `src/lib/mongodb.ts` exporta `connectDB` con global.mongooseCache, maxPoolSize 5, bufferCommands false
- `src/lib/models/OC.ts` exporta `OC` con guard `mongoose.models.OC ??` y índice compuesto único (importadorId + referenciaOC)
- `src/lib/models/User.ts` exporta `User` con guard `mongoose.models.User ??`
- POST /api/webhooks/clerk retorna 200 para eventos válidos de Clerk y 400 para peticiones sin firma válida
- El middleware permite el paso de peticiones a /api/webhooks/clerk sin requerir autenticación
</success_criteria>

<output>
Después de completar este plan, crear `.planning/phases/05-backend-core/05-01-SUMMARY.md` con:
- Archivos creados/modificados
- Decisiones de implementación tomadas
- Cualquier divergencia del plan
- Estado: COMPLETE
</output>
