---
phase: 05-backend-core
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/actions/oc.ts
  - src/app/(importador)/importador/dashboard/page.tsx
  - src/app/(proveedor)/proveedor/dashboard/page.tsx
  - src/app/(despachante)/despachante/dashboard/page.tsx
  - src/components/dashboard/DashboardClient.tsx
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - OC2-09

must_haves:
  truths:
    - "getOCs retorna OCs reales de MongoDB scoped por rol (importador ve todas las suyas; proveedor/despachante ven solo las que tienen su email, sin borradores)"
    - "deleteOC borra el documento de MongoDB; la OC desaparece del dashboard tras eliminar"
    - "Las stats del dashboard (totales, enTransito, enAduana, entregadas) se calculan desde MongoDB con aggregation"
    - "Los tres dashboards son Server Components async que pasan datos a DashboardClient"
    - "DashboardClient mantiene los filtros de búsqueda con useState (sin roundtrip al servidor al filtrar)"
  artifacts:
    - path: "src/actions/oc.ts"
      provides: "createOC, updateOC, deleteOC, getOCs, getOCById"
      exports: ["createOC", "updateOC", "deleteOC", "getOCs", "getOCById"]
    - path: "src/components/dashboard/DashboardClient.tsx"
      provides: "Client Component con filtros + StatCards + OCTable"
      contains: "'use client'"
    - path: "src/app/(importador)/importador/dashboard/page.tsx"
      provides: "Server Component async que llama getOCs"
      contains: "async function ImportadorDashboardPage"
  key_links:
    - from: "src/app/(importador)/importador/dashboard/page.tsx"
      to: "src/actions/oc"
      via: "getOCs()"
      pattern: "getOCs"
    - from: "src/actions/oc.ts"
      to: "src/lib/models/OC"
      via: "OC.find + OC.aggregate"
      pattern: "OC\\.find|OC\\.aggregate"
    - from: "src/actions/oc.ts"
      to: "src/lib/mongodb"
      via: "connectDB()"
      pattern: "await connectDB"
---

<objective>
Crear todos los Server Actions de OC (`createOC`, `updateOC`, `deleteOC`, `getOCs`, `getOCById`) y reemplazar los tres dashboards mock por Server Components que fetchean datos reales de MongoDB.

Purpose: Este plan entrega el backend de OCs completo y el primer flujo end-to-end visible: el dashboard muestra datos reales de MongoDB.
Output: `src/actions/oc.ts` con 5 funciones, `src/components/dashboard/DashboardClient.tsx` nuevo, 3 dashboard pages convertidas a Server Components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-backend-core/05-CONTEXT.md
@.planning/phases/05-backend-core/05-RESEARCH.md
@.planning/phases/05-backend-core/05-PATTERNS.md
@.planning/phases/05-backend-core/05-01-SUMMARY.md
</context>

<interfaces>
<!-- Tipos disponibles tras Plan 01 -->

De src/lib/models/OC.ts (creado en Plan 01):
```typescript
export const OC  // mongoose.Model — OC.find(), OC.findById(), OC.create(), OC.findByIdAndUpdate(), OC.findByIdAndDelete(), OC.aggregate()
```

De src/lib/models/User.ts (creado en Plan 01):
```typescript
export const User  // mongoose.Model — User.findOne({ clerkId: string })
```

De src/lib/mongodb.ts (creado en Plan 01):
```typescript
export async function connectDB(): Promise<typeof mongoose>
```

De src/lib/mock-ocs.ts (interfaz OC que consume OCTable — mantener compatibilidad):
```typescript
export interface OC {
  id: string          // En MongoDB es _id.toString()
  numeroOC: string    // Mapear a referenciaOC
  proveedor: string
  emailProveedor: string    // Primer email de emailsProveedor
  despachante: string       // Campo despacho del schema
  emailDespachante: string  // Primer email de emailsDespachante
  numeroDespacho: string
  estado: EstadoOC
  fecha: string             // Mapear a fechaOC o createdAt
}
```

De src/components/dashboard/ (ya implementados, sin cambios):
```typescript
// StatCard — props: { icon: LucideIcon, value: number, label: string }
// OCTable — props: { ocs: OC[], rol: 'importador'|'proveedor'|'despachante', hasFilters: boolean }
// FilterBar — props: { rol: ..., onEstado: ..., onSearchProveedor?: ..., onSearchDespachante?: ... }
```

De src/app/(auth)/onboarding/_actions.ts (único Server Action existente — patrón a replicar):
```typescript
'use server'
import { auth, clerkClient } from '@clerk/nextjs/server'

export async function completeOnboarding(role: Role): Promise<{ success: true } | { error: string }> {
  const { userId } = await auth()
  if (!userId) return { error: 'No autenticado' }
  // ...
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Tarea 1: Server Actions — createOC, updateOC, deleteOC, getOCs, getOCById</name>
  <read_first>
    - src/lib/models/OC.ts (schema creado en Plan 01 — ver todos los campos y tipos Number para centavos)
    - src/lib/models/User.ts (para lookup de email en proveedor/despachante)
    - src/lib/mongodb.ts (connectDB)
    - src/app/(auth)/onboarding/_actions.ts (patrón de Server Action con auth — analog exacto)
    - src/lib/mock-ocs.ts (interfaz OC que consume OCTable — para mapear _id y campos al serializar)
    - src/lib/wizard-types.ts (Step1Data, InfoGeneralState, GastosDespacho, etc.)
  </read_first>
  <files>src/actions/oc.ts</files>
  <action>
Crear `src/actions/oc.ts` con 5 funciones. El archivo debe comenzar con:

```typescript
'use server'
export const runtime = 'nodejs'
```

**Función helper toCentavos** (conversión de strings del wizard a enteros para MongoDB):
```typescript
function toCentavos(val: string | undefined): number {
  if (!val || val.trim() === '') return 0
  return Math.round(parseFloat(val) * 100)
}
```

**Función helper fromCentavos** (lectura de MongoDB de vuelta a strings para el wizard):
```typescript
function fromCentavos(val: number | undefined): string {
  if (val === undefined || val === null || val === 0) return ''
  return (val / 100).toString()
}
```

**Función helper serializeOC** (mapea documento Mongoose a interfaz OC compatible con OCTable + OCDetalle):

El resultado debe ser un objeto serializable (sin ObjectId, sin instancias Mongoose). Debe satisfacer la interfaz `OC` de `mock-ocs.ts` para que `OCTable` funcione sin cambios:
```typescript
function serializeOC(doc: Record<string, unknown>): SerializedOC {
  const d = doc as {
    _id: { toString(): string }
    referenciaOC: string
    proveedor: string
    emailsProveedor: string[]
    emailsDespachante: string[]
    despacho: string
    estado: string
    fechaOC: string
    createdAt: unknown
    tipoCambio: number
    divisa: string
    notas: string
    paisOrigen: string
    llegadaEstimada: string
    fechaPago: string
    fechaDespacho: string
    productos: Array<{ producto: string; descripcion: string; cantidad: number; valorUSD: number }>
    gastosDespacho: Record<string, number>
    gastosDespachante: Record<string, number>
    gastosAdicionales: Record<string, number>
    impuestos: Record<string, number>
    otrosGastos: Array<{ descripcion: string; monto: number; divisa: string }>
    otrosImpuestos: Array<{ descripcion: string; monto: number; divisa: string }>
    documentos: Record<string, string | null>
    importadorId: string
  }
  return {
    // Campos para interfaz OC (OCTable)
    id: d._id.toString(),
    numeroOC: d.referenciaOC,
    proveedor: d.proveedor ?? '',
    emailProveedor: d.emailsProveedor?.[0] ?? '',
    despachante: d.despacho ?? '',
    emailDespachante: d.emailsDespachante?.[0] ?? '',
    numeroDespacho: '',
    estado: d.estado,
    fecha: d.fechaOC ?? (d.createdAt ? new Date(d.createdAt as string).toISOString().split('T')[0] : ''),
    // Campos extendidos para OCDetalle
    referenciaOC: d.referenciaOC,
    emailsProveedor: d.emailsProveedor ?? [],
    emailsDespachante: d.emailsDespachante ?? [],
    paisOrigen: d.paisOrigen ?? '',
    llegadaEstimada: d.llegadaEstimada ?? '',
    tipoCambio: fromCentavos(d.tipoCambio),
    divisa: (d.divisa ?? 'ARS/USD') as 'ARS/USD' | 'ARS/EUR',
    notas: d.notas ?? '',
    fechaDespacho: d.fechaDespacho ?? '',
    fechaPago: d.fechaPago ?? '',
    importadorId: d.importadorId,
    productos: (d.productos ?? []).map(p => ({
      id: crypto.randomUUID(),
      producto: p.producto,
      descripcion: p.descripcion ?? '',
      cantidad: fromCentavos(p.cantidad),   // cantidad se guarda en centavos (x100)
      valorUSD: fromCentavos(p.valorUSD),
    })),
    gastosDespacho: {
      sim: fromCentavos(d.gastosDespacho?.sim),
      derechos: fromCentavos(d.gastosDespacho?.derechos),
      tasaEstadistica: fromCentavos(d.gastosDespacho?.tasaEstadistica),
      otros: fromCentavos(d.gastosDespacho?.otros),
    },
    gastosDespachante: {
      terminal: fromCentavos(d.gastosDespachante?.terminal),
      fleteInternacional: fromCentavos(d.gastosDespachante?.fleteInternacional),
      fleteInterno: fromCentavos(d.gastosDespachante?.fleteInterno),
      senasa: fromCentavos(d.gastosDespachante?.senasa),
      despachante: fromCentavos(d.gastosDespachante?.despachante),
      gastosOperativos: fromCentavos(d.gastosDespachante?.gastosOperativos),
      gastosBancarios: fromCentavos(d.gastosDespachante?.gastosBancarios),
    },
    gastosAdicionales: {
      depositoFiscal: fromCentavos(d.gastosAdicionales?.depositoFiscal),
      digitalizacion: fromCentavos(d.gastosAdicionales?.digitalizacion),
      estanciaCamion: fromCentavos(d.gastosAdicionales?.estanciaCamion),
    },
    impuestos: {
      iva: fromCentavos(d.impuestos?.iva),
      ivaAd: fromCentavos(d.impuestos?.ivaAd),
      iibb: fromCentavos(d.impuestos?.iibb),
      iigg: fromCentavos(d.impuestos?.iigg),
    },
    otrosGastos: (d.otrosGastos ?? []).map(g => ({
      id: crypto.randomUUID(),
      descripcion: g.descripcion,
      monto: fromCentavos(g.monto),
      divisa: g.divisa as 'ARS' | 'USD',
    })),
    otrosImpuestos: (d.otrosImpuestos ?? []).map(g => ({
      id: crypto.randomUUID(),
      descripcion: g.descripcion,
      monto: fromCentavos(g.monto),
      divisa: g.divisa as 'ARS' | 'USD',
    })),
    documentos: {
      facturaProveedor: d.documentos?.facturaProveedor ?? null,
      facturaDespachante: d.documentos?.facturaDespachante ?? null,
      conocimientoEmbarque: d.documentos?.conocimientoEmbarque ?? null,
      certificadoOrigen: d.documentos?.certificadoOrigen ?? null,
      certificadoAnalisis: d.documentos?.certificadoAnalisis ?? null,
      otro: d.documentos?.otro ?? null,
    },
  }
}
```

Nota sobre `cantidad` en productos: la cantidad es una cantidad de unidades (ej: 10), no un valor monetario. Guardar como `Math.round(parseFloat(val) * 100)` es incorrecto para cantidades enteras. Usar en cambio `Math.round(parseFloat(val || '0'))` (sin multiplicar por 100) para `cantidad` y `Math.round(parseFloat(val || '0') * 100)` solo para valores monetarios (valorUSD, todos los gastos). Ajustar `fromCentavos` para cantidad retornando `(val).toString()` directamente.

**Definir tipo SerializedOC** al inicio del archivo para TypeScript strict:
```typescript
import type { EstadoOC, OCDetalle } from '@/lib/mock-ocs'
import type { InfoGeneralState, ProductRow, GastosDespacho, GastosDespachante, GastosAdicionales, Impuestos, OtroGastoRow } from '@/lib/wizard-types'

type SerializedOC = OCDetalle & {
  id: string
  numeroOC: string
  emailProveedor: string
  despachante: string
  emailDespachante: string
  numeroDespacho: string
  fecha: string
  importadorId: string
  otrosImpuestos: OtroGastoRow[]
}

type StatsResult = {
  totales: number
  enTransito: number
  enAduana: number
  entregadas: number
}

function defaultStats(): StatsResult {
  return { totales: 0, enTransito: 0, enAduana: 0, entregadas: 0 }
}
```

**createOC** (D-02: validar unicidad; D-03: guardar borrador y retornar _id):
```typescript
export async function createOC(data: {
  info: InfoGeneralState
  productos: ProductRow[]
}): Promise<{ data: { id: string } } | { error: string }> {
  const { userId } = await auth()
  if (!userId) return { error: 'No autorizado' }

  await connectDB()

  const existente = await OC.findOne({
    importadorId: userId,
    referenciaOC: data.info.referenciaOC.trim(),
  })
  if (existente) return { error: 'Ya existe una OC con esta referencia' }

  const emailsProveedor = data.info.emailsProveedor
    .map(e => e.toLowerCase().trim())
    .filter(e => e !== '')
  const emailsDespachante = data.info.emailsDespachante
    .map(e => e.toLowerCase().trim())
    .filter(e => e !== '')

  try {
    const oc = await OC.create({
      importadorId: userId,
      referenciaOC: data.info.referenciaOC.trim(),
      estado: 'borrador',
      proveedor: data.info.proveedor,
      emailsProveedor,
      emailsDespachante,
      despacho: data.info.despacho,
      fechaDespacho: data.info.fechaDespacho,
      paisOrigen: data.info.paisOrigen,
      fechaOC: data.info.fechaOC,
      llegadaEstimada: data.info.llegadaEstimada,
      fechaPago: data.info.fechaPago,
      tipoCambio: toCentavos(data.info.tipoCambio),
      divisa: data.info.divisa,
      notas: data.info.notas,
      productos: data.productos.map(p => ({
        producto: p.producto,
        descripcion: p.descripcion,
        cantidad: Math.round(parseFloat(p.cantidad || '0')),
        valorUSD: toCentavos(p.valorUSD),
      })),
    })
    return { data: { id: oc._id.toString() } }
  } catch (err: unknown) {
    if (
      err instanceof Error &&
      (err.message.includes('duplicate key') || err.message.includes('E11000'))
    ) {
      return { error: 'Ya existe una OC con esta referencia' }
    }
    return { error: 'Error al guardar la OC' }
  }
}
```

**getOCById** (D-05: proveedor/despachante también pueden leer si tienen el email; D-09: no exponer borradores a proveedor/despachante):
```typescript
export async function getOCById(
  id: string
): Promise<{ data: { oc: SerializedOC } } | { error: string }> {
  const { userId, sessionClaims } = await auth()
  if (!userId) return { error: 'No autorizado' }
  const rol = (sessionClaims?.metadata as { role?: string })?.role

  await connectDB()

  const doc = await OC.findById(id).lean()
  if (!doc) return { error: 'OC no encontrada' }

  const oc = doc as Record<string, unknown> & { importadorId: string; estado: string; emailsProveedor: string[]; emailsDespachante: string[] }

  if (rol === 'importador') {
    if (oc.importadorId !== userId) return { error: 'Sin acceso' }
  } else if (rol === 'proveedor') {
    const clerkUser = await (await clerkClient()).users.getUser(userId)
    const email = clerkUser.emailAddresses[0]?.emailAddress?.toLowerCase() ?? ''
    if (oc.estado === 'borrador') return { error: 'Sin acceso' }
    if (!oc.emailsProveedor.includes(email)) return { error: 'Sin acceso' }
  } else if (rol === 'despachante') {
    const clerkUser = await (await clerkClient()).users.getUser(userId)
    const email = clerkUser.emailAddresses[0]?.emailAddress?.toLowerCase() ?? ''
    if (oc.estado === 'borrador') return { error: 'Sin acceso' }
    if (!oc.emailsDespachante.includes(email)) return { error: 'Sin acceso' }
  }

  return { data: { oc: serializeOC(doc as Record<string, unknown>) } }
}
```

**updateOC** (D-04: actualiza gastos + estado; IDOR guard):
```typescript
export async function updateOC(
  id: string,
  data: {
    gastosDespacho: GastosDespacho
    gastosDespachante: GastosDespachante
    gastosAdicionales: GastosAdicionales
    impuestos: Impuestos
    otrosGastos: OtroGastoRow[]
    otrosImpuestos: OtroGastoRow[]
    estado: EstadoOC
  }
): Promise<{ data: { id: string } } | { error: string }> {
  const { userId } = await auth()
  if (!userId) return { error: 'No autorizado' }

  await connectDB()

  const existing = await OC.findById(id).lean() as { importadorId?: string } | null
  if (!existing || existing.importadorId !== userId) return { error: 'Sin acceso' }

  await OC.findByIdAndUpdate(id, {
    gastosDespacho: {
      sim: toCentavos(data.gastosDespacho.sim),
      derechos: toCentavos(data.gastosDespacho.derechos),
      tasaEstadistica: toCentavos(data.gastosDespacho.tasaEstadistica),
      otros: toCentavos(data.gastosDespacho.otros),
    },
    gastosDespachante: {
      terminal: toCentavos(data.gastosDespachante.terminal),
      fleteInternacional: toCentavos(data.gastosDespachante.fleteInternacional),
      fleteInterno: toCentavos(data.gastosDespachante.fleteInterno),
      senasa: toCentavos(data.gastosDespachante.senasa),
      despachante: toCentavos(data.gastosDespachante.despachante),
      gastosOperativos: toCentavos(data.gastosDespachante.gastosOperativos),
      gastosBancarios: toCentavos(data.gastosDespachante.gastosBancarios),
    },
    gastosAdicionales: {
      depositoFiscal: toCentavos(data.gastosAdicionales.depositoFiscal),
      digitalizacion: toCentavos(data.gastosAdicionales.digitalizacion),
      estanciaCamion: toCentavos(data.gastosAdicionales.estanciaCamion),
    },
    impuestos: {
      iva: toCentavos(data.impuestos.iva),
      ivaAd: toCentavos(data.impuestos.ivaAd),
      iibb: toCentavos(data.impuestos.iibb),
      iigg: toCentavos(data.impuestos.iigg),
    },
    otrosGastos: data.otrosGastos.map(g => ({
      descripcion: g.descripcion,
      monto: toCentavos(g.monto),
      divisa: g.divisa,
    })),
    otrosImpuestos: data.otrosImpuestos.map(g => ({
      descripcion: g.descripcion,
      monto: toCentavos(g.monto),
      divisa: g.divisa,
    })),
    estado: data.estado,
  })

  return { data: { id } }
}
```

**deleteOC** (IDOR guard; DOC-02):
```typescript
export async function deleteOC(
  id: string
): Promise<{ data: { ok: true } } | { error: string }> {
  const { userId } = await auth()
  if (!userId) return { error: 'No autorizado' }

  await connectDB()

  const existing = await OC.findById(id).lean() as { importadorId?: string } | null
  if (!existing || existing.importadorId !== userId) return { error: 'Sin acceso' }

  await OC.findByIdAndDelete(id)
  return { data: { ok: true } }
}
```

**getOCs** (DOC-01: scoped por rol, stats desde aggregation, D-09: no borradores para proveedor/despachante):
```typescript
export async function getOCs(): Promise<
  { data: { ocs: SerializedOC[]; stats: StatsResult } } | { error: string }
> {
  const { userId, sessionClaims } = await auth()
  if (!userId) return { error: 'No autorizado' }
  const rol = (sessionClaims?.metadata as { role?: string })?.role

  await connectDB()

  let filter: Record<string, unknown> = {}

  if (rol === 'importador') {
    filter = { importadorId: userId }
  } else if (rol === 'proveedor' || rol === 'despachante') {
    const clerkUser = await (await clerkClient()).users.getUser(userId)
    const email = clerkUser.emailAddresses[0]?.emailAddress?.toLowerCase() ?? ''
    if (!email) return { data: { ocs: [], stats: defaultStats() } }
    filter = rol === 'proveedor'
      ? { emailsProveedor: email, estado: { $ne: 'borrador' } }
      : { emailsDespachante: email, estado: { $ne: 'borrador' } }
  } else {
    return { error: 'Rol no reconocido' }
  }

  const [docs, aggResult] = await Promise.all([
    OC.find(filter).sort({ createdAt: -1 }).lean(),
    OC.aggregate([
      { $match: filter },
      {
        $group: {
          _id: null,
          totales: { $sum: 1 },
          enTransito: { $sum: { $cond: [{ $eq: ['$estado', 'en_transito'] }, 1, 0] } },
          enAduana: { $sum: { $cond: [{ $eq: ['$estado', 'en_aduana'] }, 1, 0] } },
          entregadas: { $sum: { $cond: [{ $eq: ['$estado', 'entregada'] }, 1, 0] } },
        },
      },
    ]),
  ])

  const ocs = (docs as Record<string, unknown>[]).map(serializeOC)
  const stats: StatsResult = aggResult[0]
    ? {
        totales: aggResult[0].totales as number,
        enTransito: aggResult[0].enTransito as number,
        enAduana: aggResult[0].enAduana as number,
        entregadas: aggResult[0].entregadas as number,
      }
    : defaultStats()

  return { data: { ocs, stats } }
}
```

Agregar al inicio del archivo los imports necesarios:
```typescript
import { auth, clerkClient } from '@clerk/nextjs/server'
import { connectDB } from '@/lib/mongodb'
import { OC } from '@/lib/models/OC'
```
  </action>
  <verify>
    <automated>grep -c "export async function" /mnt/c/Users/Equipo/Documents/DrivaOC/src/actions/oc.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/actions/oc.ts comienza con `'use server'` en línea 1 y `export const runtime = 'nodejs'` en línea 2
    - src/actions/oc.ts exporta exactamente 5 funciones async: `createOC`, `getOCById`, `updateOC`, `deleteOC`, `getOCs`
    - src/actions/oc.ts contiene `await connectDB()` dentro de cada una de las 5 funciones
    - src/actions/oc.ts contiene `importadorId !== userId` (IDOR guard) en `updateOC` y `deleteOC`
    - src/actions/oc.ts contiene `estado: { $ne: 'borrador' }` en el filtro de proveedor y despachante dentro de `getOCs`
    - src/actions/oc.ts contiene `OC.aggregate` dentro de `getOCs`
    - src/actions/oc.ts contiene `email.toLowerCase()` en `createOC` para normalizar emails (D-07)
    - `npx tsc --noEmit` no retorna errores en src/actions/oc.ts
  </acceptance_criteria>
  <done>5 Server Actions implementados con auth guards, IDOR protection, conversión de centavos y scoping por rol.</done>
</task>

<task type="auto">
  <name>Tarea 2: Refactorizar los 3 dashboards a Server Components + crear DashboardClient</name>
  <read_first>
    - src/app/(importador)/importador/dashboard/page.tsx (Client Component actual — ver toda la estructura JSX para replicarla en DashboardClient)
    - src/app/(proveedor)/proveedor/dashboard/page.tsx (ídem — mismo patrón)
    - src/app/(despachante)/despachante/dashboard/page.tsx (ídem)
    - src/components/dashboard/StatCard.tsx (confirmar props: icon, value, label)
    - src/components/dashboard/OCTable.tsx (confirmar que acepta OC[] — interfaz de mock-ocs.ts)
    - src/components/dashboard/FilterBar.tsx (confirmar props y signature)
  </read_first>
  <files>
    src/components/dashboard/DashboardClient.tsx,
    src/app/(importador)/importador/dashboard/page.tsx,
    src/app/(proveedor)/proveedor/dashboard/page.tsx,
    src/app/(despachante)/despachante/dashboard/page.tsx
  </files>
  <action>
**Arquitectura destino** (RESEARCH.md Pitfall 6 — Opción A):
- Las page.tsx son `async function` Server Components sin `'use client'`
- Un nuevo `DashboardClient.tsx` es `'use client'` y maneja `useState` para filtros
- Los datos vienen como props desde page.tsx → DashboardClient (no hay fetch en el cliente)

**1. Crear src/components/dashboard/DashboardClient.tsx:**

Este componente reemplaza el cuerpo actual de los tres dashboard pages. Acepta `ocs`, `stats` y `rol` como props, y maneja los filtros con `useState` en el cliente (mismo comportamiento de Phase 2 pero con datos reales):

```typescript
'use client'

import { useState } from 'react'
import { FileText, Truck, Package, CheckCircle2 } from 'lucide-react'
import type { OC, EstadoOC } from '@/lib/mock-ocs'
import { StatCard } from '@/components/dashboard/StatCard'
import { OCTable } from '@/components/dashboard/OCTable'
import { FilterBar } from '@/components/dashboard/FilterBar'

interface DashboardStats {
  totales: number
  enTransito: number
  enAduana: number
  entregadas: number
}

interface DashboardClientProps {
  ocs: OC[]
  stats: DashboardStats
  rol: 'importador' | 'proveedor' | 'despachante'
  titulo: string
}

export function DashboardClient({ ocs, stats, rol, titulo }: DashboardClientProps) {
  const [searchProveedor, setSearchProveedor] = useState('')
  const [searchDespachante, setSearchDespachante] = useState('')
  const [estadoFiltro, setEstadoFiltro] = useState<EstadoOC | ''>('')

  const ocsFiltradas = ocs.filter((oc) => {
    const matchProveedor =
      rol === 'importador'
        ? oc.proveedor.toLowerCase().includes(searchProveedor.toLowerCase())
        : true
    const matchDespachante =
      rol === 'importador'
        ? oc.despachante.toLowerCase().includes(searchDespachante.toLowerCase())
        : true
    const matchEstado = estadoFiltro === '' || oc.estado === estadoFiltro
    return matchProveedor && matchDespachante && matchEstado
  })

  const hasFilters =
    searchProveedor !== '' || searchDespachante !== '' || estadoFiltro !== ''

  return (
    <div className="p-4 sm:p-6">
      <h1 className="text-xl font-bold text-titulares mb-6">{titulo}</h1>
      <div className="grid grid-cols-2 sm:grid-cols-4 gap-4 mb-8">
        <StatCard icon={FileText} value={stats.totales} label="OC Totales" />
        <StatCard icon={Truck} value={stats.enTransito} label="En tránsito" />
        <StatCard icon={Package} value={stats.enAduana} label="En aduana" />
        <StatCard icon={CheckCircle2} value={stats.entregadas} label="Entregadas" />
      </div>
      <FilterBar
        rol={rol}
        onEstado={setEstadoFiltro}
        onSearchProveedor={rol === 'importador' ? setSearchProveedor : undefined}
        onSearchDespachante={rol === 'importador' ? setSearchDespachante : undefined}
      />
      <OCTable ocs={ocsFiltradas} rol={rol} hasFilters={hasFilters} />
    </div>
  )
}
```

**2. Reemplazar src/app/(importador)/importador/dashboard/page.tsx** — eliminar `'use client'`, `useState`, y el import de MOCK_OCS. Convertir en Server Component async que llama `getOCs`:

```typescript
import { getOCs } from '@/actions/oc'
import { DashboardClient } from '@/components/dashboard/DashboardClient'

export default async function ImportadorDashboardPage() {
  const result = await getOCs()
  const { ocs, stats } = 'error' in result
    ? { ocs: [], stats: { totales: 0, enTransito: 0, enAduana: 0, entregadas: 0 } }
    : result.data
  return <DashboardClient ocs={ocs} stats={stats} rol="importador" titulo="Dashboard" />
}
```

**3. Reemplazar src/app/(proveedor)/proveedor/dashboard/page.tsx:**
```typescript
import { getOCs } from '@/actions/oc'
import { DashboardClient } from '@/components/dashboard/DashboardClient'

export default async function ProveedorDashboardPage() {
  const result = await getOCs()
  const { ocs, stats } = 'error' in result
    ? { ocs: [], stats: { totales: 0, enTransito: 0, enAduana: 0, entregadas: 0 } }
    : result.data
  return <DashboardClient ocs={ocs} stats={stats} rol="proveedor" titulo="Mis Órdenes" />
}
```

**4. Reemplazar src/app/(despachante)/despachante/dashboard/page.tsx:**
```typescript
import { getOCs } from '@/actions/oc'
import { DashboardClient } from '@/components/dashboard/DashboardClient'

export default async function DespachanteDashboardPage() {
  const result = await getOCs()
  const { ocs, stats } = 'error' in result
    ? { ocs: [], stats: { totales: 0, enTransito: 0, enAduana: 0, entregadas: 0 } }
    : result.data
  return <DashboardClient ocs={ocs} stats={stats} rol="despachante" titulo="Mis Órdenes" />
}
```

**Nota crítica:** Leer el archivo FilterBar.tsx antes de implementar `DashboardClient` para confirmar si `onSearchProveedor` y `onSearchDespachante` son opcionales o requeridos. Si son required, ajustar las props o pasar funciones vacías `() => {}` para proveedor/despachante.
  </action>
  <verify>
    <automated>grep -c "'use client'" /mnt/c/Users/Equipo/Documents/DrivaOC/src/app/\(importador\)/importador/dashboard/page.tsx 2>/dev/null || echo "0"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/dashboard/DashboardClient.tsx existe y contiene `'use client'` en línea 1
    - src/components/dashboard/DashboardClient.tsx contiene `useState` para `searchProveedor`, `searchDespachante`, `estadoFiltro`
    - src/app/(importador)/importador/dashboard/page.tsx NO contiene `'use client'`
    - src/app/(importador)/importador/dashboard/page.tsx contiene `async function ImportadorDashboardPage`
    - src/app/(importador)/importador/dashboard/page.tsx NO contiene `MOCK_OCS`
    - src/app/(importador)/importador/dashboard/page.tsx contiene `getOCs()`
    - src/app/(proveedor)/proveedor/dashboard/page.tsx NO contiene `'use client'` y contiene `getOCs()`
    - src/app/(despachante)/despachante/dashboard/page.tsx NO contiene `'use client'` y contiene `getOCs()`
    - `npx tsc --noEmit` no retorna errores en los 4 archivos
  </acceptance_criteria>
  <done>Los 3 dashboards son Server Components async. DashboardClient mantiene los filtros en el cliente. Los datos vienen de MongoDB vía getOCs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client Component → Server Action | Llamadas a createOC/updateOC/deleteOC desde el browser |
| Server Action → MongoDB | Queries con userId extraído del JWT de Clerk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Spoofing (IDOR) | updateOC / deleteOC | mitigate | `if (existing.importadorId !== userId) return { error: 'Sin acceso' }` — verificar propiedad antes de cualquier mutación |
| T-05-05 | Information Disclosure | getOCById para proveedor/despachante | mitigate | `if (oc.estado === 'borrador') return { error: 'Sin acceso' }` — D-09; verificación de email en array emailsProveedor/emailsDespachante |
| T-05-06 | Tampering (NoSQL injection) | getOCs filtro por email | mitigate | Email viene de Clerk JWT (no de input del usuario); queries Mongoose usan tipos, no string raw |
| T-05-07 | Spoofing | getOCs rol check | mitigate | `sessionClaims?.metadata?.role` viene del JWT firmado por Clerk; no aceptar rol de parámetros de request |
</threat_model>

<verification>
Después de ejecutar este plan:

1. `npm run build` sin errores TypeScript en src/actions/oc.ts y los dashboards
2. `npm run dev` → navegar a `/importador/dashboard` — si no hay OCs en MongoDB, el dashboard muestra EmptyState (sin crash)
3. Crear una OC desde el wizard (Plan 03 la conectará) o insertar manualmente en Atlas y verificar que aparece en el dashboard
4. Verificar en dashboard de importador: stats card "OC Totales" muestra el número correcto
5. Verificar que proveedor/despachante que NO están en ningún emailsProveedor/emailsDespachante ven dashboard vacío (no error 500)
</verification>

<success_criteria>
- src/actions/oc.ts exporta 5 funciones async con `'use server'` y `runtime = 'nodejs'`
- Cada función llama `await connectDB()` antes de cualquier operación Mongoose
- IDOR guard presente en updateOC y deleteOC
- Los 3 dashboards son Server Components async sin `'use client'`
- DashboardClient.tsx maneja filtros con useState
- `npm run build` sin errores TypeScript
</success_criteria>

<output>
Después de completar este plan, crear `.planning/phases/05-backend-core/05-02-SUMMARY.md` con:
- Archivos creados/modificados
- Decisiones de implementación tomadas (manejo de `cantidad` en productos, serialización OC)
- Cualquier divergencia del plan
- Estado: COMPLETE
</output>
