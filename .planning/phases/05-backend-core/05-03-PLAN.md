---
phase: 05-backend-core
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - src/components/wizard/Step1Form.tsx
  - src/components/wizard/Step2Form.tsx
  - src/components/wizard/WizardPage.tsx
  - src/app/(importador)/importador/oc/nueva/page.tsx
  - src/app/(importador)/importador/oc/[id]/page.tsx
  - src/app/(importador)/importador/oc/[id]/editar/EditWizardLoader.tsx
  - src/app/(proveedor)/proveedor/oc/[id]/page.tsx
  - src/app/(proveedor)/proveedor/oc/[id]/editar/EditWizardLoader.tsx
  - src/app/(despachante)/despachante/oc/[id]/page.tsx
  - src/app/(despachante)/despachante/oc/[id]/editar/EditWizardLoader.tsx
autonomous: true
requirements:
  - OC1-04
  - OC2-09
  - DOC-01
  - DOC-02

must_haves:
  truths:
    - "Step 1 llama createOC al hacer clic en 'Continuar a Paso 2'; el URL cambia a /importador/oc/[id-real]?step=2"
    - "Step 2 recibe ocData de MongoDB como prop desde page.tsx (no lee sessionStorage)"
    - "Al guardar en Step 2, updateOC persiste los gastos y el estado en MongoDB"
    - "EditWizardLoader es un Server Component async que fetchea la OC de MongoDB por ID"
    - "Las páginas de detalle de OC (importador, proveedor, despachante) muestran datos reales de MongoDB"
    - "sessionStorage.setItem('oc-step1-draft') no existe en ninguno de los archivos modificados"
  artifacts:
    - path: "src/components/wizard/Step1Form.tsx"
      provides: "Wizard Step 1 conectado a createOC"
      contains: "createOC"
    - path: "src/components/wizard/Step2Form.tsx"
      provides: "Wizard Step 2 conectado a updateOC, recibe props"
      contains: "updateOC"
    - path: "src/app/(importador)/importador/oc/[id]/editar/EditWizardLoader.tsx"
      provides: "Server Component async que fetchea OC de MongoDB"
      contains: "async function EditWizardLoader"
  key_links:
    - from: "src/components/wizard/Step1Form.tsx"
      to: "src/actions/oc"
      via: "createOC()"
      pattern: "createOC"
    - from: "src/components/wizard/Step2Form.tsx"
      to: "src/actions/oc"
      via: "updateOC()"
      pattern: "updateOC"
    - from: "src/app/(importador)/importador/oc/[id]/page.tsx"
      to: "src/actions/oc"
      via: "getOCById(id)"
      pattern: "getOCById"
---

<objective>
Conectar el wizard completo (Step 1 y Step 2) al backend, eliminar sessionStorage del flujo, convertir los EditWizardLoader a Server Components que leen de MongoDB, y reemplazar el mock en las páginas de detalle de OC.

Purpose: Cierra el loop completo: crear → persistir → ver. Al finalizar este plan, el sistema funciona end-to-end con datos reales sin ningún dato hardcodeado.
Output: Step1Form llama createOC, Step2Form llama updateOC, EditWizardLoader fetchea de DB, páginas de detalle leen MongoDB.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-backend-core/05-CONTEXT.md
@.planning/phases/05-backend-core/05-RESEARCH.md
@.planning/phases/05-backend-core/05-PATTERNS.md
@.planning/phases/05-backend-core/05-02-SUMMARY.md
</context>

<interfaces>
<!-- Contratos de los Server Actions creados en Plan 02 -->

De src/actions/oc.ts (creado en Plan 02):
```typescript
export async function createOC(data: {
  info: InfoGeneralState
  productos: ProductRow[]
}): Promise<{ data: { id: string } } | { error: string }>

export async function updateOC(
  id: string,
  data: {
    gastosDespacho: GastosDespacho
    gastosDespachante: GastosDespachante
    gastosAdicionales: GastosAdicionales
    impuestos: Impuestos
    otrosGastos: OtroGastoRow[]
    otrosImpuestos: OtroGastoRow[]
    estado: EstadoOC
  }
): Promise<{ data: { id: string } } | { error: string }>

export async function getOCById(
  id: string
): Promise<{ data: { oc: SerializedOC } } | { error: string }>

// SerializedOC extiende OCDetalle con campos: id, numeroOC, emailProveedor,
// despachante, emailDespachante, numeroDespacho, fecha, importadorId, otrosImpuestos
// (todos los campos de gastos como strings — resultado de fromCentavos)
```

De src/components/wizard/Step1Form.tsx (actual — a modificar):
```typescript
// Línea 157-162: handleContinuar actual
const handleContinuar = () => {
  setSubmitted(true)
  if (!isStep1Valid(info, productos)) return
  sessionStorage.setItem('oc-step1-draft', JSON.stringify({ info, productos }))
  router.push('/importador/oc/nueva?step=2')
}
// Línea 94-124: useEffect que lee sessionStorage — ELIMINAR
```

De src/components/wizard/Step2Form.tsx (actual — a modificar):
```typescript
// Props actuales: ninguna (export function Step2Form())
// Línea 85-93: useEffect que lee sessionStorage — ELIMINAR
// Línea 127-133: handleGuardar mock — REEMPLAZAR por updateOC
```

De src/components/wizard/WizardPage.tsx (actual — a extender):
```typescript
interface WizardPageProps {
  initialStep: string
  // AGREGAR: ocData? y ocId? para Step 2
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Tarea 1: Conectar Step1Form a createOC y Step2Form a updateOC (eliminar sessionStorage)</name>
  <read_first>
    - src/components/wizard/Step1Form.tsx (leer completo — ver handleContinuar línea 157-162 y useEffect sessionStorage líneas 94-124)
    - src/components/wizard/Step2Form.tsx (leer completo — ver useEffect sessionStorage líneas 85-93 y handleGuardar líneas 127-133, y todos los campos de estado que maneja)
    - src/components/wizard/WizardPage.tsx (ver props actuales — añadir ocData y ocId)
    - src/app/(importador)/importador/oc/nueva/page.tsx (ver cómo pasa initialStep a WizardPage)
    - src/app/(importador)/importador/oc/[id]/page.tsx (ver que ya fetchea params — detectar si searchParams.step existe para mostrar Step2Form)
  </read_first>
  <files>
    src/components/wizard/Step1Form.tsx,
    src/components/wizard/Step2Form.tsx,
    src/components/wizard/WizardPage.tsx,
    src/app/(importador)/importador/oc/nueva/page.tsx,
    src/app/(importador)/importador/oc/[id]/page.tsx
  </files>
  <action>
**Estrategia de rutas (D-03):**
- `/importador/oc/nueva` — Step 1 (sin ID de OC)
- `/importador/oc/[id]?step=2` — Step 2 (con ID real tras createOC)
- La page.tsx de `[id]` detecta `searchParams.step === '2'` y muestra Step2Form en lugar de OCDetailView

**1. Modificar src/components/wizard/Step1Form.tsx:**

a) Eliminar el `useEffect` completo que lee `sessionStorage.getItem('oc-step1-draft')` (líneas 94-124).

b) Agregar `useState<string | null>(null)` para serverError:
```typescript
const [serverError, setServerError] = useState<string | null>(null)
```

c) Agregar import de createOC al inicio:
```typescript
import { createOC } from '@/actions/oc'
```

d) Reemplazar `handleContinuar` (actualmente sincrónica) por versión async:
```typescript
const handleContinuar = async () => {
  setSubmitted(true)
  if (!isStep1Valid(info, productos)) return
  setServerError(null)
  const result = await createOC({ info, productos })
  if ('error' in result) {
    setServerError(result.error)
    return
  }
  router.push(`/importador/oc/${result.data.id}?step=2`)
}
```

e) En el JSX, junto al campo `referenciaOC` (input de texto), agregar mensaje de error del servidor debajo del campo. Buscar el div/label del campo referenciaOC y agregar:
```tsx
{serverError && (
  <p className="mt-1 text-xs text-red-600">{serverError}</p>
)}
```

f) Agregar estado `isLoading` para deshabilitar el botón mientras se guarda:
```typescript
const [isLoading, setIsLoading] = useState(false)
```
Actualizar handleContinuar para setIsLoading(true) al inicio y setIsLoading(false) en el catch. Agregar `disabled={isLoading}` al botón "Continuar a Paso 2".

**IMPORTANTE:** No eliminar `EMPTY_INFO`, `addProducto`, `removeProducto`, ni ningún otro estado o handler — solo eliminar el `useEffect` de sessionStorage y reemplazar `handleContinuar`.

**2. Modificar src/components/wizard/WizardPage.tsx:**

Extender las props para soportar datos pre-cargados de MongoDB:
```typescript
import type { OCDetalle } from '@/lib/mock-ocs'

interface WizardPageProps {
  initialStep: string
  ocData?: OCDetalle & { id: string; otrosImpuestos?: import('@/lib/wizard-types').OtroGastoRow[] }
  ocId?: string
}

export function WizardPage({ initialStep, ocData, ocId }: WizardPageProps) {
  if (initialStep === '2' && ocData && ocId) {
    return <Step2Form ocData={ocData} ocId={ocId} />
  }
  if (initialStep === '2') {
    // Fallback: Step 2 sin datos (no debería ocurrir tras Phase 5)
    return <Step2Form ocData={null} ocId={''} />
  }
  return <Step1Form />
}
```

**3. Modificar src/components/wizard/Step2Form.tsx:**

a) Cambiar la firma del componente para recibir props:
```typescript
interface Step2FormProps {
  ocData: (OCDetalle & { id?: string; otrosImpuestos?: OtroGastoRow[] }) | null
  ocId: string
}

export function Step2Form({ ocData, ocId }: Step2FormProps) {
```

b) Agregar import de `OCDetalle` de `@/lib/mock-ocs` si no existe.

c) Agregar import de `updateOC` de `@/actions/oc`:
```typescript
import { updateOC } from '@/actions/oc'
```

d) Eliminar el `useEffect` completo que lee `sessionStorage.getItem('oc-step1-draft')` (líneas 85-93 actuales).

e) Cambiar los `useState` de gastos para inicializarse desde `ocData` cuando está disponible:
```typescript
const [gastosDespacho, setGastosDespacho] = useState<GastosDespacho>(
  ocData?.gastosDespacho ?? { sim: '', derechos: '', otros: '', tasaEstadistica: '' }
)
const [gastosDespachante, setGastosDespachante] = useState<GastosDespachante>(
  ocData?.gastosDespachante ?? {
    terminal: '', fleteInternacional: '', fleteInterno: '', senasa: '',
    despachante: '', gastosOperativos: '', gastosBancarios: '',
  }
)
const [gastosAdicionales, setGastosAdicionales] = useState<GastosAdicionales>(
  ocData?.gastosAdicionales ?? { depositoFiscal: '', digitalizacion: '', estanciaCamion: '' }
)
const [otrosGastos, setOtrosGastos] = useState<OtroGastoRow[]>(
  ocData?.otrosGastos ?? []
)
const [impuestos, setImpuestos] = useState<Impuestos>(
  ocData?.impuestos ?? { iva: '', ivaAd: '', iibb: '', iigg: '' }
)
const [otrosImpuestos, setOtrosImpuestos] = useState<OtroGastoRow[]>(
  ocData?.otrosImpuestos ?? []
)
```

f) El `step1Data` para el ResumenStep1 se deriva de `ocData`:
```typescript
const step1Data: Step1Data | null = ocData
  ? { info: {
      referenciaOC: ocData.referenciaOC,
      estado: ocData.estado,
      proveedor: ocData.proveedor ?? '',
      emailsProveedor: ocData.emailsProveedor ?? [''],
      despacho: ocData.despacho ?? '',
      fechaDespacho: (ocData as { fechaDespacho?: string }).fechaDespacho ?? '',
      emailsDespachante: ocData.emailsDespachante ?? [''],
      paisOrigen: ocData.paisOrigen ?? '',
      fechaOC: ocData.fechaOC ?? '',
      llegadaEstimada: ocData.llegadaEstimada ?? '',
      fechaPago: (ocData as { fechaPago?: string }).fechaPago ?? '',
      tipoCambio: ocData.tipoCambio ?? '',
      divisa: ocData.divisa ?? 'ARS/USD',
      notas: ocData.notas ?? '',
    },
    productos: ocData.productos ?? [],
  }
  : null
```

g) Eliminar el `if (!step1Data) return null` que dependía del useEffect. Reemplazar con:
```typescript
if (!ocData || !step1Data) {
  return (
    <div className="max-w-4xl mx-auto px-4 py-12 text-center text-texto/60">
      No se encontró la OC. <a href="/importador/dashboard" className="underline">Volver al dashboard</a>
    </div>
  )
}
```

h) Agregar `[serverError, setServerError]` y `[isLoading, setIsLoading]` states.

i) Reemplazar `handleVolver`:
```typescript
const handleVolver = () => router.push(`/importador/oc/${ocId}`)
```
(ya no hay `/importador/oc/nueva?step=1` — la OC ya fue creada)

j) Reemplazar `handleGuardar` mock por versión real:
```typescript
const handleGuardar = async () => {
  setIsLoading(true)
  setServerError(null)
  const result = await updateOC(ocId, {
    gastosDespacho,
    gastosDespachante,
    gastosAdicionales,
    impuestos,
    otrosGastos,
    otrosImpuestos,
    estado: step1Data.info.estado,
  })
  setIsLoading(false)
  if ('error' in result) {
    setServerError(result.error)
    return
  }
  setToastVisible(true)
  setTimeout(() => {
    setToastVisible(false)
    router.push('/importador/dashboard')
  }, 2000)
}
```

k) Mantener el toast existente sin cambios.

**4. Modificar src/app/(importador)/importador/oc/[id]/page.tsx:**

La page.tsx ya es Server Component async. Agregar soporte para `searchParams.step === '2'`:

```typescript
import { notFound } from 'next/navigation'
import { getOCById } from '@/actions/oc'
import { OCDetailHeader } from '@/components/oc-detail/OCDetailHeader'
import { OCDetailView } from '@/components/oc-detail/OCDetailView'
import { WizardPage } from '@/components/wizard/WizardPage'

interface PageProps {
  params: Promise<{ id: string }>
  searchParams: Promise<{ step?: string }>
}

export default async function OCDetailPage({ params, searchParams }: PageProps) {
  const { id } = await params
  const { step } = await searchParams

  const result = await getOCById(id)
  if ('error' in result) notFound()
  const oc = result.data.oc

  if (step === '2') {
    return <WizardPage initialStep="2" ocData={oc} ocId={id} />
  }

  return (
    <div className="bg-fondo min-h-screen">
      <OCDetailHeader oc={oc} rol="importador" />
      <OCDetailView oc={oc} />
    </div>
  )
}
```

**5. src/app/(importador)/importador/oc/nueva/page.tsx** — no requiere cambios. Step 1 sigue viviendo en esta ruta. Solo quitar `step=2` del wizard — si alguien navega a `/importador/oc/nueva?step=2` directamente verá Step1Form (comportamiento aceptable; sin sessionStorage no hay datos para Step 2 de todas formas).
  </action>
  <verify>
    <automated>grep -c "sessionStorage" /mnt/c/Users/Equipo/Documents/DrivaOC/src/components/wizard/Step1Form.tsx 2>/dev/null || echo "0"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/wizard/Step1Form.tsx NO contiene `sessionStorage` en ninguna línea
    - src/components/wizard/Step1Form.tsx contiene `createOC` como import y llamada dentro de `handleContinuar`
    - src/components/wizard/Step1Form.tsx contiene `router.push(\`/importador/oc/${result.data.id}?step=2\`)` (interpolación con result.data.id)
    - src/components/wizard/Step2Form.tsx NO contiene `sessionStorage` en ninguna línea
    - src/components/wizard/Step2Form.tsx contiene `updateOC` como import y llamada dentro de `handleGuardar`
    - src/components/wizard/Step2Form.tsx NO contiene `useEffect` (el único useEffect era el de sessionStorage)
    - src/components/wizard/WizardPage.tsx tiene props `ocData` y `ocId` en la interfaz
    - src/app/(importador)/importador/oc/[id]/page.tsx contiene `getOCById` y `step === '2'`
    - src/app/(importador)/importador/oc/[id]/page.tsx NO contiene `MOCK_OCS_DETALLE`
    - `npx tsc --noEmit` no retorna errores en los archivos modificados
  </acceptance_criteria>
  <done>Wizard end-to-end conectado a MongoDB. sessionStorage eliminado del flujo de creación. Step 2 recibe datos de MongoDB como props.</done>
</task>

<task type="auto">
  <name>Tarea 2: Convertir EditWizardLoaders a Server Components + reemplazar mock en páginas de detalle</name>
  <read_first>
    - src/app/(importador)/importador/oc/[id]/editar/EditWizardLoader.tsx (ver estructura actual — 'use client', useEffect, sessionStorage, WizardPage sin props)
    - src/app/(proveedor)/proveedor/oc/[id]/editar/EditWizardLoader.tsx (ver estructura — igual a importador pero con rol='proveedor')
    - src/app/(despachante)/despachante/oc/[id]/editar/EditWizardLoader.tsx (ver estructura)
    - src/app/(importador)/importador/oc/[id]/editar/page.tsx (ver cómo llama EditWizardLoader — pasa ocId y rol)
    - src/app/(proveedor)/proveedor/oc/[id]/page.tsx (ver mock a reemplazar)
    - src/app/(despachante)/despachante/oc/[id]/page.tsx (ver mock a reemplazar)
  </read_first>
  <files>
    src/app/(importador)/importador/oc/[id]/editar/EditWizardLoader.tsx,
    src/app/(proveedor)/proveedor/oc/[id]/editar/EditWizardLoader.tsx,
    src/app/(despachante)/despachante/oc/[id]/editar/EditWizardLoader.tsx,
    src/app/(proveedor)/proveedor/oc/[id]/page.tsx,
    src/app/(despachante)/despachante/oc/[id]/page.tsx
  </files>
  <action>
**1. Reemplazar src/app/(importador)/importador/oc/[id]/editar/EditWizardLoader.tsx** (D-05):

Convertir de `'use client'` + `useEffect` + sessionStorage a Server Component async que fetchea de MongoDB:

```typescript
import { redirect } from 'next/navigation'
import { getOCById } from '@/actions/oc'
import { WizardPage } from '@/components/wizard/WizardPage'

interface Props {
  ocId: string
  rol: string
}

export async function EditWizardLoader({ ocId, rol }: Props) {
  const result = await getOCById(ocId)
  if ('error' in result) redirect(`/${rol}/dashboard`)
  return <WizardPage initialStep="1" ocData={result.data.oc} ocId={ocId} />
}
```

**Nota:** En el flujo de edición, WizardPage muestra Step 1 (initialStep="1") pre-poblado con `ocData`. Step1Form actualmente inicializa con `EMPTY_INFO` y lee sessionStorage. En esta tarea, Step1Form ya no lee sessionStorage (eliminado en Tarea 1). Para pre-poblar Step 1 con datos existentes en el flujo de edición, WizardPage debe pasar `ocData` a Step1Form como prop inicial.

Actualizar `WizardPage` (en el mismo archivo o como cambio incremental) para pasar `ocData` a `Step1Form` cuando `initialStep === '1'` y `ocData` está presente:

```typescript
// En WizardPage.tsx — agregar caso:
if (initialStep === '1' && ocData) {
  return <Step1Form initialData={ocData} ocId={ocId} />
}
return <Step1Form />
```

Y en `Step1Form.tsx`, agregar prop opcional `initialData`:
```typescript
interface Step1FormProps {
  initialData?: { info: InfoGeneralState; productos: ProductRow[] }
  ocId?: string  // Si existe, Step 1 está en modo edición
}

export function Step1Form({ initialData, ocId }: Step1FormProps = {}) {
  const [info, setInfo] = useState<InfoGeneralState>(initialData?.info ?? EMPTY_INFO)
  const [productos, setProductos] = useState<ProductRow[]>(
    initialData?.productos?.length ? initialData.productos : [
      { id: crypto.randomUUID(), producto: '', descripcion: '', cantidad: '', valorUSD: '' },
    ]
  )
  // ...
```

En el modo edición (`ocId` definido), `handleContinuar` debe llamar `updateOC` para actualizar los campos de Step 1 (info general + productos) en lugar de `createOC`. Implementar:
```typescript
const handleContinuar = async () => {
  setSubmitted(true)
  if (!isStep1Valid(info, productos)) return
  setServerError(null)
  setIsLoading(true)

  if (ocId) {
    // Modo edición: navegar directamente a Step 2 con datos actualizados
    // (Step 2 fetcheará la OC fresca de DB cuando se cargue)
    setIsLoading(false)
    router.push(`/importador/oc/${ocId}?step=2`)
    return
  }

  // Modo creación
  const result = await createOC({ info, productos })
  setIsLoading(false)
  if ('error' in result) {
    setServerError(result.error)
    return
  }
  router.push(`/importador/oc/${result.data.id}?step=2`)
}
```

**Nota sobre edición de Step 1:** En el flujo de edición, el importador puede modificar la info general en Step 1 antes de ir a Step 2. Para Phase 5, simplificar: en modo edición, Step 1 muestra los datos actuales como preview (pre-poblado) y el botón "Continuar a Paso 2" lleva a Step 2 con el ID existente. Los campos editados en Step 1 durante edición se guardarán en un Server Action adicional si se requiere; por ahora, `updateOC` en Step 2 actualiza solo gastos y estado (per D-04). Si el importador quiere cambiar info general (proveedor, emails, etc.), debe acceder a edit nuevamente.

**2. Reemplazar src/app/(proveedor)/proveedor/oc/[id]/editar/EditWizardLoader.tsx:**

```typescript
import { redirect } from 'next/navigation'
import { getOCById } from '@/actions/oc'
import { WizardPage } from '@/components/wizard/WizardPage'

interface Props {
  ocId: string
  rol: string
}

export async function EditWizardLoader({ ocId, rol }: Props) {
  const result = await getOCById(ocId)
  if ('error' in result) redirect(`/${rol}/dashboard`)
  return <WizardPage initialStep="1" ocData={result.data.oc} ocId={ocId} />
}
```

**3. Reemplazar src/app/(despachante)/despachante/oc/[id]/editar/EditWizardLoader.tsx:**

Idéntico al de proveedor — solo el nombre del componente puede variar según el archivo.

**4. Reemplazar src/app/(proveedor)/proveedor/oc/[id]/page.tsx** — reemplazar mock por getOCById:

```typescript
import { notFound } from 'next/navigation'
import { getOCById } from '@/actions/oc'
import { OCDetailHeader } from '@/components/oc-detail/OCDetailHeader'
import { OCDetailView } from '@/components/oc-detail/OCDetailView'

interface PageProps {
  params: Promise<{ id: string }>
}

export default async function OCDetailPage({ params }: PageProps) {
  const { id } = await params
  const result = await getOCById(id)
  if ('error' in result) notFound()
  return (
    <div className="bg-fondo min-h-screen">
      <OCDetailHeader oc={result.data.oc} rol="proveedor" />
      <OCDetailView oc={result.data.oc} />
    </div>
  )
}
```

**5. Reemplazar src/app/(despachante)/despachante/oc/[id]/page.tsx** — idéntico con `rol="despachante"`:

```typescript
import { notFound } from 'next/navigation'
import { getOCById } from '@/actions/oc'
import { OCDetailHeader } from '@/components/oc-detail/OCDetailHeader'
import { OCDetailView } from '@/components/oc-detail/OCDetailView'

interface PageProps {
  params: Promise<{ id: string }>
}

export default async function OCDetailPage({ params }: PageProps) {
  const { id } = await params
  const result = await getOCById(id)
  if ('error' in result) notFound()
  return (
    <div className="bg-fondo min-h-screen">
      <OCDetailHeader oc={result.data.oc} rol="despachante" />
      <OCDetailView oc={result.data.oc} />
    </div>
  )
}
```

**Verificación de tipo entre SerializedOC y OCDetalle:** `serializeOC` en Plan 02 retorna un objeto que extiende `OCDetalle`. Si TypeScript levanta errores en `OCDetailHeader` o `OCDetailView` porque esperan `OCDetalle` exacto (con `id: string` de la interfaz `OC` base), asegurarse de que `SerializedOC` incluye todos los campos requeridos. Leer `OCDetailHeader.tsx` y `OCDetailView.tsx` para confirmar las props esperadas antes de compilar.
  </action>
  <verify>
    <automated>grep -rn "sessionStorage" /mnt/c/Users/Equipo/Documents/DrivaOC/src/components/wizard/ /mnt/c/Users/Equipo/Documents/DrivaOC/src/app/ 2>/dev/null | grep -v ".planning" | wc -l || echo "0"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(importador)/importador/oc/[id]/editar/EditWizardLoader.tsx NO contiene `'use client'`
    - src/app/(importador)/importador/oc/[id]/editar/EditWizardLoader.tsx NO contiene `useEffect`
    - src/app/(importador)/importador/oc/[id]/editar/EditWizardLoader.tsx NO contiene `sessionStorage`
    - src/app/(importador)/importador/oc/[id]/editar/EditWizardLoader.tsx contiene `async function EditWizardLoader`
    - src/app/(importador)/importador/oc/[id]/editar/EditWizardLoader.tsx contiene `getOCById`
    - src/app/(proveedor)/proveedor/oc/[id]/editar/EditWizardLoader.tsx contiene `async function EditWizardLoader` y `getOCById`
    - src/app/(despachante)/despachante/oc/[id]/editar/EditWizardLoader.tsx contiene `async function EditWizardLoader` y `getOCById`
    - src/app/(proveedor)/proveedor/oc/[id]/page.tsx NO contiene `MOCK_OCS_DETALLE`
    - src/app/(despachante)/despachante/oc/[id]/page.tsx NO contiene `MOCK_OCS_DETALLE`
    - Búsqueda global de `sessionStorage` en src/ retorna 0 resultados (todos eliminados)
    - `npx tsc --noEmit` no retorna errores en los archivos modificados
  </acceptance_criteria>
  <done>sessionStorage completamente eliminado. EditWizardLoaders son Server Components. Páginas de detalle leen datos reales de MongoDB. El sistema funciona end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client Component → Server Action (createOC) | Datos del formulario Step 1 enviados desde el browser |
| Client Component → Server Action (updateOC) | Datos de gastos de Step 2 enviados desde el browser |
| Server Component → getOCById | Acceso a OC por ID desde rutas de edición/detalle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08 | Spoofing (IDOR) | EditWizardLoader → getOCById | mitigate | `getOCById` verifica `oc.importadorId === userId` para importador; verifica email en array para proveedor/despachante. Redirect a dashboard si sin acceso |
| T-05-09 | Information Disclosure | Step 2 recibe ocData como prop | accept | ocData viene de getOCById server-side que ya validó acceso; prop pasa de Server a Client Component — Next.js serializa solo datos plain |
| T-05-10 | Tampering | updateOC estado | accept | El estado que se guarda proviene de `step1Data.info.estado` (lo que eligió el importador en Step 1, ya persistido en DB). No hay override desde el cliente — IDOR guard en updateOC verifica propiedad |
</threat_model>

<verification>
Después de ejecutar este plan:

1. **Flujo de creación completo:**
   - Navegar a `/importador/oc/nueva`
   - Llenar Step 1 con referenciaOC única, proveedor, productos
   - Hacer clic en "Continuar a Paso 2"
   - Verificar que la URL cambia a `/importador/oc/[id-mongodb]?step=2` (ID real de MongoDB, no 'OC-001')
   - Llenar algunos gastos en Step 2
   - Hacer clic en "Guardar OC"
   - Verificar que redirige al dashboard y la OC aparece en la lista

2. **Flujo de edición:**
   - En el dashboard, clic en "Editar" sobre una OC existente
   - Verificar que la URL va a `/importador/oc/[id]/editar`
   - Verificar que los campos de Step 1 están pre-poblados con datos de MongoDB (no vacíos)
   - Hacer clic en "Continuar a Paso 2"
   - Verificar que Step 2 muestra los gastos previamente guardados

3. **Detalle de proveedor:**
   - Navegar a `/proveedor/oc/[id]` (con ID de una OC no-borrador que tenga el email del proveedor)
   - Verificar que muestra los datos reales (sin 'Mock OC' en ningún campo)

4. **Verificación de sessionStorage eliminado:**
   - En DevTools → Application → Storage → Session Storage: no debe existir la clave `oc-step1-draft` en ningún momento del flujo

5. `npm run build` sin errores TypeScript
</verification>

<success_criteria>
- sessionStorage eliminado de Step1Form, Step2Form, y todos los EditWizardLoader
- Step1Form llama createOC y redirige a /importador/oc/[id]?step=2 con ID real de MongoDB
- Step2Form recibe ocData como prop, inicializa desde MongoDB, llama updateOC al guardar
- Los 3 EditWizardLoader son Server Components async que usan getOCById
- Las páginas de detalle de proveedor y despachante leen de MongoDB (no MOCK_OCS_DETALLE)
- `npm run build` sin errores TypeScript
- El flujo completo crear → ver en dashboard funciona end-to-end
</success_criteria>

<output>
Después de completar este plan, crear `.planning/phases/05-backend-core/05-03-SUMMARY.md` con:
- Archivos creados/modificados
- Decisiones de implementación (manejo de edición de Step 1 en modo edición)
- Cualquier divergencia del plan
- Estado: COMPLETE

Y actualizar `.planning/STATE.md` — Phase 5 completa.
</output>
