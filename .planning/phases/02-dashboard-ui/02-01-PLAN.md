---
phase: 02-dashboard-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/mock-ocs.ts
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04

must_haves:
  truths:
    - "El array MOCK_OCS contiene OCs que cubren los 6 estados posibles"
    - "Cada OC tiene los campos necesarios para renderizar la tabla (id, numeroOC, proveedor, despachante, estado, fecha)"
    - "El tipo EstadoOC es un union literal exacto de los 6 estados de D-04"
    - "La capa de datos puede ser importada sin errores TypeScript strict"
  artifacts:
    - path: "src/lib/mock-ocs.ts"
      provides: "Tipos TypeScript OC + array MOCK_OCS"
      exports: ["EstadoOC", "OC", "MOCK_OCS"]
      contains: "export const MOCK_OCS"
  key_links:
    - from: "src/components/dashboard/OCTable.tsx"
      to: "src/lib/mock-ocs.ts"
      via: "import type { OC, EstadoOC } from '@/lib/mock-ocs'"
      pattern: "from '@/lib/mock-ocs'"
    - from: "src/app/(importador)/importador/dashboard/page.tsx"
      to: "src/lib/mock-ocs.ts"
      via: "import { MOCK_OCS } from '@/lib/mock-ocs'"
      pattern: "MOCK_OCS"
---

<objective>
Crear la capa de datos mock para Phase 2: tipos TypeScript del modelo OC y un array de OCs ficticias que cubren los 6 estados del sistema, para los tres roles.

Purpose: Todos los componentes de dashboard importan desde este único source of truth. Sin esta capa, ningún componente de Phase 2 puede ser construido.
Output: `src/lib/mock-ocs.ts` con tipos exportados y ~10 OCs ficticias.
</objective>

<execution_context>
@/home/user/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dashboard-ui/02-CONTEXT.md
@.planning/phases/02-dashboard-ui/02-PATTERNS.md

<interfaces>
<!-- Patrón de lib existente — src/lib/utils.ts -->
<!-- Named exports, no default export, TypeScript strict -->
```typescript
// src/lib/utils.ts — patrón de referencia para el layer lib
import { clsx, type ClassValue } from 'clsx'
import { twMerge } from 'tailwind-merge'

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs))
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear src/lib/mock-ocs.ts con tipos y datos mock</name>
  <files>src/lib/mock-ocs.ts</files>

  <read_first>
    - src/lib/utils.ts — patrón del layer lib: named exports, sin default export, TypeScript strict
    - .planning/phases/02-dashboard-ui/02-CONTEXT.md — D-04 (6 estados), D-02 (campos de tabla), D-07/D-08 (filtrado por rol)
    - .planning/phases/02-dashboard-ui/02-PATTERNS.md — sección "src/lib/mock-ocs.ts" con el skeleton shape exacto
  </read_first>

  <action>
Crear `src/lib/mock-ocs.ts` con el siguiente contenido exacto:

**Tipos a exportar:**

```typescript
export type EstadoOC =
  | 'borrador'
  | 'en_proceso'
  | 'en_transito'
  | 'en_aduana'
  | 'entregada'
  | 'cancelada'

export interface OC {
  id: string
  numeroOC: string
  proveedor: string        // nombre del proveedor (para mostrar en tabla)
  emailProveedor: string   // email del proveedor (para filtrado por rol)
  emailDespachante: string // email del despachante (para filtrado por rol)
  estado: EstadoOC
  fecha: string            // ISO date string: "2025-03-15"
}
```

**Array MOCK_OCS — 10 entradas:**
- Debe cubrir los 6 estados al menos una vez
- `emailProveedor` mock: usar `proveedor@empresa.com` para OCs 1-5, `otro@proveedor.com` para OCs 6-10
- `emailDespachante` mock: usar `despachante@logistica.com` para OCs 1-7, `otro@despacho.com` para OCs 8-10
- `numeroOC`: formato `OC-001` a `OC-010`
- `fecha`: fechas entre 2025-01-01 y 2025-12-01 (variar)
- `proveedor`: nombres de empresas ficticias en español (ej. "Importadora del Sur", "Tech Supplies SA")

Distribución de estados sugerida:
- 2 x `borrador`
- 2 x `en_proceso`
- 2 x `en_transito`
- 1 x `en_aduana`
- 2 x `entregada`
- 1 x `cancelada`

**Regla de estructura:** No default export. Tipos primero, luego el array. Sin comentarios explicativos.
  </action>

  <verify>
    <automated>cd /c/Users/Equipo/Documents/DrivaOC && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>

  <acceptance_criteria>
    - src/lib/mock-ocs.ts existe en el filesystem
    - El archivo contiene `export type EstadoOC`
    - El archivo contiene `export interface OC`
    - El archivo contiene `export const MOCK_OCS`
    - El array MOCK_OCS tiene exactamente 10 entradas (verificar contando objetos `{` en el array)
    - Los 6 estados aparecen al menos una vez en el array: `borrador`, `en_proceso`, `en_transito`, `en_aduana`, `entregada`, `cancelada`
    - `npx tsc --noEmit` no reporta errores en este archivo
    - No hay `default export` en el archivo
    - No hay uso de `any` ni `as any`
    - El campo `emailProveedor` existe en la interfaz OC
    - El campo `emailDespachante` existe en la interfaz OC
  </acceptance_criteria>

  <done>
    `src/lib/mock-ocs.ts` existe, exporta `EstadoOC`, `OC` y `MOCK_OCS`, el array tiene 10 OCs que cubren los 6 estados, y TypeScript strict no reporta errores.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descripción |
|----------|-------------|
| No aplica | Fase 2 es UI-only con datos hardcodeados — no hay entrada de usuario ni requests externos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Information Disclosure | src/lib/mock-ocs.ts | accept | Datos ficticios sin PII real. En producción (Phase 5), este archivo será reemplazado por Server Actions con queries Mongoose scoped por rol. |
| T-02-01-02 | Spoofing | emailProveedor / emailDespachante | accept | En Phase 2 los emails son strings hardcodeados para demo — no hay autenticación de identidad. Phase 5 reemplaza con sessionClaims del JWT de Clerk. |
</threat_model>

<verification>
- `src/lib/mock-ocs.ts` existe
- `grep "export type EstadoOC" src/lib/mock-ocs.ts` retorna la línea
- `grep "export interface OC" src/lib/mock-ocs.ts` retorna la línea
- `grep "export const MOCK_OCS" src/lib/mock-ocs.ts` retorna la línea
- `npx tsc --noEmit` sin errores
</verification>

<success_criteria>
El archivo `src/lib/mock-ocs.ts` es importable desde cualquier componente con `import { MOCK_OCS, type OC, type EstadoOC } from '@/lib/mock-ocs'` sin errores TypeScript. El array cubre los 6 estados posibles necesarios para renderizar todos los badges en Phase 2.
</success_criteria>

<output>
Después de completar, crear `.planning/phases/02-dashboard-ui/02-01-SUMMARY.md`
</output>
