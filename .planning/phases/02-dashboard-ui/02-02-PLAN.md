---
phase: 02-dashboard-ui
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/components/dashboard/StatCard.tsx
  - src/components/dashboard/OCTable.tsx
  - src/components/dashboard/DeleteModal.tsx
  - src/components/dashboard/EmptyState.tsx
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03

must_haves:
  truths:
    - "StatCard renderiza un ícono lucide, un número grande y un label descriptivo con clases del UI-SPEC"
    - "OCTable renderiza la tabla con las 5 columnas (Nº OC, Proveedor, Estado, Fecha, Acciones)"
    - "Los badges de estado usan las clases Tailwind exactas del UI-SPEC para los 6 estados"
    - "Importador ve Eye + Pencil + Trash2; Proveedor/Despachante ven Eye + Pencil (sin Trash2)"
    - "El skeleton loading muestra 5 filas con animate-pulse antes de revelar los datos"
    - "DeleteModal muestra overlay con copy exacto del Copywriting Contract"
    - "EmptyState muestra heading, body, y CTA solo para importador"
  artifacts:
    - path: "src/components/dashboard/StatCard.tsx"
      provides: "Componente de stat card con ícono + número + label"
      exports: ["StatCard"]
      contains: "border border-acento rounded-xl p-6"
    - path: "src/components/dashboard/OCTable.tsx"
      provides: "Tabla responsive con badges, skeleton y botones de acción por rol"
      exports: ["OCTable"]
      contains: "animate-pulse"
    - path: "src/components/dashboard/DeleteModal.tsx"
      provides: "Modal de confirmación de eliminación con overlay"
      exports: ["DeleteModal"]
      contains: "fixed inset-0 z-50"
    - path: "src/components/dashboard/EmptyState.tsx"
      provides: "Estado vacío con CTA condicional por rol"
      exports: ["EmptyState"]
      contains: "No hay órdenes de compra"
  key_links:
    - from: "src/components/dashboard/OCTable.tsx"
      to: "src/lib/mock-ocs.ts"
      via: "import type { OC, EstadoOC } from '@/lib/mock-ocs'"
      pattern: "from '@/lib/mock-ocs'"
    - from: "src/components/dashboard/OCTable.tsx"
      to: "src/components/dashboard/DeleteModal.tsx"
      via: "import { DeleteModal } from './DeleteModal'"
      pattern: "DeleteModal"
    - from: "src/components/dashboard/OCTable.tsx"
      to: "src/components/dashboard/EmptyState.tsx"
      via: "import { EmptyState } from './EmptyState'"
      pattern: "EmptyState"
---

<objective>
Crear los 4 componentes del dashboard: StatCard (Server Component), OCTable (Client Component con skeleton + badges + acciones por rol), DeleteModal (Client Component con overlay), y EmptyState (Server Component con CTA condicional).

Purpose: Estos son los building blocks visuales del dashboard. El plan 02-03 los wirará en las páginas reales con datos y filtros.
Output: 4 archivos en `src/components/dashboard/`, todos con clases Tailwind exactas del UI-SPEC aprobado.
</objective>

<execution_context>
@/home/user/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-dashboard-ui/02-CONTEXT.md
@.planning/phases/02-dashboard-ui/02-UI-SPEC.md
@.planning/phases/02-dashboard-ui/02-PATTERNS.md
@.planning/phases/02-dashboard-ui/02-01-SUMMARY.md

<interfaces>
<!-- Tipos del plan 02-01 (src/lib/mock-ocs.ts) -->
```typescript
export type EstadoOC =
  | 'borrador'
  | 'en_proceso'
  | 'en_transito'
  | 'en_aduana'
  | 'entregada'
  | 'cancelada'

export interface OC {
  id: string
  numeroOC: string
  proveedor: string
  emailProveedor: string
  emailDespachante: string
  estado: EstadoOC
  fecha: string // ISO date string
}
```

<!-- Patrón cn() de src/lib/utils.ts -->
```typescript
import { cn } from '@/lib/utils'
```

<!-- Patrón de importación lucide-react de Sidebar.tsx -->
```typescript
import { Eye, Pencil, Trash2, FileText, PlusCircle } from 'lucide-react'
import type { LucideIcon } from 'lucide-react'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: StatCard y DeleteModal</name>
  <files>
    src/components/dashboard/StatCard.tsx
    src/components/dashboard/DeleteModal.tsx
  </files>

  <read_first>
    - src/components/layout/Sidebar.tsx — patrón cn(), touch targets 44px, overlay backdrop (líneas 139-157)
    - src/lib/utils.ts — para verificar import path de cn()
    - .planning/phases/02-dashboard-ui/02-UI-SPEC.md — secciones "Stat Card Spec" y "Delete Confirmation Modal"
    - .planning/phases/02-dashboard-ui/02-CONTEXT.md — D-06 (stat card style)
  </read_first>

  <action>
**StatCard.tsx — Server Component (sin 'use client')**

Props interface:
```typescript
import type { LucideIcon } from 'lucide-react'

interface StatCardProps {
  icon: LucideIcon
  value: number
  label: string
}
```

Estructura JSX con clases EXACTAS del UI-SPEC:
```tsx
export function StatCard({ icon: Icon, value, label }: StatCardProps) {
  return (
    <div className="bg-white border border-acento rounded-xl p-6 flex flex-col gap-3">
      <Icon size={24} className="text-principal" />
      <p className="text-4xl font-bold text-texto">{value}</p>
      <p className="text-sm font-light text-titulares">{label}</p>
    </div>
  )
}
```

Named export. Sin default export. Sin `'use client'`.

---

**DeleteModal.tsx — Client Component**

Directiva: `'use client'` (primera línea).

Props interface:
```typescript
interface DeleteModalProps {
  open: boolean
  ocNumero: string
  proveedor: string
  onConfirm: () => void
  onCancel: () => void
}
```

Estructura con clases EXACTAS del UI-SPEC:
- Overlay: `fixed inset-0 z-50 bg-texto/30 flex items-center justify-center p-4`
- Panel: `bg-white rounded-xl border border-acento p-6 max-w-sm w-full flex flex-col gap-4`
- Heading: `<p className="text-xl font-bold text-titulares">¿Eliminar esta OC?</p>`
- Body: `<p className="text-base text-texto/70">Estás por eliminar la OC {ocNumero} de {proveedor}. Esta acción no se puede deshacer.</p>`
- Botón Eliminar: `className="bg-red-600 text-white hover:bg-red-700 px-4 py-2 rounded-lg font-medium min-h-[44px] transition-colors duration-150"`
- Botón Cancelar: `className="border border-acento text-texto hover:bg-fondo px-4 py-2 rounded-lg font-normal min-h-[44px] transition-colors duration-150"`

Si `open` es false, retornar `null`.

Named export. Sin default export.
  </action>

  <acceptance_criteria>
    - src/components/dashboard/StatCard.tsx existe
    - StatCard.tsx NO contiene `'use client'`
    - StatCard.tsx contiene `bg-white border border-acento rounded-xl p-6 flex flex-col gap-3`
    - StatCard.tsx contiene `text-4xl font-bold text-texto`
    - StatCard.tsx contiene `text-sm font-light text-titulares`
    - StatCard.tsx contiene `export function StatCard`
    - src/components/dashboard/DeleteModal.tsx existe
    - DeleteModal.tsx contiene `'use client'` en la primera línea
    - DeleteModal.tsx contiene `fixed inset-0 z-50 bg-texto/30`
    - DeleteModal.tsx contiene `¿Eliminar esta OC?`
    - DeleteModal.tsx contiene `Esta acción no se puede deshacer`
    - DeleteModal.tsx contiene `bg-red-600 text-white hover:bg-red-700`
    - DeleteModal.tsx contiene `export function DeleteModal`
    - Ninguno de los dos archivos usa `any` o `as any`
  </acceptance_criteria>

  <done>
    StatCard renderiza con las clases exactas del UI-SPEC. DeleteModal muestra overlay con copy correcto y botones destructivos. TypeScript strict sin errores.
  </done>
</task>

<task type="auto">
  <name>Task 2: OCTable y EmptyState</name>
  <files>
    src/components/dashboard/OCTable.tsx
    src/components/dashboard/EmptyState.tsx
  </files>

  <read_first>
    - src/components/layout/Sidebar.tsx — patrón conditional rendering por rol (líneas 100-115), touch targets (líneas 60-73)
    - .planning/phases/02-dashboard-ui/02-UI-SPEC.md — secciones "Table Spec", "Badge Specs: Estados de OC", "Action Buttons Spec", "Skeleton Loading Spec", "Empty State Spec"
    - .planning/phases/02-dashboard-ui/02-CONTEXT.md — D-01 (scroll horizontal), D-02 (columnas), D-03 y D-08 (botones por rol)
    - src/lib/mock-ocs.ts — para confirmar la interfaz OC exacta antes de tipar los props
  </read_first>

  <action>
**OCTable.tsx — Client Component**

Directiva: `'use client'` (primera línea).

Imports necesarios:
```typescript
'use client'
import { useState } from 'react'
import Link from 'next/link'
import { Eye, Pencil, Trash2 } from 'lucide-react'
import { cn } from '@/lib/utils'
import type { OC, EstadoOC } from '@/lib/mock-ocs'
import { DeleteModal } from './DeleteModal'
import { EmptyState } from './EmptyState'
```

Props interface:
```typescript
type Rol = 'importador' | 'proveedor' | 'despachante'

interface OCTableProps {
  ocs: OC[]
  rol: Rol
  isLoading?: boolean
}
```

**Función helper para badge classes** (mapeo EXACTO del UI-SPEC):
```typescript
function getBadgeClasses(estado: EstadoOC): string {
  const base = 'text-sm px-2 py-0.5 rounded-full'
  const map: Record<EstadoOC, string> = {
    borrador:    `${base} bg-fondo text-titulares border border-acento font-light`,
    en_proceso:  `${base} bg-acento/50 text-titulares font-light`,
    en_transito: `${base} bg-acento text-titulares font-normal`,
    en_aduana:   `${base} bg-principal/20 text-titulares font-normal`,
    entregada:   `${base} bg-principal/10 text-principal font-normal`,
    cancelada:   `${base} bg-texto/10 text-texto font-light line-through`,
  }
  return map[estado]
}
```

**Labels de estado** (mapeo EXACTO del Copywriting Contract del UI-SPEC):
```typescript
const ESTADO_LABELS: Record<EstadoOC, string> = {
  borrador:    'Borrador',
  en_proceso:  'En proceso',
  en_transito: 'En tránsito',
  en_aduana:   'En aduana',
  entregada:   'Entregada',
  cancelada:   'Cancelada',
}
```

**Función helper para formatear fecha** (ISO → dd/mm/yyyy):
```typescript
function formatFecha(iso: string): string {
  const [year, month, day] = iso.split('-')
  return `${day}/${month}/${year}`
}
```

**Estado del modal** (en el cuerpo del componente):
```typescript
const [deleteTarget, setDeleteTarget] = useState<OC | null>(null)
```

**Skeleton — cuando isLoading es true**, retornar la tabla con 5 filas skeleton:
- Wrapper: `<div className="w-full overflow-x-auto rounded-xl border border-acento bg-white">`
- Tabla: `<table className="w-full min-w-[640px] text-base">`
- Thead idéntico al normal (con las 5 columnas reales)
- 5 filas `<tr className="border-b border-acento/50 animate-pulse">` con celdas:
  - td 1: `<div className="h-4 w-16 rounded bg-acento/30" />`
  - td 2: `<div className="h-4 w-32 rounded bg-acento/30" />`
  - td 3: `<div className="h-5 w-20 rounded-full bg-acento/40" />`
  - td 4: `<div className="h-4 w-24 rounded bg-acento/30" />`
  - td 5: `<div className="flex gap-1"><div className="h-8 w-8 rounded-lg bg-acento/30" /><div className="h-8 w-8 rounded-lg bg-acento/30" /></div>`

**Cuando ocs.length === 0** (y no isLoading): retornar `<EmptyState rol={rol} hasFilters={false} />`

**Tabla principal** (cuando hay OCs):
- Wrapper: `<div className="w-full overflow-x-auto rounded-xl border border-acento bg-white">`
- Tabla: `<table className="w-full min-w-[640px] text-base">`
- Thead: `<thead className="bg-fondo border-b border-acento">`
- Th cells: `<th className="px-4 py-3 text-left text-sm font-medium text-titulares whitespace-nowrap">`
- Columnas en orden con ancho sugerido: `Nº OC` (`w-[100px]`), `Proveedor` (auto), `Estado` (`w-[140px]`), `Fecha` (`w-[110px]`), `Acciones` (`w-[100px]`)
- Tbody rows: `<tr className="border-b border-acento/50 hover:bg-acento/20 transition-colors duration-150">`
- Td cells: `<td className="px-4 py-3 text-base text-texto">`
- Nº OC cell: `<td className="px-4 py-3 w-[100px]"><span className="font-medium text-titulares">{oc.numeroOC}</span></td>`

**Botones de acción** (rutas EXACTAS del UI-SPEC):
- Contenedor: `<div className="flex items-center gap-1">`
- Cada botón base: `className="min-h-[44px] min-w-[44px] flex items-center justify-center rounded-lg text-texto hover:text-principal hover:bg-acento/30 transition-colors duration-150"`
- Eye button: `aria-label="Visualizar OC"`, usa `Link` con href:
  - importador: `/importador/oc/${oc.id}`
  - proveedor: `/proveedor/oc/${oc.id}`
  - despachante: `/despachante/oc/${oc.id}`
- Pencil button: `aria-label="Editar OC"`, usa `Link` con href:
  - importador: `/importador/oc/${oc.id}/editar`
  - proveedor: `/proveedor/oc/${oc.id}/editar`
  - despachante: `/despachante/oc/${oc.id}/editar`
- Trash2 button: SOLO para `rol === 'importador'`. Classes: `"min-h-[44px] min-w-[44px] flex items-center justify-center rounded-lg text-red-600 hover:text-red-700 hover:bg-red-50 transition-colors duration-150"`. `aria-label="Eliminar OC"`. Al hacer click: `setDeleteTarget(oc)`.

Renderizar `<DeleteModal>` al final del componente (fuera de la tabla) con:
- `open={deleteTarget !== null}`
- `ocNumero={deleteTarget?.numeroOC ?? ''}`
- `proveedor={deleteTarget?.proveedor ?? ''}`
- `onConfirm={() => setDeleteTarget(null)}` (Phase 2: no hay eliminación real)
- `onCancel={() => setDeleteTarget(null)}`

Named export `export function OCTable`. Sin default export.

---

**EmptyState.tsx — Server Component (sin 'use client')**

Props interface:
```typescript
interface EmptyStateProps {
  rol: 'importador' | 'proveedor' | 'despachante'
  hasFilters: boolean
}
```

Estructura EXACTA del UI-SPEC:
```tsx
import Link from 'next/link'
import { FileText, PlusCircle } from 'lucide-react'

export function EmptyState({ rol, hasFilters }: EmptyStateProps) {
  return (
    <div className="flex flex-col items-center justify-center py-16 gap-4 text-center">
      <FileText size={48} className="text-acento" />
      <p className="text-xl font-bold text-titulares">
        {hasFilters ? 'No hay resultados para tu búsqueda' : 'No hay órdenes de compra'}
      </p>
      <p className="text-base font-normal text-texto/70 max-w-sm">
        {hasFilters
          ? 'Probá cambiando el filtro de estado o el nombre del proveedor.'
          : 'Todavía no creaste ninguna OC. Comenzá creando tu primera orden de compra.'}
      </p>
      {!hasFilters && rol === 'importador' && (
        <Link
          href="/importador/oc/nueva"
          className="inline-flex items-center gap-2 px-6 py-3 rounded-lg bg-principal text-white font-medium hover:bg-titulares transition-colors duration-150 min-h-[44px]"
        >
          <PlusCircle size={18} />
          Nueva OC
        </Link>
      )}
    </div>
  )
}
```

Named export. Sin default export. Sin `'use client'`.
  </action>

  <acceptance_criteria>
    - src/components/dashboard/OCTable.tsx existe
    - OCTable.tsx contiene `'use client'` en la primera línea
    - OCTable.tsx contiene `animate-pulse` (skeleton loading)
    - OCTable.tsx contiene `bg-fondo text-titulares border border-acento font-light` (badge borrador)
    - OCTable.tsx contiene `bg-acento/50 text-titulares font-light` (badge en_proceso)
    - OCTable.tsx contiene `bg-acento text-titulares font-normal` (badge en_transito)
    - OCTable.tsx contiene `bg-principal/20 text-titulares font-normal` (badge en_aduana)
    - OCTable.tsx contiene `bg-principal/10 text-principal font-normal` (badge entregada)
    - OCTable.tsx contiene `bg-texto/10 text-texto font-light line-through` (badge cancelada)
    - OCTable.tsx contiene `text-red-600 hover:text-red-700 hover:bg-red-50` (botón Trash2)
    - OCTable.tsx contiene `aria-label="Visualizar OC"`
    - OCTable.tsx contiene `aria-label="Editar OC"`
    - OCTable.tsx contiene `aria-label="Eliminar OC"`
    - OCTable.tsx contiene `min-h-[44px] min-w-[44px]` (touch targets)
    - OCTable.tsx contiene `overflow-x-auto` (scroll horizontal mobile)
    - OCTable.tsx contiene `min-w-[640px]` (tabla no se comprime)
    - OCTable.tsx contiene `hover:bg-acento/20` (row hover)
    - OCTable.tsx contiene `DeleteModal`
    - OCTable.tsx contiene `EmptyState`
    - OCTable.tsx contiene `export function OCTable`
    - src/components/dashboard/EmptyState.tsx existe
    - EmptyState.tsx NO contiene `'use client'`
    - EmptyState.tsx contiene `No hay órdenes de compra`
    - EmptyState.tsx contiene `No hay resultados para tu búsqueda`
    - EmptyState.tsx contiene `py-16 gap-4`
    - EmptyState.tsx contiene `href="/importador/oc/nueva"`
    - EmptyState.tsx contiene `export function EmptyState`
    - Ninguno de los dos archivos usa `any` o `as any`
    - `npx tsc --noEmit` no reporta errores
  </acceptance_criteria>

  <done>
    OCTable renderiza tabla responsive con badges de colores exactos del UI-SPEC, skeleton de 5 filas con animate-pulse, botones Eye+Pencil+Trash2 para importador y Eye+Pencil para proveedor/despachante, e integra DeleteModal. EmptyState muestra los dos estados (sin OCs / sin resultados de filtro) con CTA solo para importador.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descripción |
|----------|-------------|
| Render de strings mock | Strings del array MOCK_OCS se renderizan en JSX — en Phase 2 son datos ficticios controlados |
| Navegación por rol | Los botones Eye/Pencil/Trash2 se renderizan condicionalmente según `rol` prop |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Tampering | OCTable — prop `rol` | accept | En Phase 2 el rol es hardcodeado en la page. En Phase 5, el rol vendrá de `sessionClaims.metadata.role` en un Server Component — no hay superficie de tampering en producción. |
| T-02-02-02 | Elevation of Privilege | Trash2 button | accept | En Phase 2 no hay eliminación real — `onConfirm` cierra el modal sin efecto. En Phase 5, el Server Action `deleteOC` verificará el rol antes de ejecutar. |
| T-02-02-03 | Information Disclosure | DeleteModal — copy dinámico | accept | `ocNumero` y `proveedor` son strings del array mock. No hay XSS posible — React escapa strings en JSX por defecto. |
</threat_model>

<verification>
- Los 4 archivos existen en `src/components/dashboard/`
- `npx tsc --noEmit` sin errores
- StatCard no tiene `'use client'`, OCTable y DeleteModal sí tienen `'use client'`, EmptyState no tiene `'use client'`
- Todos los badges del UI-SPEC están presentes en OCTable con clases exactas
- Touch targets de 44px en todos los botones de acción
</verification>

<success_criteria>
Los 4 componentes existen en `src/components/dashboard/`, compilan sin errores TypeScript strict, usan las clases Tailwind exactas del UI-SPEC aprobado, y respetan las reglas de `'use client'` del proyecto.
</success_criteria>

<output>
Después de completar, crear `.planning/phases/02-dashboard-ui/02-02-SUMMARY.md`
</output>
